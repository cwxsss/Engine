[package]
name = "uc-application"
version.workspace = true
license.workspace = true
edition = "2021"
publish = false

[features]
default = []
lan-compat = []
test-support = []

[dependencies]
# Core domain models
uc-core = { path = "../uc-core", features = ["task-registry"] }
# Mobile-sync wire protocol codec (connect-uri). Leaf crate, zero internal deps.
# Portable observability contracts (events, flow correlation, stage constants)
uc-observability-contract = { path = "../uc-observability-contract" }

# Async runtime
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }
async-trait = "0.1"

# Error handling
anyhow = "1.0"

# Serialization
serde = { version = "1", features = ["derive"] }
# Phase 098 · v2 跨设备 person 聚合：A1 use case 构造 GroupIdentifyPayload.set
# 时需要 serde_json::Map / Value（与观测契约的 IdentifyPayload 同
# 形态）。本 crate 此前不直接拼 JSON，故新增依赖。
serde_json = "1"
postcard = { version = "1", features = ["use-std"] }
dirs = "6"
zip = { version = "2", default-features = false, features = ["deflate"] }

# Time handling
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2.0.17"
futures = "0.3.31"
tracing = "0.1.44"
sha2 = "0.10"
rand = "0.9.2"
blake3 = "1"
# Slice 2 Phase 2: carry ciphertext bytes across use case / facade / port
# boundaries without extra clones. Matches uc-core + uc-infra.
bytes = "1.7"
url = "2"
unicode-normalization = "0.1"
zeroize = { version = "1.8", features = ["derive"] }
# Semver parsing for upgrade-detection use case (`facade::upgrade`).
# Compares last_seen_version (port-stored String) against current build
# version to derive `UpgradeStatus`.
semver = "1"
# Concurrent TTL cache with bounded capacity. Used by ApplyInbound to dedupe
# rapid-replay inbound frames (snapshot_hash / visible_key → entry_id) without
# rolling a custom expiring map.
moka = { version = "0.12", default-features = false, features = ["sync"] }

[dependencies.uuid]
version = "1.10.0"
features = ["v4"]

[dev-dependencies]
mockall = "0.13"
tempfile = "3"
tokio = { version = "1", features = ["test-util"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[lints]
workspace = true
