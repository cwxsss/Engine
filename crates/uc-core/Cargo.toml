[package]
name = "uc-core"
version.workspace = true
license.workspace = true
edition = "2021"
description = "Core domain models and business logic for UniClipboard"
publish = false

[dependencies]
# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
postcard = { version = "1", features = ["use-std"] }
toml = "0.8"

# Time handling
chrono = { version = "0.4", features = ["serde"] }

# Error handling
anyhow = "1.0"

# Async traits
async-trait = "0.1"

# Bytes handling
bytes = "1.7"

# Async runtime (for mpsc channels in ports)
tokio = { version = "1", features = ["sync"] }
# Task registry needs rt + time; activated only via `task-registry` feature.
tokio-util = { version = "0.7", optional = true }
thiserror = "2.0.17"
serde_with = { version = "3.16.1", features = ["base64"] }
hex = "0.4.3"
url = "2"

# Deterministic digest derivation for removal intents and convergence state.
sha2 = "0.10"

# Single source of truth for the blake3v1 content-identity algorithm, shared
# with the mobile FFI crate so every implementation reaches the same hash.
uc-content-hash = { path = "../uc-content-hash" }

uuid = { version = "1", features = ["v4", "fast-rng", "serde"] }

# Stack-string for `Copy`-able device ids (固定 64 字节上限,详见
# `ids/device_id.rs`)。
arrayvec = { version = "0.7", features = ["serde"] }

zeroize = "1.8.2"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }

[features]
default = []
# Enables the `task_registry` module (adds tokio rt+time + tokio-util).
# Consumers that need TaskRegistry opt in; keeps the default dep footprint slim.
task-registry = ["dep:tokio-util", "tokio/rt", "tokio/time", "tokio/macros"]

[lints]
workspace = true
