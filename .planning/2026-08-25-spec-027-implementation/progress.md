# Progress

## 2026-08-25

- 读取并逐段核对 Spec 027 的目标、工作流、删除清单、边界情况、测试策略和 20 条验收条件。
- 恢复当前工作树状态，确认分支 ahead 4 且存在大量并行未提交改动。
- 搜索旧符号，确认当前实现离目标仍有实质缺口，不能把已有目录移动视为完成。
- 建立独立实施计划，开始需求与现状审计。
- 运行 application 测试清单和 all-targets 编译基线；当前均在旧接口/旧测试混杂处失败，尚不能形成非零测试清单。
- 读取当前 facade、SpaceModules 和三 runtime 组装，确认公开面与生命周期均不符合目标。
- 确认 application 生产库当前可编译，旧测试目标不可编译；后续先保持生产库基线，再逐步替换目标测试。
- 盘点 V2 历史和加入记录能力，确认核心签名规则可直接复用，无需在 application 重写。
- 搜索普通成员消费者，确认剪贴板路径存在成员表、内容门禁和成员范围三重判断。
- 写入并观察第一条目标失败测试：无当前 Space 时普通成员范围不可用。
- 建立 `membership_ledger` 的持久负载、加载/提交 port、错误和最终范围基础模型。
- 停止编译强制删除范围内的旧聚合测试树，修正一处独立初始化测试 fixture。
- 精确执行新测试：1 listed、1 passed、0 failed。
- 以失败测试补齐 V2 本机活动派生和一致关系对端范围，收缩未测试分支后 3 个 ledger 测试全部通过。
- 以失败测试补齐待本机决定的稳定暂停原因，聚焦测试通过。
- 以失败测试补齐 effect 未完成、本机加入未激活、分叉/无效/版本过旧范围门禁。
- 建立唯一 scope snapshot port，并以失败测试完成修订号溢出和历史摘要条件提交。
- ledger 模块 10 个测试通过。
- 建立 `QueryDeviceTrustUseCase`，以失败测试完成空状态、活动设备列表和当前待决定变化。
- query_device_trust 模块 3 个测试通过。
- 将聚焦测试切到独立 `/tmp/uc-spec027-target` 构建目录，消除外部工作区检查的锁竞争。
- 以失败测试建立 `RemoveSpaceMemberUseCase`，一次提交历史、关系、受限事件、Prepared 效果和 revision。
- 以失败测试让查询保留已移除设备的完整状态，同时普通 scope 继续排除它。
- 以失败测试补齐一次 CAS 冲突后的完整重试；移除模块 2 个测试通过。
- 以失败测试建立 `DecideDeviceTrustChangeUseCase` 的本机确认、接受、拒绝、重复和一次冲突恢复。
- 决定提交与移除共享 ledger、查询和维护唤醒边界；决定模块 5 个测试通过。
- 以失败测试建立入站历史完整用例；完成单页幂等、两页先存后应用和 Prepared 效果。
- 增加入站乱序和 transfer 替换回归；入站模块 4 个测试通过。
- 将旧发送类型明确改名为 Legacy，仅供待删除旧组装引用；目标名称切换到完整新发送用例。
- 以失败测试完成全量范围排序去重和单对端 deferred；发送模块 1 个测试通过。
- 建立维护用例固定顺序、Corrupt 截止和 PeerOnline 精确路径；3 个用例测试通过。
- 建立唯一成员 runtime，验证 startup/pause/resume/presence/shutdown；维护模块共 4 个测试通过。
- 对外开放加密 ledger 负载/原子提交 ports，并新增 `SpaceApplication` 单一目标组装。
- 将普通 clipboard/roster/resend/delivery 消费者切换到最终 scope，删除双重内容 gate 判断。
- 普通消费者旧 gate/scope 符号搜索归零；45 个迁移回归通过。
- `cargo check -p uc-application --all-targets --locked` 通过；强制删除审计确认旧 facade/admission/runtime/aggregate 仍待一次切换。
- `SpaceFacade` 已改用新 ledger 范围和新 `SpaceApplication`，不再构造旧准入、旧成员总对象或邀请方后台循环。
- 将成员名单、偏好、保护状态和在线订阅收进 `SpaceFacade`；`MemberRosterFacade` 改为 crate 内部实现，`AppFacade` 只转发到唯一 Space 入口。
- 新增单条准入入站用例；加入记录、V2 历史、关系和 Prepared 效果一次保存，保存成功后才返回回复，聚焦原子测试通过。
- 加入恢复改为每封待发送消息重载最新记录版本；两封连续确认的失败测试转绿。
- 重建 Space 改为一次清空 ledger 中旧历史、关系、传输、效果和加入记录；原子清理测试通过。
- 删除旧成员总对象、旧准入 owner、旧 gossip、旧 store、旧综合状态、旧 runtime、旧 side facade、旧诊断和旧浅测试文件。
- 删除独立成员连接 runtime；暂停现在先通知网络工作停止，再等待当前保存边界完成，聚焦生命周期测试通过。
- 接收门禁改为一次取得操作内授权凭证，解密后复用同一 revision；单次读取测试通过。
- 邀请判断忽略已不在当前 V2 历史中的旧关系，回归测试先失败后通过。
- 新增捕获日志测试，确认代表性设备标识和地址不进入 reachability 日志。
- application all-targets 编译在旧路径删除后重新通过；架构脚本已改为检查目标入口、强制删除项和禁止旧符号。
- 新准入入站入口复用 application 自己的邀请持有者；新尝试必须校验同一邀请码与当前代次，并把邀请码摘要和正式加入事实同次提交，成功后才消费内存邀请。
- 会话锁定、恢复、创建、解锁和访问查询全部收进 `SpaceFacade`；顶层 `AppFacade` 不再持有或公开这些内部用例。
- 删除 facade 的成员刷新、候选枚举和单设备拨号入口，并删除独立 reachability 用例；统一成员后台的历史同步按最终范围处理联系与恢复。
- 持久 ledger、准入输入和准备结果的 Debug 输出已脱敏；application 源树中的设备、地址、文件名和路径 tracing 字段搜索归零。
- 架构守卫新增目标目录、强制删除、禁止旧符号、内部类型公开、旧 scope 绕过和敏感 tracing 字段检查，完整预检通过。
- 规格要求的 Space 测试清单确认 102 项非零，102 项单线程全部通过；完整 application 646 项全部通过。
- `cargo check -p uc-application --all-targets --locked`、`cargo fmt -p uc-application -- --check`、架构预检和 `git diff --check` 通过。
- 严格复审补齐跨 Space 完成时沿革、本机成员实例和关系基线的原子切换；目标 Space 回归测试先失败后通过。
- 历史同步现在包含可核对的未确认成员，排除已移除、分叉和无效设备；入站历史也在保存任何分页前验证来源仍是当前活动成员。
- 定时维护在存在未确认关系时继续重试历史同步；并发上线事件使用去重队列，不同设备不再互相覆盖。
- 移除和决定在正式提交后、返回前尝试推进当前效果，重复决定也补做未完成效果；失败不回滚并继续唤醒后台。
- 统一 runtime 关闭的网络取消、保存等待和任务退出共用一个 5 秒截止时间；超时不打断已开始的保存任务。
- 创建、解锁、恢复、切换完成和重置成功后显式安排状态变化维护；AddDevice 最终启用后清除重建产生的重新配对提示。
- 活动剪贴板的全量发送、上线补送和拉取接收均在同一操作内复用一份 scope/授权快照，不再按目标或阶段重复读取修订。
- 最终 Space 测试清单为 113 项，113 项单线程全部通过；完整 application 657 项全部通过。
- 整仓 `cargo check --workspace --all-targets --locked` 已运行，失败仅来自 Infra/Engine 仍引用本规格明确删除的旧 application 接口及一处外层现存测试类型缺失；未恢复旧接口。
- 整仓 `cargo fmt --all -- --check` 已运行，application 通过，剩余差异只在本规格外的 Engine/Infra 工作树文件。
- 恢复发送收到稳定拒绝或邀请码消费结果后会原子结清对应 outbox，不再由定时维护永久重发。
- `SpaceFacade` 最后两处直接持久读取已移入完整查询或删除；facade 不再持有成员仓储、重建进度或当前 Space 读取 port。
- 成员范围快照现在贯穿活动剪贴板的全量发送、上线补送和拉取接收，同一操作不会按目标或阶段重新读取修订。
