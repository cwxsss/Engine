# 进度

## 2026-09-07

- 用户明确要求现在重构成员历史，并理清 Core/Application 归属。
- 已读取 codebase-design、planning-with-files；既有 test-driven-development 技能与失败测试继续使用。
- 已接收此前 F2 测试完成结果，当前没有正在运行的 Cargo 验证。
- 正在确定私有模块切分与完整证据处理的迁移点。
- 已在拆分前实际执行字节基线测试，得到存档、完整证据页与后缀页的三个固定 SHA-256；预期占位断言失败后写入实测值，后续拆分必须保持一致。
- Core 已机械拆为私有模块，所有原始代码行均有明确归属；原文件已删除，无新旧并行入口。
- 首次编译发现机械调整误给 trait 方法加了内部可见性，并有三处不再需要的导入；已按编译结果修正，未改业务行为。
- Core 39 项测试全部通过，三份字节校验值保持一致，无新增编译警告。
- 已提取 ReconcileMembershipEvidenceUseCase，入站和主动核对都交给同一完整流程；从账本删除原实现，不留转发兼容方法。
- Application 全部 749 项测试通过，零失败、零跳过；沿用既有三项无关警告。
- Core 入口 83 行，内部最长文件 history.rs 430 行；Application 完整证据流程 228 行。
- Engine 148 项库测试通过，零失败、零跳过；当前执行修复后的真实流程复跑与最终检查。
- 增加成员历史职责文档、Space 地图和架构圣经正文；维护旧文档链接并通过链接检查。
- 仓库检查新增 Core 流程、账本证据流程和公开私有模块三项反例；脚本语法检查通过。
- 重构后重新编译并串行运行两个真实场景：四实例接受/保留/重启与五实例 F2 远端分支恢复均通过，2 passed、0 failed，184.69 秒。
- 全 workspace all-targets check、完整 locked metadata（14 个 workspace 成员/809 个依赖包）、格式及 diff 检查通过。
- 架构、隐私、Collector 合同和 OpenMLS 6 项验证通过；新增三个所有权反例均被正确拒绝。
- 修复执行记录移入 completed，更新所有引用和架构圣经；没有提交或推送。
- 工作区中已有暂存的 handle_history_message/tests.rs，暂存状态保持不变；其余用户修改未撤销。
