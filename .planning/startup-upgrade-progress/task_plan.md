# 启动资料升级进度

状态：本批只读进度已完成，验证范围见正式交付记录。

## 完成标准

- 保持 `Engine::start` 兼容；新入口在启动前接收单次进度输入，宿主只执行一次启动。
- 升级由 `ProfileStorageUpgrade` 完整负责；Engine 只保存产品摘要和启动终态，不读取恢复文件。
- 最新快照、有限步骤记录、尝试标识、单调序号可在启动失败后读取；慢接收者和断开不影响升级。
- 从实际转换计数，区分内容表示、大块内容和附属记录；未知总量不造百分比，处理不冒充耐久提交。
- 恢复重建计数与警告，保留真实失败分类；本批不增加启动前密码输入或绑定入口。
- 验证真实存储、终态、断开、重复尝试、中断恢复；运行项目门禁和可达 Windows 真机验证。
- 给 Desktop 提供接入说明和可消费代码；不修改 Desktop，不新增 CI。

## 阶段

1. 已完成：交接与职责核对、最新主线、构建环境恢复。
2. 已完成：只读进度契约和最小端到端行为测试。
3. 已完成：真实计量、警告、恢复和错误分类。
4. 已完成：本机及 Windows 验证、接入说明和架构文档；完整产品矩阵的剩余项目明确交回 Desktop 后续验收。

## 责任

- 启动生命周期：Engine；升级及已有持久恢复：ProfileStorageUpgrade。
- 成功：返回完整 Engine，状态 Ready；升级完成本身不意味着 Ready。
- 失败：保留最终安全分类及最后步骤；宿主确认旧任务结束后创建新尝试，既有跨进程租约保护存储。
- 进度不持久化，不参与恢复、授权或日志；关闭窗口仅丢弃观察者。

## 环境问题

- 首轮只读 Git 与编译缓存权限被拒绝；用户已解除限制，fetch 成功，基线 check 正在运行。
- 当前 target 和 sccache 均复用外置共享目录；未更换或清空缓存。
