# Progress

## 2026-08-25

- 建立独立规格调查计划。
- 确认本任务不继续生产代码迁移，只收集证据并产出一次性重写规格。
- 读取 `to-spec`、`codebase-design` 和 `planning-with-files` 的完整要求。
- 初步盘点 `space` 目录，确认当前处于新旧职责混杂的中间状态。
- 完整读取 `rebuild_space` 参考实现，提取单入口、内部阶段、持久化恢复和错误分段标准。
- 盘点 facade、Engine 分派和 `SpaceModules` 暴露面，确认成员行为入口与内部实现同时散落。
- 盘点 Space 生命周期和准入用例，记录创建、解锁、恢复、锁定、重置、升级、加入、取消和跨 Space 切换的不变量。
- 追踪当前成员范围、成员移除、决定、历史同步和 runtime，固定授权来源、失败关闭、幂等和后台恢复事实。
- 对照 ADR-020/021 与 Specs 021-024，确认新规格需取代渐进实施顺序，但保留既有产品和安全承诺。
- 核对 Engine、绑定和验收宿主的正式操作，区分正式设备信任合同与仅开发模式使用的旧综合状态入口。
- 盘点 runtime、应用接口和 Infra 存储，确定统一 runtime 与单一加密提交面的目标方向。
- 完成目标模块、持久模型和接口设计；发现现有加入/成员日志含禁止记录的标识，纳入强制清理范围。
- 新增 ADR-025 和规格 027，正式取代 ADR-021/规格 024 的渐进实施方式。
- 更新文档索引和架构总览，明确目标已批准但生产实现尚未切换。
- 按用户补充要求将规格收紧为纯 application 层；移除 Core、Infra、Engine、绑定、数据库和真实网络的实施与删除项。
- 验证规格 11 个必需章节齐全，本地文档链接和文档范围差异检查通过。
- 仓库依赖清单通过；全量格式、架构和 all-targets 编译被当前未完成的生产迁移阻断，具体错误已记录。
- `cargo check -p uc-application --lib --locked` 通过；文档实施路径检查确认只包含 application 和文档。
