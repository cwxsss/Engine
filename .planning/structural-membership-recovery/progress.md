# 执行记录

## 2026-09-07

- 用户已授权结构性修复，要求先看到红测。
- 已读取测试先行与计划技能，核对 Engine 根及 Core/Application/Infra 规则。
- 工作区初始干净；不提交、不推送。
- 当前正在定位既有测试夹具与完整负责人，尚无生产代码修改。
- 红测一已确认：`cargo test -p uc-core --test membership_history_v2 suffix_preserves_verified_receiver_side_branches_and_local_decisions -- --exact`，1 项真正断言失败，错误为 `InvalidPersistedHistory`；不是编译失败。
- 已扩展真实 Engine F2，用远端分支选择后重启及完整成员查询验证旧清理问题，正在执行。
- 首次 F2 命令遗漏 `--features dev-tools`，实际执行 0 项，不计验证；已使用正确开关重跑。
- F2 正确入口执行 1 项通过（129 秒）；证明该拓扑未稳定触发清理，不把它冒充红测。已用真实 SQLite 成员仓储和冻结故障等价的完整 ledger 输入补充确定性清理契约红测。
- 清理测试首次编译发现测试 helper 不跨模块导出、事件 ID 私有构造器误用；已仅在 cfg(test) 复用 helper，并改用公开 ID 解析。这次编译失败不计红测。
- 红测二已确认：`cargo test -p uc-infra --lib selected_branch_keeps_active_members_despite_old_completed_removal`，1 项真正断言失败，真实 SQLite 中有效成员被删除。两条确定性红测均已具备，开始结构性修复。
- 已替换旧 Infra 清理模块：Application 生成当前投影计划；Infra 在同一 SQLite 事务中校验 ledger 修订并维护成员、可信身份、地址。清理红测已绿（1 项），旧类已删除。
- 新红测 `old_branch_incomplete_effect_does_not_pause_a_current_member` 按预期失败；正在把历史效果日志与当前可执行视图收口，V3 换组改用 Application 的完整目标记录构造。
- 当前效果视图红测已绿（1 项）。成员单元组首次扩大运行 98/101 通过，三项旧夹具把不属于任何当前历史的伪造 event 当作待执行任务；已改为建立真实因果历史，保留原有阶段顺序和暂停断言。
- Ledger 组 22 项通过；V3 完整分支 transition 各阶段崩溃重放 1 项通过。历史增量红测仍为红，开始明确证明范围的 V4 交换替换。
- V4 归档证明范围实现后，原历史误拒绝红测已绿（1 项），Engine dev-tools 全目标 check 通过。旧存储迁移红测确认 3≠4 后加入 V4 codec，尚待完整复验。
- 新红测 `invalid_current_peer_recovers_only_after_complete_verified_evidence` 确认旧 Invalid 永久跳过；现实现只对当前已验证成员做完整证据恢复，内容权限仍由原 scope 独立关闭，正在复验。
- Invalid 恢复红测已绿。补充恶意发送者夹带证明范围外记录的红测，确认原新协议会接受后，已加到统一证明覆盖验证，现绿（1 项）。
- 新增并确认迟到历史 ACK 覆盖新分支关系的红测；回复提交现绑定发送时的历史位置，过期回复归为 Superseded，不修改新关系，正在复验。
- 迟到 ACK 红测已绿；成员组扩大到 103 项通过。旧存储 V1/V2/V3→V4 写入迁移测试通过。
- Core 组 40/41 通过，唯一差异为显式 V4 新帧的固定字节向量；已核对 V2 历史和完整证据字节未变化，仅更新 V4 向量。
- 新增“新传输取代旧暂存，迟到旧页不能隔离成员”红测并确认失败；分页暂存现由 InboundMembershipTransfer 的单一状态转换负责，正在复验。
- 分页状态机红测已绿；重构时遗留一处日志计数变量导致编译失败，已从完整暂存对象读取计数修复，不计入红测。
- 成员资料契约补验丢失成员重建与过期计划不得清理数据，正在运行。
- 成员资料重建与过期计划保护已通过；workspace 全目标 check 和架构/隐私 preflight 均通过。
- Desktop 后台与实际 E2E GUI 已按新 Engine 重建；原先失败的四配置 `cross-remote` 已通过，含真实文本接收和重启查询，证据在同级 Desktop `.cache/device-group-e2e/run-mtr7ink4/`。
- 全仓测试在 Engine lib 的关闭同步重发用例失败：文件已成功送达后使用空目标（差集发送）先返回 NoEligibleTargets，没走到同步关闭检查。已把目标明确为接收端，保持 SynchronizationDisabled 的原断言，正在复验。未改动重发生产逻辑。
- 五配置 `new-peer` GUI 场景正在运行。
- 五配置新场景已通过，真实 A→E 文本在 GUI 可见且排除关系发送被拒绝：同级 Desktop `.cache/device-group-e2e/run-mtr7spcm/`。
- 新增仅操作测试身份的冻结现场恢复入口；四配置旧故障副本已在 5.4 秒内恢复查询和实际发送，证据 `.cache/device-group-e2e/run-mtr8kd1j/`，原失败资料校验值未变化。
- 全仓重发测试用显式文件/正文目标仍返回 PayloadLost；已确认发送计划把关闭同步折叠成不可用载荷。恢复该测试文件全部本轮试改，使用干净 fe55e543 临时源码树对照，不修改无关重发生产逻辑。
- 外置盘顶层不允许创建临时源码目录；改在系统临时目录建立干净源码树，构建仍复用原外置 Engine target，未创建新的构建目录。
- 干净 fe55e543 使用 dev-tools 实际执行关闭同步重发测试，复现同样的 NoEligibleTargets 与 SynchronizationDisabled 差异。临时源码工作树已移除，共享外置构建目录保留。原测试本轮改动为空；扩大回归明确排除此已证实旧问题，不修改无关重发规则。
- 五配置冻结副本首次业务断言通过，但运行器因晚退出进程的可执行文件检查退出失败，不计完整通过。Desktop 清理工具先以已退出进程复现真正红测，再统一资料归属与存活检查，保留对其他可执行程序的拒绝。
- 五配置冻结副本完整复测通过，退出码 0：同级 Desktop `.cache/device-group-e2e/run-mtr97qhm/`，包含真实接收截图和排除 C 的发送断言，测试后台正常清理。
- Desktop 增加非空 WAL 快照拒绝的红测并修复，避免冻结资料复制时遗漏未合并写入；旧冻结来源只读校验，未原地修复。
- 扩大全仓运行在真实多实例测试中出现 F0/F1/F2/F4/F5/F7 失败，且被用户中断，未拿到完整终态，不能计全仓通过。随后改为分离 GUI 与复杂 Engine 实例测试。
- F2 单独运行实测失败在候选名单断言：旧测试取所有事项的第一个远端候选，实际取到 5 人祖先组，而场景要求 B 的 4 人组。保留 4 人名单、影响、重启及隔离断言，测试工具改为按指定 branch 的公开 choiceId 等待并选择，不假定候选顺序。
- 临时基线对照曾在共享 target 留下另一 feature 组合的旧 Core 产物，独立命令报 V4 符号缺失，而当前源码明确存在。仅更新 Core 编译入口时间触发重新编译，未清空 target 或 sccache；重新编译已成功。
- Desktop 第一次扩大矩阵已完成前 9 轮，其中第 6 轮 new-peer 因 WebDriver elementId 缺失失败，其余 8 轮通过；第 10 轮被中断，不计结果。中断后专门清理本轮测试后台与已失效的空 GUI 租约。
- 点击 helper 的页面查询与驱动元素查找存在两套等待，已用替换按钮夹具观察红测，改为同一个驱动元素的存在/显示/启用等待后单次点击；helper 15 项通过，完整前端 180 文件 1136 项通过。
- F2 按指定候选独立复验通过：1 项、163.97 秒，含新增重启后的完整成员查询。日志 `.cache/membership-recovery-checks/f2.log`。这证明该项扩大失败来自候选顺序假设，不把其余 F0/F1/F4/F5/F7 尚未单独复验的失败一并归为测试问题。
- 排除 Engine 自身测试的其余全工作区测试已完整通过，包含 Core、Application、Infra 和绑定测试；Infra 792 通过、4 项原有忽略。扩大检查发现并修正请求布局测试仍写死旧协议 3 的遗漏，保持新协议 4 固定字节断言并新增旧 3 请求拒绝断言。
- 架构/隐私 preflight 再次完整通过；四/五冻结来源再次重新计算校验值均未改变。
- F0/F1/F4/F5 串行通过。F7 的失败为即时发送返回 pending=1，旧断言只允许 accepted=1；已对照真实发送合同和既有 F1，统一为同组允许有界后台发送，但最终必须实际收到原文，跨组 accepted+pending 必须为 0。
- F7 单独完整通过，1 项、417.06 秒，十实例 90 个不同设备对的实际收发/隔离断言全部完成。再次启动 Desktop 完整矩阵，前一次中断与测试工具失败证据保留。
- 最后核查确认新投影仍继承旧清理的仅完整轮次限制：暂时失败后定期任务遗漏恢复。新增真正红测（0≠1），将本地投影核对纳入每轮维护，无新增重试旗标；维护 9 项、Application 758 项通过。
- 第二次 Desktop 矩阵 `.cache/device-group-e2e/suite-mtrcecg8/`：11 轮通过，new-peer 在选择被新资料清除后仍等待确认启用而失败，系统键盘环境阻挡。确认 helper 改为等待/重选/已提交状态转换，实际观察两条红测后修复，保留单次提交与未知结果核对。
- 前端最终 180 文件、1142 项通过；后台和 GUI 再次从含定期投影恢复的 Engine 构建成功，第三次完整矩阵正在运行。
