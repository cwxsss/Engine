# 成员同步记录可解释性

完整业务负责人仍是 Application 成员同步用例；Engine 只装饰既有完整交换能力，Infra 在协议边界提供固定请求用途和失败分类。调用和业务结果、重试恢复、线上消息格式均不改变。

验收：导出节点直接携带固定结果/错误；成员请求名称区分用途与发送/处理回复；读取、超时、格式和解码失败不再全部只显示传输失败；双端父关系与隐私过滤保留。Jaeger 实际查看新记录，不修改旧记录伪造证据。

先补失败测试，再实现；复用现有类型化合同、运行时过滤和 Collector 校验。保留其他存储/构建目录工作。

## 完成状态

本轮单次交换可解释性修复已完成，未提交、未推送。

- [x] 真实 OTLP 节点直接包含固定结果及失败类别；非法结果、自由文本错误与矛盾组合被拒绝。
- [x] 真实双 Engine 交换两端完整、名称含用途，结果与日志一致。
- [x] Jaeger 已展开查看暂停原因；正常交换两端父子关系已检查。
- [x] 成员协议 9 项、网络暂停门 1 项、Engine 观测 6 项、观测合同和运行时 50 + 42 项通过。
- [x] 配对完整 trace 与剪贴板双 Engine 回归通过。
- [x] 全目标编译、完整 metadata、格式、架构/隐私和 diff check 通过；保留既有无关警告。

没有改写旧 trace，也没有把单次网络交换冒充完整成员收敛。实际产品宿主矩阵与真实 PostHog 项目投递未执行。
