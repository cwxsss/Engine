# Progress

## Session: 2026-09-15

### Phase 9-12: Cancellation, In-flight Request and Regression

- **Status:** complete
- 用户要求继续后续故障测试；本轮目标为切换取消和真实在途业务请求。
- 已恢复规划、测试先行与 Rust 开发规则，完成标准保持旧权限不复活、单网络、当前进程恢复和三秒性能门。
- 已确认工作区存在用户自己的架构记录改动和未跟踪 `skills/` 目录；本轮保留并排除出提交范围。
- 下一步先盘点既有 Blob、文件传输、网络分区和生命周期测试能力，选择最小真实红测。
- 已从规格确认发布后取消和在途文件/Blob 是剩余验收目标；先用公开 suspend/resume 覆盖生命周期取消，再为一个真实 Blob 请求寻找最窄的可控停顿。
- 已新增第一颗公开行为测试：切换准备失败进入恢复空窗后执行 suspend，越过 watcher 的一秒重试时点再 resume；预期网络总构造次数恰为两次并完成目标加入。
- 生命周期取消测试直接通过，耗时 11.66 秒：暂停没有被旧 watcher 绕过，恢复只重建一次网络并完成目标加入；该场景无需修改生产逻辑。
- 已确认在途网络测试不能只靠大文件或竞速；下一步寻找 Blob handler 现有测试控制或最窄的测试专用停顿，保证请求确实进入旧会话后才触发切换。
- 网络分区门只负责连接拒绝与关闭，不提供“请求已取得旧会话权限”的确定性信号；继续检查共享会话注册表现有挂起测试，优先复用而非新增 Blob 专用开关。
- 已确认共享网络层已有挂起连接跨代际关闭的直接证明；下一颗测试改为真实 `SendFiles` 操作在途时切换，补齐 Engine 操作门、取消和后继会话可用性的端到端证据。
- 已确定文件测试不能永久阻塞同步宿主读取；将使用多分块短延迟文件，使发送操作稳定跨过两秒宽限期并能在分块间响应取消。
- 已核对取消时序和 64 KiB 分块读取点；准备加入只在本测试使用的慢文件宿主，并先运行端到端场景观察红绿结果。
- 额外发现文件导入临时目录只覆盖显式错误清理，future 在取消点被丢弃可能遗留目录；新测试会把“取消结果、切换完成、临时目录清空”作为同一公开行为验收。
- 已加入慢分块宿主和真实文件发送端到端测试；现在运行红测，预期若缺少取消清理保护会在临时导入目录断言处失败。
- 红测失败但先暴露更早的问题：旧文件发送返回成功，而不是取消。原因是取消与操作结果同时就绪时没有确定优先级；进入最小修复后复跑同一测试。
- 加入取消优先级后测试仍在约五秒结束为成功，证明三秒文件窗口早于真正切换；已改为七秒读取，下一轮将实际覆盖取消时点。
- 七秒在途文件发送测试通过，耗时 12.76 秒：两段排空等待均超时后切换继续，旧发送最终返回取消，临时导入为零，目标加入完成。
- 已加强在途文件测试：补充网络构造次数保持一次、入口不变，以及新会话完成一次真实正文接收，避免只验证成员状态。
- 加强后的在途文件测试通过，耗时 12.67 秒；旧发送取消、临时导入为零、长期网络只构造一次，新会话正文接收成功。
- 已核对发布到监督状态之间没有异步让出点，不制造不存在的取消窗口；进入性能和全仓回归。
- 两项新增场景串行复验通过：暂停场景 12.28 秒，在途文件场景 12.33 秒；三秒交接门为 1.853 秒。
- 旧连接关闭的既有网络测试通过；Engine 库测试为 167 项通过、2 项按设计跳过。
- 全仓 metadata、check、格式、Rust 规范、Engine 仓库规则和差异检查全部通过；仅观察到未改动鸿蒙测试文件已有的未使用导入提醒。

### Phase 6: Remaining Failure Discovery

- **Status:** complete
- 用户要求继续覆盖其他故障场景；已重新读取执行计划、测试先行与 Rust 开发规则。
- 继续沿用已确认的公开 Engine 操作测试边界，先盘点切换流程中的真实失败位置。
- 已确认下一轮两个最小场景：持久切换完成失败，以及新会话准备成功但发布失败。
- 补充确认切换封口失败可由既有一秒重试恢复，因此本轮覆盖封口、持久切换完成和新会话发布三类失败。
- 新测试按预期因缺少统一故障点合同而编译失败，红态成立。

### Phase 7: Remaining Failure TDD

- **Status:** in_progress
- 已把上一轮单一准备失败开关收敛为四个明确故障点共用的一套测试控制和匿名计数，避免继续堆叠独立动作。
- 连续两次旧会话封口失败的真实双端测试通过，当前进程随后恢复，网络只构造一次。
- 持久切换完成失败场景首次运行暴露停止重试：第二次失败后返回 1103 且不可重试；已中止测试，正在核对真实错误转换。
- 已确认不是单纯测试类型错误：现有 1103 转换固定不可重试。已改为运行期可恢复分类并增加直接单元检查，准备复跑端到端场景。
- 1103 单元检查和连续两次完成失败的真实双端测试已通过，端到端场景耗时 8.09 秒。
- 新会话发布失败场景未恢复：第一次清理约 1.6 秒后进入长等待并出现中继超时。已中止运行，转查准备后失败的清理边界。
- 已确认失败清理对象不直接拥有长期网络；下一步用窄化日志定位第二次准备卡在 Application 启动还是会话装配。
- 将先把发布失败缩成一次，排除“第二个注入仍未消费”造成的误判，并开启会话关闭阶段日志。
- 单次发布失败同样无法恢复；日志确认失败会话的三段关闭都已完成，随后 watcher 已计划重试，但没有产生第二次发布尝试。
- 进程采样只显示异步运行线程停泊，无法还原具体 future；下一步增加测试专用阶段计数，不把临时诊断日志留入最终实现。
- 代码路径已缩小为会话网络能力准备、Application 组装与 Application 启动三个阶段；将加临时窄化检查点，定位后删除。
- 临时检查点已定位：恢复卡在新的网络会话准备内部，Application 尚未再次启动；已停止复现，转查尚未发布 handler 内的 Blob 存储关闭责任。
- 根因已确认：未发布 handler 直接丢弃，没有执行异步关闭，Blob 存储任务继续占用文件。下一步给未发布会话增加唯一清理动作，并让发布失败路径调用它。
- 修复范围确定为 Infra 未发布会话关闭、注册表返还失败集合、Engine 注入失败调用关闭；不会移动正常已发布代际的关闭责任。
- 最小修复后单次发布失败测试通过，耗时 10.99 秒；恢复阶段重新完成了网络会话准备和 Application 启动。
- 已移除临时定位日志，并恢复连续两次发布失败目标；下一步补注册表拒绝发布的直接清理测试。
- 注册表拒绝发布后的直接清理测试通过，确认失败集合可关闭且不会误退役当前活动会话。
- 四类连续故障矩阵全部通过，4 passed，耗时 36.73 秒；每类均消费两次故障并在当前进程恢复。
- 进一步收紧完成错误分类：临时状态错误继续由后台恢复，明确没有活动加入的终态不再无限重试；两类直接检查均通过。
- 故障修复后三秒交接门复测通过：端到端 1.695 秒，本机 1.561 秒，网络 0.134 秒。
- Infra 的失败清理与同入口会话替换直接测试通过；Engine 单元和宿主合同测试 167 项通过、2 项按既有条件忽略。
- 最终差异审查把“拒绝后返还资源再由节点关闭”收回注册表内部，节点只处理发布成功或失败；直接清理、同入口替换和连续两次发布失败测试再次通过。
- 收口后的三秒交接门再次通过：端到端 1.842 秒；全仓编译、格式、编写规范、架构边界、隐私和差异检查全部通过。

### Phase 1: Discovery

- **Status:** complete
- 已确认测试 seam 为现有 Engine 操作；测试专用能力只制造下一次会话准备失败并读取匿名计数。
- 已读取规划、TDD 和 Uni Rust 开发规则。
- 已盘点现有测试能力：可查询网络 endpoint、控制网络分区，但没有会话准备失败开关。

### Phase 2: Red Test

- **Status:** complete
- 确认首个场景为“持久切换后下一次会话准备失败一次，旧 endpoint 保持，后台重试后配对完成”。
- 确认测试期间可用已有 endpoint 查询证明没有重绑，但需先去除该查询对活动 facade 的无关依赖。
- 红测按预期因缺少 `FailNextSessionPreparation` 和对应结果而编译失败。

### Phase 3: Minimal Implementation

- **Status:** complete
- 增加 Engine 内部一次性故障开关，并把无会话依赖的 dev 操作从活动会话获取中分离。
- 第一次绿色运行通过：故障实际发生，当前进程约一秒后完成恢复。

### Phase 4: Failure Matrix

- **Status:** complete
- 加强后的场景连续注入两次会话准备失败，验证旧操作保持关闭、网络构造次数保持为 1，并在当前进程第三次尝试自动恢复。
- 定向测试通过，1 passed，耗时 9.08 秒。

### Phase 5: Verification and Commit

- **Status:** complete
- 三秒配对目标复测通过：端到端 1.844 秒，本机 1.706 秒，网络 0.138 秒。
- 已更新规格 043 和架构圣经；下一步执行完整交付检查。
- 故障场景在锁定依赖下复验通过；Engine 单元与宿主合同测试 165 项通过、2 项按既有条件忽略。
- 全仓编译、格式、编写规范、架构边界、隐私和差异检查全部通过。
- 最终差异审查未发现会影响正常版本的脏实现；测试开关和计数只存在于开发测试版本。

## Test Results

| Test | Expected | Actual | Status |
| --- | --- | --- | --- |
| repeated session preparation failures | 两次失败、旧权限关闭、单网络入口、自动恢复 | 1 passed, 9.08s | passed |
| three-second handover gate | 端到端小于 3 秒 | 1.844s | passed |
| uc-engine lib | 全部可执行项通过 | 165 passed, 2 ignored | passed |
| workspace delivery gates | 全部通过 | metadata/check/fmt/style/repository/diff passed | passed |
| repeated session quiesce failures | 两次失败、旧权限关闭、单网络入口、自动恢复 | 1 passed, 9.28s | passed |
| repeated handover failure matrix | 四类各连续失败两次并自动恢复 | 4 passed, 36.73s | passed |
| rejected unpublished session cleanup | 失败集合完整关闭且当前活动会话不受影响 | 1 passed | passed |
| three-second handover gate after failure fixes | 端到端小于 3 秒 | 1.695s | passed |
| three-second handover gate after cleanup refactor | 端到端小于 3 秒 | 1.842s | passed |
| uc-engine lib after failure fixes | 全部可执行项通过 | 167 passed, 2 ignored | passed |
| suspend during transition recovery | 旧 watcher 不复活网络，resume 只重建一次并完成加入 | 1 passed, 12.28s | passed |
| in-flight file send during transition | 旧发送取消、无临时残留、单网络、新会话正文可用 | 1 passed, 12.33s | passed |
| three-second gate after cancellation fix | 端到端小于 3 秒 | 1.853s | passed |

## Error Log

| Error | Attempt | Resolution |
| --- | --- | --- |
| 红测 patch 未找到预期的后继测试名称 | 1 | 已读取实际上下文，改在 `existing_device_switches_space_through_stable_operations` 后插入 |
| 红测缺少故障注入合同而编译失败 | 1 | 预期的 TDD 红态，开始最小实现 |
| 测试把正常切换空窗误当成注入故障，计数仍为 0 | 1 | 改以故障消费计数作为观察起点，随后检查普通操作关闭 |
| 新故障矩阵缺少统一故障点类型和结果字段 | 1 | 预期 TDD 红态，开始最小实现 |
| 持久切换完成失败后 watcher 不再重试 | 1 | 保留为真实红态，先判断注入错误是否匹配生产失败语义 |
| 完成错误文件旧路径不存在 | 1 | 使用搜索返回的目录内 `error.rs` |
| 会话发布失败后后台恢复长时间无进展 | 1 | 保留为红态，检查已启动会话的失败清理是否完整且不会破坏长期网络 |
| 规划文件追加 patch 的旧行上下文不完整 | 1 | 读取当前尾部后按完整行更新 |
| 单次发布失败仍长时间无恢复 | 1 | 排除第二次注入影响，进入阶段计数定位 |
| macOS 进程采样没有可用异步调用栈 | 1 | 改用测试专用匿名阶段计数 |
| 注册表关闭测试缺少错误类型导入 | 1 | 已补同模块集中导入，准备复跑 |
| 带基线的编写规范检查因分支差异超过子进程默认输出上限而退出 | 1 | 改用仓库规定的默认工作区差异检查，当前改动全部通过 |
| 发布失败定向测试过滤名少了 `committed_` 前缀，实际运行 0 项 | 1 | 读取真实名称后以完整名称复跑，1 项通过 |
