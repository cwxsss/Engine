receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

processors:
  filter/diagnostics:
    error_mode: propagate
    trace_conditions:
      - 'resource.schema_url != ""'
      - 'Len(resource.attributes) != 9'
      - 'resource.dropped_attributes_count != 0'
      - 'resource.attributes["service.namespace"] != "uniclipboard"'
      - 'resource.attributes["service.name"] != "uc-engine"'
      - 'IsString(resource.attributes["service.version"]) == false'
      - 'Len(resource.attributes["service.version"]) > 32'
      - 'IsMatch(resource.attributes["service.version"], "^[0-9]{1,5}[.][0-9]{1,5}[.][0-9]{1,5}(-(alpha|beta|rc)([.][0-9]{1,5})?)?$") == false'
      - 'IsString(resource.attributes["service.instance.id"]) == false'
      - 'IsMatch(resource.attributes["service.instance.id"], "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$") == false'
      - 'IsMatch(resource.attributes["deployment.environment.name"], "^(development|test|staging|production)$") == false'
      - 'IsMatch(resource.attributes["os.type"], "^(ios|android|macos|windows|linux|ohos|other)$") == false'
      - 'IsMatch(resource.attributes["host.arch"], "^(arm64|arm|x86|x86_64|riscv64|s390x|powerpc|powerpc64|wasm32|other)$") == false'
      - 'IsMatch(resource.attributes["uc.app.channel"], "^(development|test|alpha|beta|stable|production)$") == false'
      - 'IsInt(resource.attributes["uc.telemetry.schema.version"]) == false'
      - 'resource.attributes["uc.telemetry.schema.version"] != 1'
      - 'scope.name != "uc-observability-runtime"'
      - 'scope.schema_url != ""'
      - 'scope.version != ""'
      - 'Len(scope.attributes) != 0'
      - 'scope.dropped_attributes_count != 0'
      - 'span.attributes["target"] != "uc.telemetry"'
      - 'span.attributes["uc.flow.id"] == nil and span.attributes["uc.outcome"] == nil and span.attributes["error.type"] == nil and ((span.attributes["uc.record.kind"] == nil and Len(span.attributes) != 4) or (span.attributes["uc.record.kind"] != nil and Len(span.attributes) != 5))'
      - 'span.attributes["uc.flow.id"] == nil and span.attributes["uc.outcome"] != nil and span.attributes["error.type"] == nil and ((span.attributes["uc.record.kind"] == nil and Len(span.attributes) != 5) or (span.attributes["uc.record.kind"] != nil and Len(span.attributes) != 6))'
      - 'span.attributes["uc.flow.id"] == nil and span.attributes["uc.outcome"] != nil and span.attributes["error.type"] != nil and ((span.attributes["uc.record.kind"] == nil and Len(span.attributes) != 6) or (span.attributes["uc.record.kind"] != nil and Len(span.attributes) != 7))'
      - 'span.attributes["uc.flow.id"] != nil and span.attributes["uc.outcome"] == nil and span.attributes["error.type"] == nil and ((span.attributes["uc.record.kind"] == nil and Len(span.attributes) != 5) or (span.attributes["uc.record.kind"] != nil and Len(span.attributes) != 6))'
      - 'span.attributes["uc.flow.id"] != nil and span.attributes["uc.outcome"] != nil and span.attributes["error.type"] == nil and ((span.attributes["uc.record.kind"] == nil and Len(span.attributes) != 6) or (span.attributes["uc.record.kind"] != nil and Len(span.attributes) != 7))'
      - 'span.attributes["uc.flow.id"] != nil and span.attributes["uc.outcome"] != nil and span.attributes["error.type"] != nil and ((span.attributes["uc.record.kind"] == nil and Len(span.attributes) != 7) or (span.attributes["uc.record.kind"] != nil and Len(span.attributes) != 8))'
      - 'span.attributes["uc.outcome"] != nil and (IsString(span.attributes["uc.outcome"]) == false or IsMatch(span.attributes["uc.outcome"], "^(ok|error|conflict|partial|skipped|deferred|rejected|cancelled)$") == false)'
      - '(span.attributes["error.type"] != nil and span.attributes["uc.outcome"] != "error") or (span.attributes["uc.outcome"] == "error" and span.attributes["error.type"] == nil)'
      - 'span.attributes["error.type"] != nil and IsMatch(span.attributes["error.type"], "^(membership_recovery_failed|delivery_failed|network_paused|authentication_failed|address_unavailable|connect_failed|stream_failed|decode_failed|timeout|channel_closed|storage|security|corrupt|source_changed|manifest|join_failed|shutdown_timeout|unavailable|peer_rejected|peer_incompatible|local_policy_exceeded|internal)$") == false'
      - 'span.attributes["error.type"] != nil and IsString(span.attributes["error.type"]) == false'
      - '(span.attributes["uc.outcome"] == "ok" and span.status.code != STATUS_CODE_OK) or (span.attributes["uc.outcome"] == "error" and span.status.code != STATUS_CODE_ERROR)'
      - 'IsMatch(span.attributes["uc.outcome"], "^(conflict|partial|skipped|deferred|rejected|cancelled)$") and span.status.code != STATUS_CODE_UNSET'
      - 'span.attributes["uc.operation"] == "membership_history_sync" and span.name != "membership_history_sync" and ((span.kind == SPAN_KIND_CLIENT and IsMatch(span.name, "^membership[.](compare_summary|request_history|send_history|request_conflict_evidence|send_conflict_evidence|acknowledge|deliver_restricted_event|deliver_restricted_decision)[.]exchange$") == false) or (span.kind == SPAN_KIND_SERVER and IsMatch(span.name, "^membership[.](compare_summary|request_history|send_history|request_conflict_evidence|send_conflict_evidence|acknowledge|deliver_restricted_event|deliver_restricted_decision)[.]handle_and_reply$") == false))'
      - 'IsString(span.attributes["uc.domain"]) == false'
      - 'IsMatch(span.attributes["uc.domain"], "^(clipboard|space_admission|space_membership|storage|runtime)$") == false'
      - 'IsString(span.attributes["uc.operation"]) == false'
      - 'IsMatch(span.attributes["uc.operation"], "^(clipboard[.](copy_and_sync|send|resend|persist|write_system)|clipboard_dispatch|clipboard_receive|clipboard_address_resolve|clipboard_connect|space_admission|membership_recovery|membership_history_sync|membership_group_update|network_transport|profile_storage_upgrade|session_lifecycle|runtime[.](shutdown_tasks|recover_session))$") == false'
      - 'IsString(span.attributes["uc.role"]) == false'
      - 'IsMatch(span.attributes["uc.role"], "^(local|client|server|joiner|sponsor|member)$") == false'
      - 'span.attributes["uc.domain"] != "space_admission" and span.attributes["uc.operation"] != "membership_history_sync" and span.attributes["uc.operation"] != "membership_recovery" and span.attributes["uc.operation"] != "profile_storage_upgrade" and span.attributes["uc.operation"] != "session_lifecycle" and span.name != span.attributes["uc.operation"]'
      - 'span.attributes["uc.operation"] == "profile_storage_upgrade" and IsMatch(span.name, "^(profile_storage_upgrade|storage[.]initialize_profile)$") == false'
      - 'span.attributes["uc.operation"] == "session_lifecycle" and span.name != "runtime.transition_session"'
      - 'span.attributes["uc.operation"] == "runtime.shutdown_tasks" and span.attributes["uc.outcome"] == "ok"'
      - 'span.attributes["uc.record.kind"] != nil and (IsString(span.attributes["uc.record.kind"]) == false or IsMatch(span.attributes["uc.record.kind"], "^(business|diagnostic)$") == false)'
      - 'span.attributes["uc.record.kind"] == "business" and (span.attributes["uc.role"] != "local" or IsMatch(span.attributes["uc.operation"], "^(space_admission|clipboard[.](copy_and_sync|send|resend)|membership_recovery|profile_storage_upgrade|runtime[.]recover_session)$") == false)'
      - 'span.attributes["uc.record.kind"] == "diagnostic" and (IsEmpty(span.parent_span_id) == false or (span.attributes["uc.role"] == "local" and IsMatch(span.attributes["uc.operation"], "^(space_admission|clipboard[.](copy_and_sync|send|resend)|membership_recovery|profile_storage_upgrade|runtime[.]recover_session)$")))'
      - 'span.attributes["uc.operation"] == "membership_recovery" and (span.attributes["uc.role"] != "local" or span.kind != SPAN_KIND_INTERNAL or IsMatch(span.name, "^membership[.]recover[.](startup|resume|peer_online|retry|state_changed|requested)$") == false)'
      - 'span.attributes["uc.operation"] == "membership_recovery" and span.attributes["uc.outcome"] == "skipped"'
      - 'span.attributes["uc.operation"] == "profile_storage_upgrade" and span.attributes["uc.outcome"] == "skipped"'
      - 'span.attributes["uc.domain"] == "space_admission" and ((span.attributes["uc.role"] == "local" and span.name != "pairing.lifecycle") or (span.attributes["uc.role"] == "joiner" and span.attributes["uc.operation"] == "space_admission" and IsMatch(span.name, "^pairing[.](authenticate|reconnect)$") == false) or (span.attributes["uc.role"] == "joiner" and span.attributes["uc.operation"] == "network_transport" and IsMatch(span.name, "^pairing[.](send_request|(request_join|confirm_prepared|confirm_applied|settle|cancel)[.]send)$") == false) or (span.attributes["uc.role"] == "sponsor" and span.attributes["uc.operation"] == "network_transport" and span.name != "pairing.receive_request") or (span.attributes["uc.role"] == "sponsor" and span.attributes["uc.operation"] == "space_admission" and IsMatch(span.name, "^pairing[.](process_request|(request_join|confirm_prepared|confirm_applied|settle|cancel)[.]process)$") == false))'
      - 'span.attributes["uc.flow.id"] != nil and IsString(span.attributes["uc.flow.id"]) == false'
      - 'span.attributes["uc.flow.id"] != nil and IsMatch(span.attributes["uc.flow.id"], "^[0-9a-f]{32}$") == false'
      - 'span.attributes["uc.flow.id"] != nil and (span.attributes["uc.domain"] != "space_admission" or ((span.attributes["uc.role"] != "joiner" or IsMatch(span.attributes["uc.operation"], "^(space_admission|network_transport)$") == false or span.kind != SPAN_KIND_CLIENT) and (span.attributes["uc.role"] != "local" or span.attributes["uc.operation"] != "space_admission" or span.kind != SPAN_KIND_INTERNAL or IsEmpty(span.parent_span_id) == false)))'
      - 'span.trace_state != ""'
      - 'Len(span.events) != 0'
      - 'Len(span.links) != 0'
      - 'span.status.message != ""'
      - 'span.status.code != STATUS_CODE_UNSET and span.status.code != STATUS_CODE_OK and span.status.code != STATUS_CODE_ERROR'
      - 'span.dropped_attributes_count != 0'
      - 'span.dropped_events_count != 0'
      - 'span.dropped_links_count != 0'
      - 'IsEmpty(span.trace_id)'
      - 'IsEmpty(span.span_id)'
      - '(span.attributes["uc.domain"] == "clipboard" and IsMatch(span.attributes["uc.operation"], "^clipboard[_.]") == false)'
      - '(span.attributes["uc.domain"] == "space_admission" and IsMatch(span.attributes["uc.operation"], "^(space_admission|network_transport)$") == false)'
      - '(span.attributes["uc.domain"] == "space_membership" and IsMatch(span.attributes["uc.operation"], "^membership_") == false)'
      - '(span.attributes["uc.domain"] == "storage" and span.attributes["uc.operation"] != "profile_storage_upgrade")'
      - '(span.attributes["uc.domain"] == "runtime" and IsMatch(span.attributes["uc.operation"], "^(session_lifecycle|runtime[.](shutdown_tasks|recover_session))$") == false)'
      - '(span.attributes["uc.operation"] == "clipboard_dispatch" and (span.attributes["uc.role"] != "client" or span.kind != SPAN_KIND_CLIENT))'
      - '(span.attributes["uc.operation"] == "clipboard_receive" and (span.attributes["uc.role"] != "server" or span.kind != SPAN_KIND_SERVER))'
      - '(IsMatch(span.attributes["uc.operation"], "^clipboard[.](copy_and_sync|send|resend|persist|write_system)$") and (span.attributes["uc.role"] != "local" or span.kind != SPAN_KIND_INTERNAL))'
      - '(IsMatch(span.attributes["uc.operation"], "^clipboard_(address_resolve|connect)$") and (span.attributes["uc.role"] != "client" or span.kind != SPAN_KIND_INTERNAL))'
      - '(span.attributes["uc.operation"] == "space_admission" and ((span.attributes["uc.role"] == "joiner" and span.kind != SPAN_KIND_CLIENT) or (span.attributes["uc.role"] == "local" and span.kind != SPAN_KIND_INTERNAL) or (span.attributes["uc.role"] == "local" and span.kind == SPAN_KIND_INTERNAL and (span.attributes["uc.flow.id"] == nil or IsEmpty(span.parent_span_id) == false)) or (span.attributes["uc.role"] == "sponsor" and span.kind != SPAN_KIND_INTERNAL) or IsMatch(span.attributes["uc.role"], "^(local|joiner|sponsor)$") == false))'
      - '(span.attributes["uc.operation"] == "network_transport" and ((span.attributes["uc.role"] == "joiner" and span.kind != SPAN_KIND_CLIENT) or (span.attributes["uc.role"] == "sponsor" and span.kind != SPAN_KIND_SERVER) or IsMatch(span.attributes["uc.role"], "^(joiner|sponsor)$") == false))'
      - '(IsMatch(span.attributes["uc.operation"], "^membership_") and span.attributes["uc.operation"] != "membership_recovery" and (span.attributes["uc.role"] != "member" or IsMatch(span.kind.string, "^(Client|Server)$") == false))'
      - '(span.attributes["uc.operation"] == "profile_storage_upgrade" and (span.attributes["uc.role"] != "local" or span.kind != SPAN_KIND_INTERNAL))'
      - '(IsMatch(span.attributes["uc.operation"], "^(session_lifecycle|runtime[.](shutdown_tasks|recover_session))$") and (span.attributes["uc.role"] != "local" or span.kind != SPAN_KIND_INTERNAL))'
    log_conditions:
      - 'resource.schema_url != ""'
      - 'Len(resource.attributes) != 9'
      - 'resource.dropped_attributes_count != 0'
      - 'resource.attributes["service.namespace"] != "uniclipboard"'
      - 'resource.attributes["service.name"] != "uc-engine"'
      - 'IsString(resource.attributes["service.version"]) == false'
      - 'Len(resource.attributes["service.version"]) > 32'
      - 'IsMatch(resource.attributes["service.version"], "^[0-9]{1,5}[.][0-9]{1,5}[.][0-9]{1,5}(-(alpha|beta|rc)([.][0-9]{1,5})?)?$") == false'
      - 'IsString(resource.attributes["service.instance.id"]) == false'
      - 'IsMatch(resource.attributes["service.instance.id"], "^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$") == false'
      - 'IsMatch(resource.attributes["deployment.environment.name"], "^(development|test|staging|production)$") == false'
      - 'IsMatch(resource.attributes["os.type"], "^(ios|android|macos|windows|linux|ohos|other)$") == false'
      - 'IsMatch(resource.attributes["host.arch"], "^(arm64|arm|x86|x86_64|riscv64|s390x|powerpc|powerpc64|wasm32|other)$") == false'
      - 'IsMatch(resource.attributes["uc.app.channel"], "^(development|test|alpha|beta|stable|production)$") == false'
      - 'IsInt(resource.attributes["uc.telemetry.schema.version"]) == false'
      - 'resource.attributes["uc.telemetry.schema.version"] != 1'
      - 'scope.name != "uc.telemetry"'
      - 'scope.schema_url != ""'
      - 'scope.version != ""'
      - 'Len(scope.attributes) != 0'
      - 'scope.dropped_attributes_count != 0'
      - 'log.body != nil'
      - 'log.event_name != "uc.diagnostic"'
      - 'log.attributes["event.name"] != "uc.operation.completed"'
      - 'log.attributes["uc.flow.id"] != nil'
      - 'log.attributes["error.type"] == nil and Len(log.attributes) != 6'
      - 'log.attributes["error.type"] != nil and Len(log.attributes) != 7'
      - 'IsString(log.attributes["uc.domain"]) == false'
      - 'IsMatch(log.attributes["uc.domain"], "^(clipboard|space_admission|space_membership|storage|runtime)$") == false'
      - 'IsString(log.attributes["uc.operation"]) == false'
      - 'IsMatch(log.attributes["uc.operation"], "^(clipboard[.](copy_and_sync|send|resend|persist|write_system)|clipboard_dispatch|clipboard_receive|clipboard_address_resolve|clipboard_connect|space_admission|membership_recovery|membership_history_sync|membership_group_update|network_transport|profile_storage_upgrade|session_lifecycle|runtime[.](shutdown_tasks|recover_session))$") == false'
      - 'IsString(log.attributes["uc.role"]) == false'
      - 'IsMatch(log.attributes["uc.role"], "^(local|client|server|joiner|sponsor|member)$") == false'
      - 'IsString(log.attributes["uc.outcome"]) == false'
      - 'IsMatch(log.attributes["uc.outcome"], "^(ok|error|conflict|partial|skipped|deferred|rejected|cancelled)$") == false'
      - 'IsInt(log.attributes["duration_ms"]) == false'
      - 'log.attributes["duration_ms"] < 0'
      - 'log.attributes["uc.outcome"] == "error" and log.attributes["error.type"] == nil'
      - 'log.attributes["uc.outcome"] != "error" and log.attributes["error.type"] != nil'
      - 'log.attributes["error.type"] != nil and IsString(log.attributes["error.type"]) == false'
      - 'log.attributes["error.type"] != nil and IsMatch(log.attributes["error.type"], "^(membership_recovery_failed|delivery_failed|network_paused|authentication_failed|address_unavailable|connect_failed|stream_failed|decode_failed|timeout|channel_closed|storage|security|corrupt|source_changed|manifest|join_failed|shutdown_timeout|unavailable|peer_rejected|peer_incompatible|local_policy_exceeded|internal)$") == false'
      - 'IsEmpty(log.trace_id) != IsEmpty(log.span_id)'
      - 'IsMatch(log.severity_text, "^(INFO|ERROR)$") == false'
      - '(log.attributes["uc.outcome"] == "error" and (log.severity_number != SEVERITY_NUMBER_ERROR or log.severity_text != "ERROR"))'
      - '(log.attributes["uc.outcome"] != "error" and (log.severity_number != SEVERITY_NUMBER_INFO or log.severity_text != "INFO"))'
      - 'log.dropped_attributes_count != 0'
      - '(log.attributes["uc.domain"] == "clipboard" and IsMatch(log.attributes["uc.operation"], "^clipboard[_.]") == false)'
      - '(log.attributes["uc.domain"] == "space_admission" and IsMatch(log.attributes["uc.operation"], "^(space_admission|network_transport)$") == false)'
      - '(log.attributes["uc.domain"] == "space_membership" and IsMatch(log.attributes["uc.operation"], "^membership_") == false)'
      - '(log.attributes["uc.domain"] == "storage" and log.attributes["uc.operation"] != "profile_storage_upgrade")'
      - '(log.attributes["uc.domain"] == "runtime" and IsMatch(log.attributes["uc.operation"], "^(session_lifecycle|runtime[.](shutdown_tasks|recover_session))$") == false)'
      - '(log.attributes["uc.operation"] == "clipboard_dispatch" and log.attributes["uc.role"] != "client")'
      - '(log.attributes["uc.operation"] == "clipboard_receive" and log.attributes["uc.role"] != "server")'
      - '(IsMatch(log.attributes["uc.operation"], "^clipboard[.](copy_and_sync|send|resend|persist|write_system)$") and log.attributes["uc.role"] != "local")'
      - '(IsMatch(log.attributes["uc.operation"], "^clipboard_(address_resolve|connect)$") and log.attributes["uc.role"] != "client")'
      - '(log.attributes["uc.operation"] == "space_admission" and IsMatch(log.attributes["uc.role"], "^(local|joiner|sponsor)$") == false)'
      - '(log.attributes["uc.operation"] == "network_transport" and IsMatch(log.attributes["uc.role"], "^(joiner|sponsor)$") == false)'
      - '(IsMatch(log.attributes["uc.operation"], "^membership_") and IsMatch(log.attributes["uc.role"], "^(local|member)$") == false)'
      - '(IsMatch(log.attributes["uc.operation"], "^(profile_storage_upgrade|session_lifecycle|runtime[.](shutdown_tasks|recover_session))$") and log.attributes["uc.role"] != "local")'
  transform/allowlist:
    error_mode: propagate
    trace_statements:
      - context: resource
        statements:
          - 'keep_keys(resource.attributes, ["service.namespace", "service.name", "service.version", "service.instance.id", "deployment.environment.name", "os.type", "host.arch", "uc.app.channel", "uc.telemetry.schema.version"])'
      - context: span
        statements:
          - 'keep_keys(span.attributes, ["target", "uc.flow.id", "uc.domain", "uc.operation", "uc.role", "uc.outcome", "error.type", "uc.record.kind"])'
    log_statements:
      - context: resource
        statements:
          - 'keep_keys(resource.attributes, ["service.namespace", "service.name", "service.version", "service.instance.id", "deployment.environment.name", "os.type", "host.arch", "uc.app.channel", "uc.telemetry.schema.version"])'
      - context: log
        statements:
          - 'set(log.event_name, log.attributes["event.name"]) where log.attributes["event.name"] != nil'
          - 'keep_keys(log.attributes, ["event.name", "uc.domain", "uc.operation", "uc.role", "uc.outcome", "error.type", "duration_ms"])'

exporters:
  debug:
    verbosity: detailed
  otlp_grpc/jaeger:
    endpoint: jaeger:4317
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [filter/diagnostics, transform/allowlist]
      exporters: [debug, otlp_grpc/jaeger]
    logs:
      receivers: [otlp]
      processors: [filter/diagnostics, transform/allowlist]
      exporters: [debug]
