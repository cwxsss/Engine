# UniClipboardEngine 领域词表

本文件记录跨 crate 使用的业务术语。新增或调整术语时，优先更新本文件；具体协议、存储格式和第三方库名称不属于领域词表。

## 稳定入口

- **Engine**：宿主使用 UniClipboard 核心能力的唯一稳定入口。宿主通过启动配置、宿主能力、操作、结果、事件和稳定错误与 Engine 交互。
- **宿主**：使用 Engine 的平台运行环境，例如 iOS、Android、HarmonyOS 或桌面进程。宿主不依赖内部 crate。
- **操作**：宿主向 Engine 发起的一项明确业务动作或查询。操作结果表达该动作的业务结果，不暴露内部实现细节。

## 空间与成员

- **空间（Space）**：一组获准共享剪贴板内容和相关资源的设备集合。
- **空间切换（Space Switch）**：已完成设置的设备通过同一次可恢复空间加入进入另一个空间的过程。新空间完成前，当前空间继续生效；成功后才切换到新空间，并清除旧空间留下的成员关系和安全恢复状态。
- **Profile 数据世代（Profile Data Generation）**：保存本机历史、搜索资料、受管文件和历史内容密钥目录的一代 profile 数据。完成 V3 存储升级后，它跨空间切换保持不变，不属于当前空间。
- **空间控制世代（Space Control Generation）**：只属于一个空间的一代成员、凭据、MLS、安全与恢复状态。空间切换会整体替换它，调用方不能逐表拼装或部分激活。
- **活动运行布局（Active Runtime Layout）**：当前空间、Profile 数据世代和空间控制世代的合法组合。Profile 数据世代可跨空间复用；空间控制世代必须随目标空间替换。
- **不可读历史保留（Unreadable History Preservation）**：空间切换前发现旧历史无法读取时，必须先由用户明确决定是否保留。确认后保留原记录，不自动删除。
- **设备（Device）**：空间中的一个可识别终端，以 `DeviceId` 标识。
- **成员（Space Member）**：属于某个空间的设备及其同步偏好。
- **空间加入（Space Join）**：设备进入新空间或重新进入已有空间的可恢复过程。每次用户明确提交都创建新的加入；同一次加入使用同一公开标识并跨重启保留，只公开进行中、成功或拒绝三种结果。发起方在正式加入前逐台核对已有成员资料，但全部核对共用最多 10 秒；时间耗尽后继续加入，不因更多离线设备延长本次等待。临时离线、消息重放和后台恢复继续原加入，不通过再次调用加入创建第二次尝试。
- **加入取代（Join Supersession）**：用户再次明确提交时，用全新加入取代尚未持久保存准备证明的旧本机加入。旧加入保存为本机被取代终态并继续阻止迟到消息生效；准备证明一旦保存，旧邀请方可能已经正式提交，只能继续原加入并明确拒绝本次取代。
- **加入取消（Join Cancellation）**：本机请求停止一次尚未正式提交的空间加入。取消先保存时加入被拒绝；正式提交先保存时取消已经太晚，同一次加入继续完成，也不会自动变成成员移除。
- **成员候选（Space Membership Candidate）**：已发现或正在准入、但尚未完成成员激活的设备。只有身份、完整成员历史和目标安全状态都通过核对，并由双方确认同一次正式加入后，候选才能取得普通成员权限。
- **历史验证凭据（Historical Verification Credential）**：成员首次进入签名成员历史时永久保存的验证材料。它只证明该成员过去是否有权签署某条记录；成员被移除后继续保留，但不能恢复当前权限。
- **成员准入激活（Membership Admission Activation）**：新设备完整核对历史并暂存目标安全状态后，双方正式保存同一成员记录和安全状态，再解除普通权限门禁的过程。它不同于剪贴板内容的“激活”。
- **普通空间重置（Space Reset）**：用户明确触发的设备关系重置。它保留本机历史、文件、一般设置、设备身份和解锁能力，废弃全部旧设备关系并建立只含本机的新空间；中断、重启或重复调用继续同一次重置。其他设备不会被远程清空，也不能重新取得新空间资格。
- **彻底空间重置（Factory Space Reset）**：停止并永久失效旧运行实例，删除本机密钥、空间资料和受管文件后建立全新本机状态的可恢复操作。它只证明本机资料已销毁，不会伪造远端拒绝、取消或成员移除。
- **当前成员范围（Current Member Scope）**：当前仍获准参与设备列表、连接、剪贴板发送、恢复和重发等普通功能的其他设备集合。它由已应用的成员历史决定；历史成员记录、可信关系、地址和旧发送记录都不能单独恢复当前资格。无法确认该范围时，普通功能必须失败，不得退回历史关系推断。
- **可信对等端（Trusted Peer）**：已完成身份确认的设备关系；它不同于尚未完成验证的成员候选。设备被移除后，该关系可为历史验证和受限决定继续保留，但只有同时属于当前成员范围时才能参与普通功能。
- **成员移除（Member Removal）**：发起设备将移除写入签名成员历史并立即应用；其他设备收到后必须由本机用户接受或拒绝，不能自动移除成员。
- **成员历史核对（Membership Reconciliation）**：可信设备建立连接后自动比较并补齐签名成员历史。普通新增可以自动应用，未确认的移除必须等待本机用户决定。
- **待移除决定（Pending Removal Decision）**：本机收到有效但尚未接受的移除后，继续保留当前成员和内容权限，等待用户接受或拒绝；同一决定会跨重启保留。
- **成员历史分叉（Membership Divergence）**：双方历史不可比较，或一方拒绝另一方发起的移除时形成的设备关系。分叉只隔离相关设备之间的内容和成员历史传播，不影响各自分支中的其他设备继续工作。
- **需要升级（Upgrade Required）**：仅在已确认对端低于 `1.1` 时成立。双方仍可发现彼此并保留空间和成员资料，但暂停内容同步；对端升级到 `1.1` 并重新确认后恢复。网络失败、离线或身份问题不属于需要升级。
- **旧资料独立化（Legacy Profile Isolation）**：`0.20.0` 之前已经完成设置的资料首次升级时，保留本机内容、已完成文件、设置、设备身份和解锁材料，清除全部旧设备关系与恢复状态，并建立只包含本机的新空间。它不探测、等待或自动恢复旧设备。
- **需要重新配对（Re-pairing Required）**：旧资料独立化后跨重启保存的产品状态，表示全部旧设备关系都必须重新建立。产品收到通知或启动时查询到该状态后，应提示用户在一台设备上创建邀请，并在另一台设备上切换或加入；成功创建新空间或加入新空间后清除该状态。
- **在线状态（Reachability）**：本设备对另一成员当前可达性的观察结果，包括 Online、Offline 和 Unknown。它不是成员资格本身。

## 剪贴板与同步

- **剪贴板条目（Clipboard Entry）**：一次捕获或接收的剪贴板内容记录，带有来源、时间、内容类别与表示形式。
- **活动剪贴板状态（Active Clipboard State）**：空间内当前活动剪贴板内容的收敛记录。它引用本地条目和激活信息，用于设备间最终收敛，不等同于完整内容本体。
- **激活（Activation）**：将某条本地持有的剪贴板内容推进为活动剪贴板状态的动作。
- **收敛（Convergence）**：成员交换活动剪贴板状态后，依据既定顺序规则得到一致状态的过程。
- **重同步（Resync）**：成员重新在线后，将当前活动剪贴板状态重新发送给该成员以促进收敛的动作。它只处理当前活动状态，不补送历史条目。
- **同步总开关（Global Sync）**：用户对同步的总许可。关闭后，不接收内容、不发送内容或文件，也不执行自动补送、手动重发和恢复广播。
- **自动同步（Automatic Sync）**：同步总开关开启时，对本机新内容自动发送和离线自动补送的选择。关闭后，用户仍可手动重发允许发送的内容或文件。
- **离线投递恢复（Offline Delivery Recovery）**：自动同步开启时，本机内容先立即投递；既有设备暂时不可达时，每台设备只保留最新一条本机内容作为自动补送目标。新的本机复制会替代该设备更早的待送内容；待其恢复在线或应用重启后重新发现在线时，只自动补送这一条。它不向新设备补送旧内容，不处理远端来源，不重复发送已送达或已被替代的内容；自动同步关闭、同步总开关关闭或内容无法恢复时停止自动补送。
- **已被替代（Superseded）**：同一设备有更新的本机内容后，更早的暂时未送达内容进入的最终状态。它不会再自动补送，也不表示发送失败。
- **历史（History）**：已持久化的剪贴板条目及其检索、资源和投递状态集合。单条记录的发送情况只展示当前仍有效且可信的设备；已移除设备即使保留旧发送记录也不再显示，当前成员范围无法确认时查询失败。
- **普通远端接收（Interactive Receive）**：收到远端内容后保存到历史，并写入本机剪贴板的完整接收方式。
- **只保存拉取（Store-only Pull）**：收到远端内容后只保存到历史，不写入本机剪贴板的完整接收方式。

## 设置与连接

- **设置（Settings）**：用户可配置的产品行为集合，包括同步、网络、保留、安全与界面偏好。
- **中继配置（Relay Configuration）**：设置中关于中继连接的业务配置，包括是否允许中继回退、用户指定的中继地址和拥塞控制偏好。
- **中继配置事务（Relay Configuration Transaction）**：同时变更中继配置与中继访问令牌的动作。设置与安全存储必须共同提交；若进程在中途停止，下一次恢复会回到变更前的完整状态。
- **中继地址（Relay URL）**：用户指定的中继服务器地址。地址必须是有效的 HTTP 或 HTTPS 地址，且不能携带用户信息。
- **中继访问令牌（Relay Access Token）**：用于访问受保护中继的敏感凭据。令牌不属于可显示的设置，不得写入日志、普通持久化记录或公开结果。
- **中继探测（Relay Probe）**：对指定中继地址执行的显式诊断动作，可使用已存储、临时提供或不使用访问令牌。
- **P2P**：设备间同步的默认连接能力。
- **LAN 兼容线**：由用户明确选择的局域网兼容能力；它不能作为 P2P 失败后的自动回退。
- **网络会话恢复（Network Session Recovery）**：网络中断后恢复当前连接会话的过程。用户主动恢复与满足严格条件的自动恢复共用同一轮，每次只运行一次；宿主只看到空闲、恢复中、等待重试或失败。失败会说明是否可再试及下次时间。普通离线、首次连接、过期观察和 LAN 兼容线都不会自动触发；新的成功连接会结束旧的自动恢复周期。

## 观测

- **移动端产品分析（Mobile Product Analytics）**：仅在用户同意且移动宿主明确开启时发送的脱敏使用信息。发送失败不影响正常功能，也不得包含用户内容或敏感信息。

## 文件与安全

- **资源（Resource）**：剪贴板条目关联的可读取内容，例如缩略图、文件或二进制数据。
- **文件集（Entry File Set）**：一个剪贴板条目关联的逐文件 manifest。复制多个文件、一个目录或混合选择都使用同一模型；目录展开为相对路径成员，不打包成单一归档 blob。完整取舍见 ADR-010。
- **文件传输（File Transfer）**：在成员之间传递文件资源的过程，具有方向、进度、终态和取消原因。网络会话重建会结束尚未完成的传输，并以“连接恢复中断”作为取消原因；已经结束的传输不会再次改变结果。
- **安全存储（Secure Storage）**：保存敏感凭据的受保护存储能力。它是实现能力，不定义业务规则。
- **明文业务负载**：可恢复用户内容或元数据的业务数据，包括剪贴板正文、标题、预览、标签、文件名和路径。默认不得以明文持久化。
