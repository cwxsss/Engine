# UniClipboard 架构圣经

本文是 `UniClipboardEngine` 当前架构的总入口，回答系统为什么存在、如何分层、数据如何保存、设备如何通信，以及各平台应如何接入。

本文描述的是当前仓库事实，不是未来设计。具体接口以 `crates/uc-engine/` 的公共契约为准，安全规则以 `AGENTS.md` 和 `docs/security/` 为准，已批准但尚未实现的方向以 ADR 或规划文件为准。发生冲突时，先修正文档与实现的不一致，不能默认任意一方正确。

## 1. 项目目标

UniClipboardEngine 是 macOS、Windows、Linux、iOS、Android 和 HarmonyOS 共用的端到端加密剪贴板引擎。它本身不是可直接运行的应用，而是由桌面或移动宿主接入的共享核心。

项目要实现五个结果：

1. 所有平台使用同一套设备身份、空间、配对、同步和内容规则。
2. 默认通过经过身份认证的加密 P2P 通道同步文字、图片、文件和活动剪贴板状态。
3. 历史、搜索、文件传输和成员关系可以跨重启恢复，同时业务负载默认只以密文持久化。
4. 平台只提供系统能力，不复制加密、持久化、配对、传输、恢复和迁移流程。
5. 每次发布都能追溯到唯一版本、源码提交、产物校验值和设备验收记录。

### 不做什么

- 不提供产品界面或平台业务页面。
- 不允许外部直接依赖内部 crate。
- 不在 P2P 失败时自动切换到 LAN HTTP。
- 不让宿主自行拼装数据库、加密、发送、重试或恢复步骤。
- 不把移动平台验收宿主发展成另一套产品实现。

## 2. 整体架构

系统采用端口与适配器分层。依赖方向从外向内组装，但业务知识从领域层向外逐级收敛。

```text
桌面宿主       iOS / Android       HarmonyOS
   |               |                  |
Rust API       UniFFI 绑定          N-API 绑定
   \_______________|__________________/
                   |
               uc-engine
          稳定操作、结果、事件、生命周期
                   |
             uc-application
       完整业务流程、顺序、恢复和后台任务
                   |
                uc-core
        领域模型、规则、状态和能力约定
                   ^
                   |
               uc-infra
   SQLite、MasterKey AEAD、搜索、文件、Iroh P2P
```

### 分层责任

| 层 | 负责 | 不负责 |
| --- | --- | --- |
| `uc-core` | 领域模型、业务规则、状态和能力接口 | 数据库、网络框架、操作系统调用 |
| `uc-application` | 一项业务从开始到结束的完整流程 | 对外稳定协议、具体基础设施 |
| `uc-infra` | SQLite、加密、搜索、文件缓存、Iroh 网络等具体能力 | 决定业务顺序和产品规则 |
| `uc-engine` | 组装内部能力，暴露唯一稳定入口 | 复制领域规则或让宿主逐步编排内部流程 |
| `bindings/` | 语言类型、异步运行时和平台调用转换 | 平台专属业务规则 |
| 宿主应用 | 目录、安全存储、系统剪贴板、文件句柄、生命周期通知，以及经用户许可的观测发送和身份保存 | 核心业务状态、事件定义和身份切换顺序 |

### 稳定边界

外部只依赖 `uc-engine`，并只理解以下概念：

- `EngineConfig`：宿主版本和资料空间。
- `HostCapabilities`：目录、安全存储、剪贴板、文件访问，以及可选的脱敏观测能力。
- `Engine::start`：启动引擎并取得事件流。
- `Operation` / `OperationResult`：执行命令或查询并取得稳定结果。
- `EngineEvent`：状态变化、操作终态、传输和刷新通知。
- `EngineError`：稳定编号、错误类别和是否可重试。
- `suspend` / `resume` / `shutdown`：平台生命周期。

内部 crate、数据库表、网络库类型、加密实现和后台任务均不属于稳定边界。

### 运行时所有权

一个跨层功能必须由一个模块负责完整结果。调用方只发起一次操作，不得知道内部的持久化顺序、网络握手、补偿、重试和关闭细节。

删除负责模块时，如果这些知识会重新散落到多个调用方，说明模块有效隐藏了复杂度；如果删除后没有变化，说明它只是转发层，应合并或重新划分。

## 3. 核心模块

### 工作区模块

| 模块 | 主要职责 |
| --- | --- |
| `crates/uc-engine/` | 唯一稳定 Rust 入口；操作路由、结果转换、事件流、生命周期和内部组装 |
| `crates/uc-core/` | 空间、成员、配对、剪贴板、文件传输、搜索、设置、安全和端口定义 |
| `crates/uc-application/` | 空间会话、配对准入、剪贴板出入站、历史、搜索、文件传输、成员收敛和恢复流程 |
| `crates/uc-infra/` | SQLite 仓储、MasterKey AEAD、安全状态、blob、搜索索引、文件缓存和 Iroh P2P |
| `crates/uc-content-hash/` | 跨平台一致的内容身份摘要算法，不依赖其他业务模块 |
| `crates/uc-observability-contract/` | 宿主与核心共享的脱敏观测约定 |
| `bindings/uc-engine-uniffi/` | iOS 和 Android 绑定及 XCFramework、AAR 打包 |
| `bindings/uc-ohos-napi/` | HarmonyOS N-API、ArkTS 声明和 HAR 打包 |
| `compatibility/` | 用户显式选择的 LAN HTTP 兼容线，独立版本和发布 |
| `tests/hosts/` | 三个移动平台的接入验收宿主，不承载产品功能 |

### 应用层目录归属（ADR-018）

`uc-application` 先按业务领域组织：剪贴板、空间、传输、搜索和设置各自保有短动作、持续流程和
测试；跨领域且无业务所有权的小型能力才进入 `support/`，`deps.rs` 只保存组装数据。`use case`
只是短动作的职责，不是目录归属，因此不保留集中 `usecases/`，领域内部也不嵌套同名目录。
短动作直接位于所属领域或有明确业务含义的子域，持续工作也留在所属领域的 `runtime/` 或
`coordinator/` 中。`facade/` 仍是唯一对外业务入口，按领域提供入口而不保存流程实现。运行期、
协调器、会话、内部适配、事件总线、缓存和投影构建不得留在 `facade/`；它们分别归入所属领域或
无业务所有权的 `support/`。稳定事件类型可以由门面公开，事件投递实现不在门面目录。

完整的路径迁移表、Engine 收口规则、删除清单和验收矩阵见[规格 018](../specs/018-domain-oriented-application-layout.md)。

### 领域模块

| 领域 | 核心事实 | 完整流程归属 |
| --- | --- | --- |
| 空间与加密会话 | 空间身份、设置状态、解锁状态、密钥世代 | `uc-application` 的空间会话与空间切换流程 |
| 配对与准入 | 一次性邀请、加入方、发起方、身份指纹、信任关系 | 配对入站、出站和空间准入流程 |
| 成员关系 | 当前分支成员、可信对等端、同步偏好、在线状态和历史关系 | 成员历史核对与用户决定流程 |
| 剪贴板历史 | 事件、条目、表示、选择、资源、收藏和标签 | 捕获、物化、历史维护、恢复和删除流程 |
| 活动剪贴板 | 当前内容的跨设备收敛值 | 活动状态登记、广播、拉取和应用流程 |
| 文件传输 | 传输身份、时间线、进度、结果和接收尝试 | 文件导入、发布、接收、提交、取消和清理流程 |
| 搜索 | 搜索文档、不可逆词项标签、标签和索引状态 | 实时索引、查询、重建和隐私维护流程 |
| 设置 | 同步、网络、保留、安全和文件偏好 | 设置读取、事务更新和中继诊断流程 |

### 组装原则

- `uc-engine` 可以知道具体实现如何装配，但不能拥有业务规则。
- `uc-application` 可以依赖 `uc-core` 的能力接口，不能依赖宿主类型。
- `uc-infra` 实现能力接口，不能反向调用应用流程。
- 绑定只依赖 `uc-engine`，不能穿透到内部 crate。
- LAN 兼容线不能进入默认 P2P 路径，也不能读取 P2P 失败信号后自动接管。

## 4. 数据模型

### 主关系

```text
ClipboardEvent
  ├─ 1..n ClipboardRepresentation ──> Blob
  └─ 1..n ClipboardEntry
          ├─ 1 ClipboardSelection
          ├─ 0..n EntryDelivery
          ├─ 0..n EntryFileSetItem
          ├─ 0..n FileTransfer
          └─ 0..1 SearchDocument ──> SearchPosting / SearchEntryTag

Space
  ├─ 1..n SpaceMember
  ├─ 1..n TrustedPeer
  ├─ 0..n MembershipAnnouncement / OutboxBatch
  └─ 1 SpaceKeyEpochState ──> Revocation / UpgradeRecoveryLog
```

### 剪贴板模型

- **事件**记录一次捕获的时间、来源设备和跨设备内容摘要。
- **条目**是本机历史中的稳定记录，保存创建时间、活动时间、大小、内容类别、收藏和是否启用投递追踪。
- **表示**是一份内容的某种系统格式，例如纯文本、HTML、图片或文件列表。小内容可内联，大内容引用 blob。
- **选择**记录主表示、辅助表示、预览表示和恢复系统剪贴板时使用的表示。
- **活动状态**独立于历史排序，表示当前应生效的内容。跨设备身份是内容摘要，本机条目编号不能用于跨设备比较。
- 普通远端接收与只保存拉取是两种完整模式；调用方不能逐项拼装接收能力，也不能用假写入表达只保存语义。
- 普通远端接收由应用层入站运行期完整负责订阅、成员策略、解密、内容类型策略、应用、宿主通知、发送方确认和关闭等待。
- 引擎只提供完整依赖、转换轻量宿主事件并启动或关闭该运行期，不订阅中间通知，也不判断应用结果或推进发送方确认。

### 内容可用状态

| 状态 | 含义 |
| --- | --- |
| `Inline` | 小内容已以内联密文保存 |
| `BlobReady` | 大内容已物化到 blob，并有可读取引用 |
| `Staged` | 已登记，等待后台处理 |
| `Processing` | 后台正在物化 |
| `Failed` | 本轮处理失败，可带内部失败记录 |
| `Lost` | 内容已永久不可恢复 |

### 文件与传输模型

- 文件集合把一个剪贴板条目中的文件或目录展开为有序项目。
- 目录同步使用文件集合清单，不把目录当成单个不可解释对象。
- 文件内容可以以原始字节保存在受管 blob 或入站缓存中。
- 文件名、原始路径、相对路径、根目录名、传输元数据和恢复记录必须加密。
- 文件传输使用事件时间线记录开始、进度、完成、失败和取消；接收尝试另用尝试编号避免旧操作影响新一轮接收。
- 接收方的用户取消一旦取得该尝试的原子取消权，就由应用层取消在途下载、清理已登记的临时目录、写入已取消终态并发出终态事件；普通文件和目录都不能停留在中间取消状态等待其他回调。

### 成员与安全模型

- **空间成员**表示成员资格和该成员的同步偏好。
- **可信对等端**表示已验证的设备身份关系。
- **在线状态**只是当前可达性的观察，不等于成员资格或信任。
- **成员公告**携带签名后的设备身份、传输地址、序号和当前密钥世代。
- **空间密钥世代**决定当前成员可读取的内容范围；接受成员历史中的移除后必须推进世代。
- 成员关系、空间安全状态、撤销记录和升级恢复状态都必须可跨重启继续。
- 空间保护状态只读取当前空间尚未完成的升级记录；已完成记录不再参与状态判断，也不得因其不可读阻塞当前空间。
- **规格 023 目标**：签名成员历史是唯一的成员事实来源。每个条目绑定父条目、发起成员实例、成员操作、结果成员摘要和安全状态；每次 AddDevice 还永久保存该成员实例的版本化历史验证凭据，成员实例由设备标识与本次凭据唯一确定。
- **规格 023 目标**：事件作者资格和验签公钥都从精确父历史取得；移除决定从被引用移除的父历史验证。两者都不能从当前 OpenMLS 成员树、成员资料、可信关系、地址或在线状态推断。移除只取消后继权限，不删除旧凭据或破坏过去合法事件和决定的验证。
- **规格 023 目标**：正式 AddDevice 是新增资格的唯一正向事实；准入应用回执和完成状态只作为临时负门禁。在双方完成同一事件和安全状态前，新成员不得进入普通运行范围。
- **规格 023 目标**：旧 V1 历史缺少已移除作者公钥时保留原数据并使用规范内容检查点；不同当前成员对同一检查点的证明可以并存，证明者差异不能制造历史分叉。
- **规格 023 核心基线**：`uc-core` 已提供版本化成员历史的单一验证入口，永久保留成员实例凭据，并统一验证精确父位置的作者资格、移除决定、激活回执、旧记录证据、迁移检查点、迁移激活基线和公共安全承诺。该入口只处理历史规则，不读取网络、在线状态、设备资料、当前安全成员树或持久化实现。
- 远端移除先保存为待本机决定，不能改变本机成员集合或越过该条目应用后继。接受才应用该移除；拒绝只隔离对应对端，不自动移除任何其他设备。
- 设备上线只触发有界成员历史核对。在线状态、当前分支成员资格和双方历史关系是独立事实；分叉或待决定对端不能交换普通内容。
- 成员历史、用户决定、对端关系和安全状态都只保存在加密负载中，不形成第二份成员事实。

### 成员上线核对与用户决定（ADR-020）

已认证设备上线后，`WorkspaceConvergence` 自动启动一次有界成员核对。双方先交换 Space 沿革、
本机已经知道的事件头、本机已经实际应用的事件头、成员摘要和决定摘要；完全一致时不发送完整成员
名单或历史，存在差异时才按父事件范围补齐签名事件。

规格 023 的核心历史基线已使每项成员变化绑定唯一父事件、发起成员实例、操作结果、安全状态和签名。验证时先从
精确父历史确认作者当时有效，再使用该成员首次加入时永久保存的凭据验签；当前 OpenMLS 树只表示当前
安全状态，不能证明旧历史。现有生产流程尚未切换到这套核心入口，仍从已应用头和当前 OpenMLS 成员中
解析旧作者；后续持久化与准入阶段必须删除这一限制。连续新增事件验证后可以自动应用；本机用户从未接受的移除只能保存为待决定事实，不能修改本机
成员集合和内容权限，也不能越过它应用后继事件。

用户接受时应用收到的同一移除事件并回复签名决定，双方摘要一致后恢复正常分享。用户拒绝时保持
本机成员集合不变，只把相关设备历史关系标为分叉；拒绝不会自动移除发起者或其他设备。分叉设备
之间停止普通内容和旧 Space 成员历史传播，各自分支内部继续工作。

可达性、当前分支成员资格和双方历史关系是三个独立事实：设备可以同时是在线、当前分支有效成员、
但与本机历史分叉。分叉表示双方拥有可验证但不互认的历史；资料无效表示签名、摘要、身份或结构
不能验证，二者不能合并为一个状态。

上线核对、事件补齐、待决定移除、用户决定、关系门禁、加密保存、重启恢复和完整查询全部由
`WorkspaceConvergence` 负责。产品端只提交一次接受或拒绝并读取完整结果，不编排协议消息。
负责人在开始新准入前核对加密保存的本机成员实例与当前安全身份；两者不一致时保存恢复状态并拒绝
继续准入，不能对外报告加入成功。主动连接也只有在收到远端当前空间明确接纳后才发布在线。

本次成员核对与内容同步流程只服务于 `1.1` 设备之间。对端版本低于 `1.1` 时，已有 Space、成员资料和可发现性
继续保留，但不进入当前流程。负责人保存可跨重启恢复的“需要升级”状态，暂停双方普通内容同步，产品只在 `1.1`
设备一侧提示用户；对端升级到 `1.1` 并重新确认后，负责人清除提示、进入当前流程并恢复内容同步。负责人先取得
本次 `1.1` 的已认证确认；只有未取得该确认且已认证对端成功接受旧入口的空连接时，才判为低于 `1.1`。本次
`1.1` 不监听旧内容入口，空连接不发送旧请求也不改变任何资料。独立的旧空间安全提升入口继续运行，只负责让
原有 Space 建立共同保护状态，不传输普通内容或生成成员事实。双方升级后，稳定发起端建立唯一成员历史起点，
每台设备提交自己签名的当前身份资料；随后只运行当前成员核对和内容流程。未知版本、离线、连接失败、身份不符
和资料损坏不能显示为需要升级。不存在为低于 `1.1` 的设备继续同步内容的备用流程；版本识别与当前 `1.1` 流程
不能对同一对端同时拨号，也不能互相覆盖成员事实、安全状态或用户决定。旧资料保留到另行结束低于 `1.1` 的兼容支持
为止。若进程在共同保护状态写入后、成员历史起点写入前中断，持有未完成升级记录的创建方在重启后继续完成
成员历史，不再重新按设备编号选择负责人。

### 配对作为工作空间准入通道（ADR-017、ADR-022，规格 023）

ADR-017 的目标是成员加入由 `WorkspaceConvergence` 完整负责，配对只保留内部通信职责。正常加入时产品和
绑定只创建工作空间邀请或调用一次 JoinSpace，查询、取消和移除等管理动作也不能看到或驱动候选、历史
分页、安全暂存、确认和重试。配对已经内收为私有通道，产品赎回、发起方编排和空间切换统一由
`WorkspaceConvergence` 推进同一持久尝试，不再保留旧成功路线。发起方只能在加入方验证完整历史并持久
暂存同一候选后正式写入 AddDevice；
提交后双方只向前交付同一事件、目标安全状态和回执，加入方收到完成消息后才返回 Active。该结果不
表示其他离线成员接受或不存在未知分支。进程中断后，负责人从加密保存的同一加入尝试继续。

规格 023 的目标是配对不直接保存成员变化、驱动旧成员补送或持有可选工作空间依赖；配对会话与邀请不是
持久成员事实。工作空间加密保存加入尝试、候选、暂存状态、激活回执、门禁、outbox 和终态，不保留旧成功
路径。长期历史凭据和加密准入仓储已经实施；后者原子保存三类尝试状态、待发送消息、
防重放索引、单调编号和版本，并支持重启处理待发送消息与终态压缩。单一应用事务负责人已经覆盖准入事务、
精确确认、入站候选投影、并发裁决和可恢复的彻底重置编排。常驻 profile 入口、统一三类产品结果和
Candidate、Prepared、Commit、Applied、Complete 五步网络交换均已接入。

规格 023 的应用事务负责人已完成第一条持久闭环：加入方先保存请求，发起方保存候选，加入方保存历史
核对结果，随后按正式提交、双方应用和完成确认向前推进；每一步都先保存状态与待发送消息，重复消息从
已存结果重建同一回复。取消与正式提交只产生“提交前拒绝”或“提交后继续完成”两种结果，终态仅在消息
确认清空后压缩。启动恢复会逐条交给窄发送能力，并按精确持久确认更新状态和清理终态。加入方完成本机
激活或跨 Space 前向切换后，从保存的邀请方地址重开受限准入通道并发送同一 CompleteAck；发送失败不回滚
Active，下次启动继续发送。邀请方收到重复回执或已经压缩终态时均幂等成功。

profile 级 `WorkspaceConvergence` 已随 Engine 常驻，即使没有活动 Space 也负责加入终态、取消和跨 Space
revision/ordinal；活动 Space 上下文仍以完整对象装入或移除。当前仓储
已经保证 profile 内单调编号、单调版本、防重放索引和唯一未收束准入槽；应用层可以投影当前本机加入与当前
Space 唯一入站候选，正式设备信任快照也读取这些投影。活动
`AppFacade` 仍不允许半构造，`uc-engine` 只组装并路由。profile 同时只允许一个被占用的准入槽，不区分
本机加入或为其他设备发起；非终态、共享写前恢复、Space 切换或改变活动世代的清理全部收束后才释放，
避免 Cross-Space 切换遗弃原 Space 的入站尝试。业务已终态且只剩按 attempt 隔离的消息重发或压缩时，
旧记录继续恢复但不阻塞下一次准入。ADR-022 增加唯一例外：新的用户 JoinSpace 可以在同一次持久提交中
把尚未保存 Prepared 的旧本机 Joiner 变成 SupersededByNewJoin，并立即让全新尝试取得同一个槽；这个过程
从不把槽释放给调用方，也不允许取代入站尝试、共享恢复或 Space transition。

加入方已经持久保存 Prepared、尚未收到正式 Commit 而原发起方不可恢复时，无法安全判断是否已经提交，
只能保持 Pending。加入方
已保存 Commit 和应用回执后，任一仍在同一当前分支、已验证并保存正式 AddDevice 和该回执的成员可以
接手完成。`WorkspaceConvergence` 先让帮助设备保存绑定三方身份、双方网络身份、单调计数和随机挑战的
签名证明，再验证加入方使用本次加入专用凭据返回的原事件、原提交和原回执。帮助设备必须证明自己在
候选父位置和当前历史中都有效，并实际应用只属于自己的安全更新；重新计算结果完全相同后，才保存并
返回等价完成消息。中断后从同一加密记录重试，不生成第二成员事实。加入方只接受首份有效完成结果；
没有合格成员可达或帮助设备尚未应用更新时继续保持 Pending。

规格 023 目标还要求：每个 V2 Add 的唯一应用回执按 event_id 永久保存在版本化成员历史中，Completion
只留在加入方本机尝试或压缩终态，不建立独立全网证明账本；创世和两类旧数据迁移建立明确激活基线，其他
V2 Add 对所有观察者都要求永久应用回执，本机加入方还要求完成消息。发起方独占提交或拒绝裁决，加入方取消
只是一项持久请求。取消先保存时提交前拒绝；正式提交
先保存时取消已经太晚，同一尝试继续到 Active，不自动生成另一项成员移除。正式提交到永久回执和
Complete outbox 建立前，本分支串行化其他成员安全变化。

正式准入状态直接扩展现有 `DeviceTrustSnapshot`：`current_join` 返回唯一非终态或最近本机终态，
`pending_inbound_member` 返回当前 Space 唯一入站候选。公开关联使用 `join_id`，内部恢复仍使用 attempt；
产品复用 `CancelJoinSpace(join_id)` 和现有 `RemoveMember(device_id)`，不新增按加入编号查询、入站取消或
待激活专用移除接口。`DeviceTrustSnapshot.revision` 在同一 profile 内跨 Space 单调递增，变化后只发送
`DeviceTrustChanged` 提醒重新查询。`QueryWorkspaceConvergence` 及其事件继续只用于 dev-tools 诊断。

ADR-022 已实施。每次用户明确调用 JoinSpace 都会创建新的 attempt、join、ordinal、成员凭据
和安全材料；断线、重启、消息重放和后台退避继续原尝试，宿主通过 current_join 恢复观察，不重放
JoinSpace。旧本机 Joiner 只有在 Prepared 尚未持久保存且没有相关恢复工作时可被原子取代；Prepared
及以后必须假定邀请方可能已经正式提交，新的调用返回专用 PreviousJoinCannotBeSuperseded Conflict，
原加入继续向前恢复，不能再归入通用错误 1238。取代只留下加密 SupersededByNewJoin 终态和隔离清理，
不调用普通或彻底重置，不清当前 Space、历史、设备身份或本机资料。相同邀请码也创建新本机尝试，但邀请方
已经保存的一次性 claim 不能改绑，新尝试可以得到稳定的邀请已使用结果。

旧加入可能已经在邀请方保存为候选。新的明确加入只负责原子建立新尝试并立即发送自己的请求，不等待旧
清理；旧清理由后台按各自 attempt 和原邀请方地址独立发送。邀请方保存取消结果并返回与该清理精确对应的
拒绝确认；相同清理可以重复发送，邀请方重放已保存的同一结果，不创建第二次裁决。加入方只有收到并验证
邀请方的真实确认后才清除本机待办；无法连接或没有确认时保留密文记录继续恢复，不使用新邀请的连接，也
不再用本机生成的确认代替远端结果。多个历史清理可以分别指向不同邀请方，始终不改变当前加入。

CancelJoinSpace 只与发起方正式提交点竞争。取消先保存时没有 AddDevice 并返回 Rejected；正式提交先保存
时 Commit 是“取消已太晚”的持久依据，加入方继续保持 Pending 直到同一尝试 Active。完成消息和帮助者只
负责恢复同一加入，不能继承取消并各自创建 RemoveDevice；用户仍要退出时从另一台当前成员设备另行使用
现有明确移除流程。

全新加入方在目标 MasterKey 可用前，以系统安全存储保护的 profile admission MasterKey 保存长期元数据，
并由它包裹每次尝试的数据 key；这一加密仓储和终态先重封装、再移除临时 key 的顺序已经实施。安全状态
准备、加入方暂存、从保存结果重新计算公共承诺、启用和放弃也已收进一个适配器。目标 Space 的完整成员
历史、安全状态、内容钥匙目录和成员关系按 attempt 与目标世代加密暂存，并在提升前从生产读取入口重开
核对。J3 明确区分 Fresh、Same-Space 和 Cross-Space：Fresh 创建首个完整世代；Same-Space 保留现有
MasterKey 和内容密文，只提升已验证状态；Cross-Space 排空来源写入，以数据库持久修订固定最终快照，
重封装内容后提升目标世代。三者都只通过原子活动清单决定当前数据库和受管文件目录，设置状态在存在清单
后只从该清单投影。每个阶段均写回同一准入尝试，中断后只向前恢复，不能把整个多步迁移假设成一次数据库
事务。跨 Space 开始联网前先检查来源历史；存在不可读密文且用户未确认时直接返回冲突，不创建尝试也不
联系邀请方。用户确认后，同一选择随尝试和切换阶段加密保存；不可读密文保留原字节并隔离，最终结果返回
保留数量，其余可读数据才重封装到目标 Space。

ResetSpace 由 ADR-024 定义为用户明确触发的设备管理重置。Engine 先排空并停止旧空间运行，再由应用层
唯一负责人保存目标，在不修改旧活动空间的独立世代中重封装本机资料、建立单设备成员与安全状态并完成读取
验证。原子活动清单替换是唯一生效点：切换前失败恢复完整旧空间，切换后只能启用同一新空间；数据库、受管
文件根、安全状态、设置投影和运行会话不得分别决定是否成功。旧世代和暂存采用有归属、可重试的延后清理，
空间不足必须在切换前失败。对外操作名称、输入和成功结果不变，本机一般设置、设备身份、解锁材料和资料保留。
旧设备不会被远程清空，也不能重新取得新空间资格。现有先清理旧关系再提升目标世代的实现不满足该原子边界，
不得作为 ADR-024 完成状态交付。
FactoryResetSpace 是唯一强制
本机销毁边界：先锁 profile、停运行并保存无标识 intent，按固定命名空间清除并确认全部活动/暂存/profile
密钥不存在，再从固定 profile 根清设置、关系、准入密文、邀请、搜索文件及受管 cache/blob 实际文件。
密钥失败不先清设置；任一文件失败保留清理阶段，密钥成功后的崩溃只能继续向前清理。
该 intent 的独立固定别名、随机代次和三值阶段已经实施，并能在重启后继续；profile 准入密钥也提供删除后
重新确认不存在的能力。它不进入数据库/文件明文，也不在准入密钥清除清单中；其余准入状态继续用 MasterKey
加密。FactoryResetSpace 已接入固定清理能力，先释放旧数据库，再删除并确认当前 keyslot、KEK、迁移密钥、
profile 准入密钥和文件网络身份，随后清数据库、配置导入暂存、Space 世代及受管缓存。启动遇到未完成阶段
会续完清理并拒绝暴露旧会话；宿主重新创建 Engine 后进入新 profile 世代。该操作不伪造远端拒绝、取消或
历史回滚，完成后旧会话和订阅失效。

### 搜索模型

搜索只在本机执行，不是远程可搜索加密协议。

- 每个可搜索条目对应一份搜索文档。
- 词项写入前用搜索专用密钥生成 HMAC 标签，磁盘不保存明文词项。
- 预览、文件名、文件路径、链接等渲染字段整体加密。
- 内容删除时同步硬删除文档、倒排项和标签关系。
- 索引版本变化时全量重建；锁定状态不能查询、更新或重建。
- 生产搜索由应用层搜索运行期一次性完整构造，并由它负责后台重建、修复和关闭；引擎层不单独启动或终止搜索任务。
- 空间锁定前必须暂停并等待搜索后台工作退出，解锁或恢复后由空间会话统一恢复；进程关闭后不得再次开启搜索后台工作。
- 无后台能力的场景必须使用明确的只读搜索模式，不能先创建半成品再运行中补装。

### 持久化安全边界

默认规则是：任何写入 SQLite、磁盘缓存或搜索索引的业务负载都先经 MasterKey AEAD 加密，并通过附加认证数据绑定到所属实体。

允许明文保存的例外只有：

- 内容类型分类枚举；
- 文件内容本体；
- 入站文件在受管文件缓存中经安全清理的原始文件名，且只能作为实际缓存文件的 basename。

原始目录路径、数据库字段、搜索字段、日志和其他关联元数据不在例外范围内。

## 5. 网络协议

### 传输基础

默认网络由 Iroh 提供经过身份认证的加密 P2P 连接。业务通道使用不同 ALPN 隔离，防止消息被送入错误处理器。非文件业务内容还使用空间密钥派生的应用层 AEAD；文件内容沿用受校验的 blob 传输格式。

### 逻辑通道

| 通道 | 当前标识 | 用途 |
| --- | --- | --- |
| 配对 | Iroh 原生配对通道 | 一次性邀请、口令证明、身份和空间准入 |
| 剪贴板内容 | `uniclipboard/clipboard-applied/1` | 加密内容头、载荷和接收结果 |
| blob | Iroh blobs ALPN | 大内容与文件字节传输 |
| 活动状态 | `uniclipboard/active-clipboard/0` | 广播当前活动剪贴板引用 |
| 活动内容拉取 | `uniclipboard/active-clipboard-pull/0` | 接收方缺少活动内容时按需拉取 |
| 在线状态 | `uniclipboard/presence/1` | 当前空间接纳确认、可达性探测与上线通知 |
| 文件进度 | `uniclipboard/transfer-progress/0` | 接收方向发送方回报字节进度 |
| 成员安全更新 | `uniclipboard/group-update/1` | 已接受成员历史移除后的密钥世代更新 |
| 成员候选资料 | `uniclipboard/membership-gossip/1` | 公告、候选成员和安全资料核验；不决定成员资格 |
| 旧资料独立化 | 无网络协议 | 升级后保留本机资料，清空旧设备关系，建立仅含本机的新空间并要求全部重新配对 |
| 成员历史核对 | `uniclipboard/membership-history/2` | 已认证成员交换完整版本化成员历史与各自签署的决定 |
| 准入完成接力 | `uniclipboard/workspace-admission-resume/1` | 原接纳设备离线后，合格当前成员验证并完成已提交的同一加入 |

每个通道都必须校验版本、消息类型、长度和上限。未知版本、错误魔数、超大消息、非法身份或无法认证的载荷一律拒绝，不能降级为不安全路径。

规格 023 已把成员历史通道从 `/1` 升为 `/2`，pairing wire 从 V9 升为 V10。V10 先读取固定外层版本和
大小，再解业务 body；`/2` 只接受完整新版历史和新版确认。旧 `/1` 只用于确认对端确实需要升级，不发送或
接受旧历史，也不注册成功处理器，因此不能回退旧成功语义。

配对、成员历史 `/2` 与准入完成接力的每个单帧都在按声明长度分配内存前执行 4 MiB 上限检查，零长度和超限长度直接
拒绝。成员事件、移除决定、激活回执、签名算法和加密仓储元数据遇到未知版本时统一要求升级，不误报为
签名错误、历史分叉或普通资料损坏。profile 级设备信任版本不随 Space 更换重新开始；过期的预期版本、
计数溢出以及版本低于已分配本机加入序号等不可能组合均失败关闭，失败事务不改变历史或公开投影。

### 配对协议

配对是 `WorkspaceConvergence` 内部的准入通信通道。邀请绑定发起方当前事件头；加入方按精确父节点
验证完整历史、历史保存的作者凭据、签名新增候选、自己的身份绑定和目标安全摘要。候选不是成员事件；
只有发起方与加入方都加密保存同一正式 AddDevice 和安全状态，并完成应用回执与完成消息后，加入方才
返回本分支 Active。该结果不等待其他旧成员。

已配置设备在任何 Space Switch 副作用前进入成员核对：同一事件幂等，连续新增可补齐，未确认移除
等待用户，不可比较历史标记相关设备分叉。配对不选择冲突赢家，也不将握手完成单独解释为加入成功。

```text
加入方                    WorkspaceConvergence                    其他成员
  |-- 加入请求 + 邀请码 + 身份 + KeyPackage -->|                       |
  |<-- 父历史 + 口令挑战 -----------------------|                       |
  |-- 口令证明 + KeyPackage ------------------->|                       |
  |<-- Candidate：历史 + 候选 + 暂存安全资料 -----|                       |
  |-- Prepared：已验证并持久暂存证明 ----------->|                       |
  |<-- Commit：同一正式事件 ---------------------|                       |
  |-- Applied：事件和安全状态已落盘 ------------>|                       |
  |<-- Complete：发起方已保存回执 ---------------|                       |
  |                                            |-- 同一事件和回执 ---->|
```

### 成员关系上线核对协议

已认证设备上线后通过 `/2` 交换完整版本化历史。接收方验证沿革、规范摘要、精确父事件中的发起者资格、
父历史保存公钥对应的签名、操作结果、安全状态绑定和连续事件链，不使用当前安全成员树替代过去授权。
发送方可以缺少接收方已经保存的其他成员决定；合并保留双方决定的并集，但发送方只能新增自己签署的决定，
不能携带另一成员尚未由接收方保存的决定。连续新增可自动应用；未由本机接受的移除只保存并等待用户，
不能越过它应用后继事件。

用户接受时应用收到的同一移除并回复签名决定；拒绝时保持本机成员集合，只把相关设备历史关系设为
分叉。分叉设备之间停止普通内容和旧 Space 成员历史传播，各自分支内部继续运行。拒绝不自动产生
移除发起者的事件。

#### 当前成员运行范围

`WorkspaceConvergence` 必须从本机已接受成员历史一次派生完整的当前成员运行范围。普通设备列表、
主动连接、成员发现、内容与文件发送、活动状态和恢复任务只能在该范围内继续筛选；
成员记录、可信关系、网络地址、在线状态和安全组接纳都不能单独把设备加入范围。地址或资料缺失可以
使当前成员暂时不可连接，旧资料存在不能使已移除设备恢复普通资格。

活动 Space 的成员连接运行期启动时立即读取该范围并连接可寻址的已接受成员。若加密资料暂时不可读，
它不把设备加入范围，也不重复发起网络连接；资料可用的统一会话事件会立即唤醒一次连接恢复。真实连接
失败继续使用既有的有界退避和休眠恢复，产品页面的手动刷新只是额外的用户动作，不能成为自动恢复的前提。

统一快照优先读取已验证的 V2 历史。创世成员和经核对的迁移基线成员直接进入当前范围；普通
AddDevice 只有在历史内已有永久激活回执后才进入。新设备本机即使已经保存同一历史和回执，在本机加入
完成状态持久化之前仍关闭全部普通范围；发起设备保存激活回执后可把它视为暂时离线的当前成员。激活
门禁只能减少历史授予的资格；没有 AddDevice 时，任何回执、完成消息或安全状态都不能增加成员。

跨空间加入开始最终切换前，统一快照继续使用来源空间。只有同一次本机加入记录明确绑定当前来源空间和
目标历史时才允许该过渡；正式提交后被移除的加入保留目标历史中的加入与移除事实，并在终态能精确对应
该加入记录时恢复来源空间。无关历史、已开始最终切换后的旧运行入口、历史锁定或损坏均关闭普通范围。
新准入的安全状态接收者同样只从候选父历史中的已激活成员派生；历史凭据和残留成员资料不能把已移除
设备重新加入安全更新。

旧空间安全提升期间，本机建立当前成员历史后，仍可接纳当前成员运行范围内尚未完成提升的旧成员加入
同一保护状态。若当前历史的起点暂时只包含本机，持久旧升级记录中明确处于等待重新加入状态的已知旧
成员也可以完成已经开始的同一次安全提升；该记录只授权加入既有保护状态，不会把设备加入普通运行范围，
也不会提前恢复发现或内容同步。授权不得回退旧成员表；已移除、未知、没有等待重新加入记录或不在本机
当前分支的设备不得通过旧升级入口恢复资格。

接纳端在发出旧升级接纳资料前必须准备自己的当前成员身份；加入端安装共同保护状态后，必须沿刚完成认证
的同一设备关系立即发起一次当前成员历史交换。该交接不依赖普通成员运行范围，避免双方都等待对方先进入
当前历史；只有交换完成并应用的成员才进入普通设备列表、连接和内容同步范围。加入端完成交接后必须立即
重新建立与接纳端的在线关系，不能让升级前的离线结果一直保留到下一轮定时连接或应用重启。

身份解析与当前授权必须分开。不可变成员历史、已完成决定及验证旧事件所需的身份资料继续保留；
向刚被移除设备传递有界成员历史决定使用负责人保存的受限计划，不开放普通在线、升级、发现、内容或
恢复流程。成员历史存在后，读取失败、损坏或锁定一律失败关闭，不得回退旧成员表。只有明确处于受
支持旧空间模式且尚无当前历史时，旧升级流程才可使用旧成员范围；建立当前历史后不再回退。

刚进入本机已应用分支的成员事件由一个可重入、可恢复的效果执行入口按历史顺序处理。本地决定、远端
自动应用和重启恢复不能各自维护事件分支。新增设备只有在必要资料和安全效果完成后才进入普通范围；
移除一经本机接受并保存就立即退出普通范围。跨存储中断通过加密保存的有界效果进度恢复，物理整理旧
关系资料不是授权正确性的前提。

### 设备信任产品边界

`WorkspaceConvergence` 同时负责把已验证成员历史转换为一个完整设备信任结果。产品一次读取即可获得
变化来源、精确目标、两种选择的关系影响、每台设备独立的在线状态、成员资格、设备组关系、版本兼容
和可直接展示的同步结果。在线状态只作为独立观察，不参与成员决定；整个工作空间或某台设备的资料
无法验证时，结果统一暂停相关同步并隐藏不可安全决定的变化。

用户只提交当前变化编号、应用或保留选择，以及本机将被移除时的明确二次确认。负责人串行处理决定，
继续复用同一签名、保存、门禁和后台传播路径；相同重复、相反重复和过期提交返回不同稳定结果，不建
第二份决定队列。状态保存成功后只发送 `DeviceTrustChanged` 失效提醒，产品收到后重新读取完整结果。
Engine、iOS、Android 和 HarmonyOS 共用同一版本契约，三个移动平台透传同一份完整 JSON 结果，不在
绑定内重算关系。旧查询、旧决定和旧提醒不进入正式产品构建；旧内部快照只允许在明确开启的诊断
构建中供多设备测试观察。共同设备组恢复仍未实施，当前结果明确返回不可用。

工作空间收敛记录继续整块加密保存，密文内带明确格式标记。每个已落盘版本使用独立且固定的持久化
结构，不直接序列化会随业务演进的领域状态；新增字段必须启用新版本并提供显式转换。已有收敛状态行
按其实际格式读取；`1.1.0-rc.3` 写入的 V2 记录按该版本的固定字段顺序恢复，新增的成员历史分页暂存
初始化为空，再通过保留源密文、旁写目标、重新读取核验和最后切换的流程迁移到 V3。任何解析、身份
核对、旁写或复读失败都保留 V2 原密文且不启用半成品，问题版本已发生过的失败读取不影响后续修复版
重试恢复；
旧版资料可能尚未保存收敛状态行。Engine 只把确认的首次安装视为新资料；已经初始化的安装只要状态行
确实不存在，就建立等待旧空间共同升级的来源，并在会话就绪后的恢复流程中加密保存。这样即使应用版本
记录已被先前版本推进到当前版本，保留的旧成员关系也仍能进入既有受控迁移，不能补造签名成员历史。
已有收敛状态始终优先；共同保护关系和当前签名成员历史都覆盖保留成员后才清除迁移标记，成员历史不得
先于共同保护关系结束升级。全新安装不能开启旧成员范围；版本记录文件读取失败或损坏时停止本次启动。
无法按任一受支持布局读取的数据保持关闭式失败，并通过独立稳定错误与普通收敛失败区分。
新建空间尚未建立签名成员历史时，设备信任查询复用同一负责人给出的当前成员范围结论。明确处于
旧保护状态时，已恢复的本机空间会话证明本机仍在空间中；旧成员表只提供其他设备，不能要求它重复
保存本机身份。缺少旧保护状态或当前签名历史时继续返回不可用，不根据设备数量或到达顺序猜测成员
关系。

### 成员候选资料核验

收到候选资料不等于成员资格。候选设备必须经过空间一致性、身份指纹、安全历史和直接证明验证后才能进入就绪状态；成员资格始终只从签名成员历史得出。

候选的状态转换由 `SpaceMembershipCandidate::apply(event)` 单一入口驱动，事件是状态变化的唯一途径；转换返回的效果（持久化、唤醒运行期）由调用方执行：

```text
事件：Seed / VerifiedAnnouncement / VerifiedPeer / Confirming / AttestationFailed
     / SecurityMaterialApplied / Admitted

Pending ──Confirming──────────────────────► Verifying
Pending ──AttestationFailed(可重试)────────► WaitingForPeer / WaitingForUpdate（带退避）
Verifying ──AttestationFailed(可重试)──────► WaitingForPeer / WaitingForUpdate
Verifying ──AttestationFailed(InvalidProof)► Rejected（终态）
Verifying ──VerifiedPeer(Updated)──────────► 合并资料，保持 Verifying
Verifying ──Admitted──────────────────────► Ready（终态）
WaitingForPeer ──Seed(新资料)──────────────► Pending
WaitingForUpdate ──SecurityMaterialApplied► Pending（资料已到达，立即重试）
任意状态 ──Seed/公告/证明(身份冲突)─────────► Blocked（终态）
Blocked / Rejected：拒绝一切推进事件，终态不可回退
```

会话恢复成功后，成员关系收敛运行期自动调度一轮“已有设备查找”，向当前 Space 中已配对且可信的设备查询本机尚未认识的成员，并复用与手动刷新相同的确认、保存和状态通知流程。自动调度按 Space 合并：会话锁定时不开始；一次恢复只启动一轮，重复解锁、就绪或在线事件不产生重复查找；来源设备离线时等待其重新上线后继续，不进入无限重试。产品端不保存查找队列，也没有新增可见状态。

本机成功应用的安全升级资料会持久化保存，作为后续向落后设备转发的资料源：缺失资料的设备既可以通过摘要交换请求，也可以在“已有设备查找”的成员种子中直接获得，不再依赖资料拥有方按其自身节奏推送。存在等待安全资料的候选时，收敛循环还会主动向已连接成员请求缺失资料，收到后立即应用并重新尝试建立关系，避免多方因资料缺失相互等待。

### LAN 兼容协议

LAN HTTP 位于 `compatibility/`，使用独立的 `uc-mobile-v*` 版本和发布流程。它只在用户明确启用时运行，不能因为 P2P 不可达、平台进入后台或中继失败而自动开启。

## 6. 同步流程

### 本机内容产生与发送

```text
系统剪贴板变化或显式发送
  -> 检查空间已解锁和是否为自身回写
  -> 读取并规范化多个表示
  -> 计算跨设备内容摘要并去重
  -> 加密保存事件、条目、表示和资源
  -> 更新活动状态和本地搜索
  -> 按信任、在线状态和成员发送偏好筛选目标
  -> 逐目标加密发送并记录投递结果
```

空目标列表表示发送给所有符合条件的可信设备；非空列表只能缩小范围，不能绕过信任和偏好检查。发送结果区分已接受、重复、离线、失败和仍在等待，等待不能伪装成失败。

### 离线投递恢复

`ClipboardSyncRuntime` 是本机剪贴板投递的唯一负责人：本机复制、设备上线和用户手动重发都经由它进入既有发送或接收流程。同步总开关是所有出站、入站、文件投递、恢复和历史恢复广播的总许可；关闭时不做任何同步。总开关开启后，自动同步只控制本机复制的自动发送和离线自动补送；文件同步保持独立，手动重发只选择用户允许的设备且不受自动同步开关影响。自动恢复只选择这台刚上线设备的待送内容。某个既有设备暂时不可达时，现有投递记录保存该事实。对同一设备，新的本机复制会将更早的 `Unreachable` 记录标记为 `Superseded`，因此最多只有最新内容保留自动补送资格。该设备重新上线或应用重启后重新发现其在线时，运行期只发送这一条，发送结果继续由原有投递记录覆盖。

运行期不保存内存队列，也不为新设备生成历史候选。已接受、重复或被新内容替代的内容、远端来源的内容、明确失败或无法重新取得的内容都不会自动补发；内容无法取得时记录为明确失败，停止自动恢复。自动同步关闭时，本机复制不产生发送尝试，既有离线记录也不会自动补发；总同步关闭时，手动重发和入站内容同样被拒绝。一次发送仍不做无限循环重试，用户手动重发保留为独立动作。

### 远端内容接收

```text
收到加密帧
  -> 校验对端身份和消息边界
  -> 检查同步总开关
  -> 检查该成员的接收总开关
  -> 应用层解密并解析内容
  -> 检查内容类型接收偏好
  -> 去重或创建接收尝试
  -> 物化内联内容、blob 和文件集合
  -> 原子提交历史、资源、搜索和接收结果
  -> 按策略写入系统剪贴板并发出宿主事件
```

失败或取消必须清理临时资源，并保持已提交数据的一致性。重启后通过接收尝试、传输时间线和产物日志继续收敛或清理。

应用层只提供两种完整的远端内容处理模式。普通接收必须同时具备系统剪贴板写入、活动状态推进、重复内容重新激活、临时接收终结和进度回报；活动内容按需拉取只负责保存、文件落地、接收记录、搜索和可用性判断，不得提前写入系统剪贴板或推进活动状态。引擎层只能选择其中一种完整模式，不能逐项拼装或用空实现模拟缺失能力。

### 活动剪贴板收敛

活动状态使用最后写入获胜规则。排序键为 `(activated_at_ms, activated_by)`：时间较新者胜出，时间相同则设备编号字典序较大者胜出。

接收一个活动状态时按以下顺序处理：

1. 空间锁定时丢弃，不排队补做。
2. 与当前值相同或更旧时忽略，阻止循环广播。
3. 时间戳超出本机未来五分钟时拒绝，避免错误时钟长期压制真实更新。
4. 检查成员和内容类型接收偏好。
5. 本机已有完整内容时写入系统剪贴板；缺少内容时向报告者拉取、解密并持久化。
6. 只有系统剪贴板写入成功后才推进寄存器，并把同一激活继续广播。

核心不允许出现“寄存器已经前进，但系统剪贴板没有成功写入”的状态。

### 设备重新在线

设备重新可达后，`WorkspaceConvergence` 先运行成员历史核对；只有关系允许时才重新发送当前活动
状态。离线只表示暂时不可达，不删除成员、不撤销信任、不改变分叉关系，也不触发 LAN 自动回退。

成员历史按确定顺序分页传输，每页每类记录最多 256 条。接收方按来源设备加密保存已收到的连续页面；
断线或进程重启后，发送方可从第一页重新开始，接收方返回下一缺失页并幂等跳过相同页面。乱序页面只
请求缺失位置，混入另一轮资料或同一位置内容冲突时清除该轮暂存并拒绝。只有全部页面到齐、完整历史
重新验签且确认是当前历史的合法延续后，才能一次替换正式成员历史并回复成功；未完成页面永远不能改
变当前成员资格。

### 成员移除

成员移除作为签名成员历史的一个条目保存。发起设备立即应用自己的条目并停止向目标发送；其他设备上线后先验证和保存该条目，只有本机用户接受才应用。重新准入同一设备会产生新成员实例，不受旧实例移除结果影响。

移除事件在发起设备本机立即生效；其他设备上线后验证并保存该事件，但本机用户从未接受时不得修改
成员集合或安全状态。用户接受后应用同一事件；用户拒绝后保持本机分支，并把与发起设备的历史关系
设为分叉。拒绝不会自动移除发起者。已在另一分支被移除但尚未知情的成员仍可继续自己的分支，这是
明确接受的风险。

本分支移除应用后，`WorkspaceConvergence` 使用该分支的有效设备集合约束对外设备名单、实时名单更新、连接刷新和内容接收，不能从原始成员、地址记录或旧连接重新带回被移除设备。原始记录只保留用于成员历史核验；无需额外通知通道。

### 历史重发与恢复

- 重发只允许本机来源且内容仍可用的历史条目。
- 恢复系统剪贴板支持完整格式、纯文本和文件路径三种模式。
- 恢复成功后才更新最近使用状态并按既定规则广播。
- 内容已丢失、模式不适用、记录不存在和真实执行失败是不同结果。

### 切换空间

已完成设置的设备加入另一个空间时，历史必须从旧 MasterKey 安全迁移到新 MasterKey：

1. 用临时迁移密钥加密备份现有表示。
2. 完成新空间握手并切换会话与安全存储中的密钥。
3. 用新 MasterKey 重写主数据。
4. 写入新成员关系并删除备份与临时密钥。

每个阶段都持久化。进程中断后按阶段继续；不能以删除历史作为恢复手段。

同一空间沿革的邀请必须在进入上述迁移前完成成员核对：相同事件幂等，连续新增只补齐，未确认
移除等待用户，不可比较历史标记相关设备分叉。任何同空间情况都不得准备跨 Space 备份、替换
MasterKey 或清理成员关系；分叉本身不强制整个 Space 切换。

迁移预检如果发现当前密钥无法读取的历史，必须在联系新空间之前停止并要求用户明确确认。确认后保留原始密文，将该记录标记为内容不可用，并继续迁移其余可读历史；不得自动删除、改写或向正常读取路径暴露异常密文。

## 7. 错误处理

### 公共错误

公共错误只包含：

- 稳定编号；
- 错误类别；
- 是否建议重试。

错误编号只在对应操作的语境内解释，不保证跨操作全局唯一。宿主不能只看一个数字判断含义。

| 类别 | 含义 | 常见处理 |
| --- | --- | --- |
| `InvalidInput` | 输入、分页标记、句柄或内容类型无效 | 修正输入，不自动重试 |
| `InvalidState` | 当前生命周期、锁定或空间状态不允许操作 | 刷新状态后再决定 |
| `Unauthorized` | 口令、身份或宿主权限不通过 | 重新认证或请求权限 |
| `NotFound` | 邀请、成员、历史或资源不存在 | 刷新列表，不盲目重试 |
| `Conflict` | 当前事实与操作冲突 | 展示业务结果或重新查询 |
| `Unavailable` | 网络、索引、宿主能力或临时服务不可用 | 仅在标记可重试时重试 |
| `DeadlineExceeded` | 操作超时或因生命周期期限取消 | 允许用户重新发起 |
| `Internal` | 不能向宿主公开细节的内部失败 | 记录脱敏诊断并提示失败 |

### 业务结果与错误的边界

预期业务分支应返回明确结果，不应伪装成异常。例如：重复内容、对端离线、邀请已过期、没有活动剪贴板、恢复模式不适用、内容已经丢失和取消已太晚。

真正的基础设施失败才转换为公共错误。底层数据库错误、文件路径、网络库错误、密钥信息和用户内容不得进入公共结果或调试输出。

### 操作终态

每个已接受的操作都必须产生一次 `OperationFinished`：成功、失败或取消。生命周期截止时间到达时，未完成操作先被取消并收到终态事件，不能静默消失。

### 重试原则

- 只有明确标记为可重试的错误才允许自动重试。
- 重试必须有边界、退避和取消入口。
- 成员收敛、安全更新和后台恢复可以由负责模块持久化重试。
- 上线核对、缺失历史、待用户决定和分叉关系由 `WorkspaceConvergence` 持久化并跨重启继续；离线
  只暂停网络动作，待决定不能自动接受，分叉只隔离相关设备关系。
- 成员历史核对和用户决定由 `WorkspaceConvergence` 负责；产品端只展示完整结果，不安排重试或恢复流程。
- 用户提交的发送、恢复和导出操作在暂停后不自动重放，避免重复副作用。
- 数据损坏、身份冲突、版本不兼容和认证失败必须关闭式失败，不能降级绕过。

### 网络会话恢复

应用层的 `NetworkRecoveryFacade` 是网络会话恢复的唯一流程负责人。它只接收脱敏的本机网络恢复、此前在线设备路径已确认耗尽和新鲜拨号成功观察，并只向 Engine 请求“重建当前网络会话”这一完整动作；它不持有 Endpoint、Router 或第三方网络错误。Engine 的 `SessionSupervisor` 是该动作的唯一执行者，负责关闭操作门、等待或取消旧操作、停止完整旧会话、终结活动传输、创建并安装完整新会话；构建失败时保持操作门关闭。

自动恢复必须同时满足当前会话代次的本机换网窗口仍有效、故障设备此前在线、基础设施已经完成受限确认且同一失败周期尚未重建。普通设备离线、冷启动首次连通、过期观察和旧代观察都不能触发重建。任意新鲜拨号成功会清除该失败周期。

基础设施只向 Engine 传递“本机中转恢复”“此前在线设备确认路径耗尽”“新鲜拨号成功”三种脱敏事实。严格窗口内的路径确认使用两轮各最多两秒的拨号；第一轮失败后全局至多一次地重置解析状态并提示当前会话重新检查，第二轮仍失败才上报路径耗尽。LAN 兼容模式不创建该观察，也不得自动切换到 LAN。

自动和手动请求共享同一轮恢复，任何时刻只允许一个重建动作。临时失败按固定的 `1s`、`2s`、`5s`、`10s`、`30s` 间隔重试，连同首次共最多六次；全部失败后状态明确为可重试失败，关闭后取消等待且不得重新启动。Engine 后续只负责安全替换完整运行会话，并把稳定状态和结果转换给产品及绑定层。

### 日志与隐私

日志不得包含剪贴板内容、密码、密钥、完整令牌、文件名、文件路径、设备备注或可恢复这些内容的派生值。需要排障时记录稳定编号、阶段、计数、耗时和脱敏身份。

成员资料交接日志记录排队、发送开始、接收确认和重试四个阶段；每条只包含脱敏目标身份、资料数量、批次数、单批大小、单批上限、尝试次数和稳定失败类别。重试还记录下一次尝试时间。设备名、地址原文、安全资料和它们的摘要都不得写入日志。

移动绑定在系统日志（OSLog / logcat）之外叠加按天滚动的文件层，写入宿主 cache 目录的 `logs/` 子目录，文件名为 `engine.YYYY-MM-DD.txt`，只接收 `info` 及以上级别；系统日志层不加过滤。日志目录由宿主能力提供，创建失败时降级为仅系统层，不影响启动。

连接刷新成功（每次拨号或恢复）后，核心记录仍在线且经中继连接的对端本次最终选择的中继地址；每条只包含稳定设备标识与中继地址，快照查询失败只记录脱敏失败类别，不改变刷新结果。中继地址是用户配置的连接端点，不包含访问令牌。

### 剪贴板同步详细延迟日志

剪贴板同步的详细延迟只有一个诊断出口：`uc-observability-contract` 以 `info` 级别写出目标为 `uc_otlp`、`event.name` 为 `clipboard.sync.stage.completed` 的结构化日志。每条记录都带有跨设备关联编号、是否为旧协议生成的临时编号、发送或接收方向、内容类别、大小区间、这次实际使用的直连或中继路径、阶段和毫秒耗时；不带内容、设备名、文件名、路径或任何可还原这些信息的字段。

| 记录端 | 阶段 | 含义 |
| --- | --- | --- |
| 发送端 | `source_to_dispatch` | 从本机刚观察到复制到该对端开始发送前的全部本地准备；手动重发等没有新复制时不伪造此值。 |
| 发送端 | `address_resolution`、`connection`、`stream_open`、`frame_write` | 查询地址、建立连接、打开传输通道和写入帧的本机耗时。 |
| 发送端 | `receiver_apply_wait` | 帧写完后，等待对方完成接收处理并回执的耗时。 |
| 发送端 | `dispatch_to_remote_commit`、`copy_to_remote_commit` | 单个对端从开始发送到对方完成，以及从本机复制到对方完成的总耗时。 |
| 接收端 | `receiver_queue`、`receiver_policy`、`receiver_decrypt`、`receiver_preflight_decode`、`receiver_apply` | 对方帧到达后的队列等待、策略检查、解密、内容分类所需的预解析，以及随后完整接收处理的耗时。完整接收处理包括详细解析、去重、需要时的内容落地和成功保存。 |

发送端和接收端只在对方成功保存，或确认内容已存在而无需再次保存后写出这些阶段。发送端的完成回执来自接收端的实际处理结果，因此总耗时的终点不是“数据已写入网络”，而是“对方已完成”。直连或中继取这一次传输结束时的实际连接路径；一时无法确定时明确标记为未知，不以猜测替代结果。

Engine 不初始化 OTLP 发送器，也不直接请求第三方服务；它只写出上述结构化本地日志。桌面和移动宿主可按各自的日志设施保留、查看或采集这些记录，并在宿主侧决定是否转发到任意 OTLP 收集端。产品分析仍只接收原有的成功、失败和首次同步等汇总事件，不接收逐阶段延迟记录。

## 8. 状态机

### Engine 生命周期

```text
Running -> Quiescing -> Quiesced -> Suspended -> Running
   |           |           |           |
   +-----------+-----------+-----------+-> ShuttingDown -> Stopped
```

- `Running`：唯一接受新操作的状态。
- `Quiescing`：停止接收新操作，等待在途操作。
- `Quiesced`：在途操作已结束或已取消。
- `Suspended`：节点、会话任务和连接已释放，但实例与事件流保留。
- `ShuttingDown`：关闭全部运行资源。
- `Stopped`：事件流关闭，实例不可恢复使用。

### 配对邀请

```text
Pending -> Consumed
   |----> Revoked
   +----> Expired
```

邀请只允许使用一次。撤销和过期都是终态，不能重新激活。

### 内容物化

```text
Inline

Staged -> Processing -> BlobReady
             |-------> Failed
             +-------> Lost
```

失败可以由明确流程重试；永久丢失只能作为稳定结果展示，不能伪造空内容。

### 接收尝试

```text
Receiving -> Committing -> Completed
    |            |
    |            +-> Failing -> Failed
    +-> Cancelling -> Cancelled
    +-> Failing ----> Failed
```

每轮接收都有独立尝试编号。取消、提交和失败处理通过原子认领决定唯一胜者，过期请求返回“已被新尝试取代”或“已经太晚”。取消权取得后，下载线程与用户取消入口可以先后完成同一个“已取消”收尾；相同尝试、无新增产物的重复取消收尾视为成功，不得把已取消误报为失败。不同尝试或其他终态仍按冲突处理。

### 文件传输时间线

```text
Started -> Progress* -> Completed
                    \-> Failed
                    \-> Cancelled
```

`Started` 只能出现一次；终态后不能追加进度或第二个终态。文件进度是可重复事件，不是独立传输身份。

接收方通过应用层的文件传输会话完成整段流程。创建会话时会在同一个入口登记接收上下文并写入开始状态；同一传输编号在进程内只对应一个活动会话。进度、完成、失败和取消都由该会话串行处理，重复提交同一种终态不会重复写入，不同终态并发时只有一个可以成功。

Blob 拉取和移动流式上传都持有或复用同一个会话，不再分别调用开始、进度和结束步骤。正常暂停会在网络停止后取消仍活动的会话，最终关闭会停止接受新会话；跨重启遗留仍由已有的启动恢复处理。`AppFacade` 不公开文件传输内部对象或逐步状态入口。

移动流式上传由移动同步应用入口内部的单一负责人管理。它生成并登记对外不可解析的上传句柄，持有临时写入、字节统计、进度节流和文件传输会话，并统一处理追加、完成、取消和关闭。追加、进度或完成失败会在同一处清理临时写入并记录失败；显式取消和关闭会记录取消。关闭会等待已经开始的操作退出，再终结其余上传。引擎只转换四个稳定用户动作的输入、结果和错误，不保存上传表或临时文件对象。

### 历史维护运行期

历史维护由应用层历史功能内部的单一运行期负责。启动时立即执行一轮，之后按固定间隔复用同一条核对、文件清理、保留策略流程。核对失败会跳过本轮两个删除步骤，文件清理失败仍继续保留策略，任一单轮失败都不终止后续定时维护。运行期只记录不含内容和路径的汇总结果；关闭会立即结束定时等待，并等待已经开始的一轮完成。引擎只启动和关闭该运行期，不掌握维护步骤、间隔、失败分流或日志。

### 应用总入口

活动 Space 生产运行期只接收完整构造的 `AppFacade`。唯一装配点必须一次提供空间、成员、接收、搜索、恢复、剪贴板同步、出站和 Blob 能力；不得使用默认值补空，也不得在运行中继续安装能力。恢复和重启重新执行同一完整装配。

另有一个职责明确的 profile 级 `WorkspaceConvergence` 运行期：它在无 Space 时也常驻，负责加入、取消、
终态投影和恢复，并持有零或一个完整 `AppFacade` 上下文；它不是缺能力的空 facade，
也不对外暴露活动 Space 的半成品。活动 Space 提升只能由同一准入事务一次替换完整上下文。

`AppFacade` 的内部对象全部私有。引擎只能调用面向用户动作的顶层方法，不能读取子对象后自行选择步骤或路线。局域网移动同步由运行会话单独持有，不在应用总入口保留永远不会装入的槽位。

应用层不保留脱离真实运行流程的通用生命周期空壳。搜索入口始终随完整搜索运行期构造，不提供只读半模式或同步重建旁路；暂停、恢复、后台重建和关闭只有空间会话与搜索运行期这一条路径。

### 成员候选收敛

```text
Pending -> WaitingForPeer / WaitingForUpdate -> Verifying -> Ready
    |                 |                         |
    +-----------------+-------------------------+-> Blocked / Rejected
```

`Ready` 才表示候选具备完整、连续且可验证的安全历史。等待对端和等待更新是可恢复状态；身份冲突、安全历史冲突和无效证明必须阻塞或拒绝。

### 工作空间成员变化状态（ADR-020）

```text
reconciliation: Idle -> Comparing -> FetchingHistory -> Consistent -> Idle
                              |              |
                              |              +-> PendingRemovalDecision
                              +-----------------> Diverged / Invalid
```

每个设备同时公开三个独立维度：可达性 `Online/Offline`，本分支成员资格 `Active/Removed`，以及
双方历史关系 `Unknown/Consistent/PendingRemovalDecision/Diverged/Invalid`。
待决定移除可以让已知事件头领先已应用事件头；后继事件不能越过它应用。接受后可回到
`Consistent`；拒绝后进入 `Diverged`，只隔离相关设备关系，不停止整个 Space。

### 空间切换迁移（当前旧实现，规格 023 实施后删除）

```text
None -> Prepared -> HandshakeDone -> Swapped -> None
```

`Prepared` 以前可以放弃并保留旧空间；`HandshakeDone` 以后旧密钥已切换，只能继续完成迁移。每个阶段都必须可在崩溃后恢复。ADR-020 要求同空间邀请在进入该状态机前完成成员历史比较；分叉只隔离相关对端，不停止原 Space 的其他关系。

规格 023 实施后，旧独立状态机只由一次性私有迁移器读取并收束，生产恢复入口和公开阶段全部删除。新的
Cross-Space 迁移只属于同一 AdmissionAttempt：`SourcePrepared -> TargetStaged -> ActivationStarted ->
SourceFinalized -> DataRewrapped -> TargetPromoted -> CleanupPending`，活动清单在完整目标世代验证后只提升
一次，profile 级 `WorkspaceConvergence` 是唯一启动恢复入口。

## 9. 跨平台差异

平台差异只能影响宿主能力、运行时限制和语言边界，不能改变空间、身份、配对、加密、协议和数据语义。

| 平台 | 接入方式 | 主要差异 |
| --- | --- | --- |
| macOS / Windows / Linux | 直接依赖 `uc-engine` | 宿主实现系统剪贴板、目录、安全存储和文件句柄；Linux/Wayland 可能提供一次性快照 |
| iOS | UniFFI + XCFramework + Swift 绑定 | Keychain、App Group、后台限制；主应用、分享扩展和键盘扩展各有短生命周期实例 |
| Android | UniFFI + AAR + Kotlin 绑定 | 启动前安装 application context；Keystore；`content://` 句柄；后台和剪贴板访问受系统限制 |
| HarmonyOS | N-API + HAR + ArkTS 声明 | 通过线程安全回调提供宿主能力；64 位文件偏移和大小以十进制字符串跨边界 |

### 共同要求

- 所有平台使用同一个 `v*` Release 中的同版本 Engine 和绑定。
- 移动端进入后台调用 `suspend`，回到前台调用 `resume`，退出调用 `shutdown`。
- 进程被系统终止后重新 `start`，不能复用旧内存实例。
- 事件必须持续消费；收到 `RefreshRequired` 后重新查询真实状态。
- iOS、Android 和 HarmonyOS 必须为成员移除公开相同的完整状态、稳定错误、提交入口、当前查询和变化事件。
- iOS 和 Android 的绑定将一次新增、修改或删除中继节点及其访问令牌交给 Engine 的原子设置操作；绑定先读取当前节点列表并只合并该次变更，令牌只经宿主安全存储使用，绑定不得将其持久化或返回给产品界面。
- 文件必须通过不透明句柄分块读写，不能把路径伪装成句柄。
- 平台限制不构成自动切换 LAN 的理由。

### 移动端产品分析边界

iOS 和 Android 绑定提供两个启动方式：原启动方式不启用产品分析；宿主明确选择带产品分析的启动方式后，才向核心提供事件发送、分析身份保存能力和一份启动时固定的平台信息。核心负责事件名称、脱敏属性、固定平台字段和身份切换顺序，移动宿主负责用户许可、供应商发送、失败重试，以及匿名身份和空间成员身份的持久保存。

公共观测约定定义 `$os`、`os`、`os_version`、`$device_type`、`arch` 和 `app_channel` 六个固定字段。移动端和桌面端都使用同一份定义；宿主在启动时提供实际值，发送层将它们加到每条事件，业务事件不能覆盖。移动宿主收到事件后应快速放入自己的发送队列，不能让第三方网络请求阻塞核心流程。事件属性只允许来自核心定义的脱敏字段，不得追加剪贴板正文、设备名、密码、密钥、令牌、文件名或路径。事件发送失败只影响观测；身份保存失败必须明确返回失败，由核心现有流程决定本次业务操作是否继续。Engine 和绑定不依赖 PostHog、Sentry 或任何 OTLP 发送器，具体供应商由产品宿主选择；剪贴板逐阶段延迟仅作为本地结构化诊断日志保留，不进入产品分析事件。

### iOS 多进程约束

主应用、分享扩展和键盘扩展不能共享内存实例。它们可以通过 App Group 目录和 Keychain Access Group 访问同一份受保护状态，但同一时刻只应有一个进程持有 P2P 运行会话。扩展启动时应根据安全存储可访问性决定是否允许恢复解锁。

### 剪贴板监听差异

变化监听是可选能力。宿主若已经从系统回调取得完整快照，下一次核心读取必须优先消费这份快照，避免 Wayland 等一次性数据源在二次读取时丢失。核心暂停或锁定期间的变化不排队补发。

## 10. 开发规范

### 修改前

1. 先确定唯一负责完整结果的模块。
2. 写清调用方唯一需要做什么、成功和失败返回什么、重启或重试由谁负责。
3. 判断是否新增持久化字段、文件、日志字段、网络消息或公共契约。
4. 新增持久化内容默认按敏感数据处理；明文例外必须有明确批准。
5. 外部能力只能通过 `uc-engine` 提供，不能让产品仓依赖内部 crate。

### 修改中

- 项目文档使用中文；代码标识符、代码注释和提交信息使用英文。
- 保持单一事实来源，不长期保留新旧两套实现。
- 生产代码禁止 `unwrap()`、`expect()`、`println!()` 和 `eprintln!()`。
- 日志和 `Debug` 输出必须脱敏。
- 文档中的仓库路径使用相对路径。
- Rust 命令从仓库根目录运行。
- 业务流程不能由宿主按内部步骤逐一调用。
- 绑定只能做转换，不能承载业务规则。
- P2P 失败不能自动进入 LAN 兼容线。

### 架构文档同步规则

任何 Agent 修改仓库内容时，必须在同一次交付中检查并更新本文：

1. 修改目标、分层、模块职责时，更新第 1 至 3 节。
2. 修改持久化、加密、搜索或迁移时，更新第 4 节。
3. 修改消息、通道、握手、兼容策略时，更新第 5 节。
4. 修改业务顺序、后台恢复或重试时，更新第 6 节。
5. 修改错误、结果或日志规则时，更新第 7 节。
6. 修改状态或转换规则时，更新第 8 节。
7. 修改绑定、宿主能力、最低平台或系统限制时，更新第 9 节。
8. 修改维护和交付规则时，更新第 10 节。
9. 即使确认没有架构语义变化，也必须在下方维护记录中增加一行，写明修改范围和“无架构变化”。

不能只更新维护记录而漏掉受影响正文，也不能为了让文档通过检查而写入未经代码验证的描述。

### 交付检查

不涉及行为改动时，至少运行：

```bash
cargo metadata --locked --format-version 1
cargo check --workspace --all-targets --locked
cargo fmt --all -- --check
node scripts/architecture/check-engine-repository.mjs
git diff --check
```

涉及行为改动时，在此基础上运行相关单元、集成和端到端测试。涉及发布时还必须运行：

```bash
node scripts/release/verify-release-bundle.mjs <产物目录>
```

未执行的设备验收只能记为“跳过”，不能记为“通过”。

### 评审检查表

- 负责完整结果的模块是否唯一？
- 调用方是否只需一个主要入口和少量明确结果？
- 敏感数据是否在所有成功、失败、迁移和恢复路径中保持密文？
- 公共错误、事件和状态是否稳定且不泄露内部细节？
- 重试、取消、暂停、恢复和关闭是否有唯一所有者？
- 平台差异是否停留在宿主和绑定边界？
- 删除负责模块后，复杂度是否会重新散落到多个调用方？
- 本文正文和维护记录是否已同步？

## 文档维护记录

- 2026-08-20：单条历史记录的发送视图在返回任何结果前确认当前成员范围，并用该范围与历史可信关系的交集组装本机发送情况；历史关系和旧发送事实继续保留，当前范围不可用时查询失败。该修改补齐既有当前成员范围规则，不改变分层或所有权。
- 2026-08-21：补充当前成员范围不可用时的脱敏原因分类，区分旧成员记录读取、空间保护关系读取以及缺少当前成员历史；只记录固定类别、保护模式、布尔值和数量，不记录设备名称、文件名、路径或用户内容。不改变成员判断、持久化或对外接口。

- 2026-08-21：修复已初始化旧资料完全缺少空间收敛记录时无法恢复的问题。除确认的首次安装外，缺少记录的既有安装会建立并加密保存旧资料迁移来源；已有记录始终优先，全新安装保持原行为，恢复过程不补造签名成员历史。不改变负责人、对外接口或持久化格式。

- 2026-08-21：补全已被早期修复转换为新格式的 `1.1.0-rc.3` 空间恢复。恢复判断不再依赖会随正常流程变化的完成阶段，而以本机身份、既有设备关系、未移除、无恢复失败且缺少成员历史这些持久事实为准；处于正常应用或收敛中的受影响空间也能恢复迁移来源，已移除、需要人工恢复、已有失败或证据不足的状态仍保持拒绝。不改变负责人、对外接口或密文持久化格式。

- 2026-08-20：为升级后当前成员范围不可用的问题增加不含用户资料的分类诊断日志，区分成员历史读取或解析、当前加入记录读取、沿革不一致、设备资料读取，以及有效成员缺少设备对应记录；日志只记录固定分类、数量和布尔值，不改变成员判断、失败策略、持久化格式或对外接口。

- 2026-08-20：单条历史记录的发送视图改用当前成员范围与历史可信关系的交集；历史关系和旧发送事实继续保留，当前范围不可用时查询失败。该修改补齐既有当前成员范围规则，不改变分层或所有权。

- 2026-08-20：修复 `1.1.0-rc.3` 收敛状态升级到 V3 时因领域状态新增字段而被误报损坏的问题。V2 改为按该发布版的固定字段顺序读取并显式补齐新增分页暂存，V3 也使用独立固定结构，不再直接序列化可演进领域状态；迁移继续先保留源密文、旁写并复读核验后才切换，失败和重启均可从原记录重试。已安装问题版本但未彻底重置资料的用户升级修复版后可自动恢复。不改变对外接口。
- 2026-08-21：同步 `CONTEXT.md` 中普通空间重置的领域定义，使其与 ADR-024 已实施的设备管理重置语义一致；仅补齐领域词表，无新增架构变化。

- 2026-08-20：单条历史记录的发送视图改用当前成员范围与历史可信关系的交集；历史关系和旧发送事实继续保留，当前范围不可用时查询失败。该修改补齐既有当前成员范围规则，不改变分层或所有权。

- 2026-08-20：完成 ADR-024 原子切换修订。`ResetSpace` 在旧空间停止后复制一致资料到独立目标世代，全部设备关系和安全状态只在该世代重建；活动清单替换是唯一生效点。生效前失败时旧空间保持完整并可重新打开，生效后重启或重复调用只继续同一个新空间和可重试清理。重置记录加密保存，旧世代和受管临时文件由收尾阶段按明确归属清理；复制前检查可用空间。长期持久 profile、失败重试、重复调用、重启恢复、历史保留和清理记录均有回归验证。

- 2026-08-20：实施 ADR-024。`ResetSpace` 现为用户明确触发的设备管理重置：先停止旧空间运行，再保留本机历史、文件、一般设置、身份和解锁能力，原子清除准入记录与全部旧设备关系，重封装资料并建立只含本机的新空间。目标在开始前保存，中断、重启和重复调用继续同一次重置；成功后恢复新空间运行并持久提示全部设备重新配对。公开名称、输入和成功结果不变，远端设备不被远程清空；实体设备与产品确认界面仍需在各产品仓验收。

- 2026-08-20：全新安装首次启动确认没有既有设置后立即保存当前应用版本，避免完成设置后的第二次启动因版本游标仍为空而被误判为旧资料升级。既有资料缺少版本游标时仍进入旧资料独立化，并且只有独立化完整成功后才推进版本游标，失败时保留重试能力。不改变对外接口或持久化格式。

- 2026-08-20：旧资料升级隔离成功后立即推进应用版本游标，并在当前运行单元内标记为已执行；重新配对清除提醒并触发会话重建时，新的运行单元据此不得再次删除新成员。隔离失败不会推进游标，仍保留重试能力；重新配对提醒继续由独立的设置状态持久化，不因版本游标推进而提前消失。避免刚完成的重新配对在会话切换后被旧升级动作撤销。不改变对外接口或持久化格式。

- 2026-08-19：修正旧版资料升级后的重新配对起点。设备迁移到新的独立空间时会移除旧空间的远端成员、清除旧恢复状态，并由本机直接建立新空间的唯一初始记录，不再复用多人旧空间的创建者选举；版本记录先更新而独立化尚未完成时，后续解锁仍以已保存的目标继续恢复。若保护组已建立但成员记录尚未完成，后续会话恢复只补做成员清理和记录重建，不重复创建保护组，并以旧成员历史为条件原子替换为新空间的单成员历史。空间切换写入成员关系时沿用当前资料身份，确保非默认资料重启后仍能解密；切换完成时以已核对并已保存的新成员历史为条件，同时提交完成状态，不再用加入前的旧历史要求已更新的资料库仍保持旧值，避免将成功的空间切换误报为另一次加入正在进行。配对基线拒绝会记录不含用户内容的原因分类，便于核对恢复是否完整。避免旧空间成员历史阻断用户按提示重新配对。不改变对外接口或持久化格式。

- 2026-08-19：修复普通旧空间安全提升被误当作旧成员重新接纳的问题。首次加入共同保护状态后直接进入成员历史交接和在线恢复；只有持久记录明确处于等待重新加入的设备才发送并要求恢复确认。同步校准升级消息新增类型字段后的固定测试基线。负责人、公开入口和普通成员范围不变。
- 2026-08-19：空间加入前的成员历史核对保持逐台执行，但所有设备共用 10 秒总等待时间；时间耗尽后直接继续准入，离线设备数量不再线性延长切换等待。负责人、公开入口、成员历史来源和恢复边界不变。
- 2026-08-19：修复空间加入完成后切换会话时的 mDNS 后台任务泄漏。受控的发现组件在目标已停止时终止延迟清理任务，不再按 10 毫秒持续重试；Engine 仍通过原有生命周期统一启动和停止发现服务，宿主无需新增取消、重启或重试步骤。
- 2026-08-19：删除已被 ADR-023 取代的旧配对自动恢复实现，包括旧设备探测、旧升级网络通道、等待重新接纳、启动重试和旧迁移查询。已发布数据库中的旧待恢复表保留迁移历史，但升级时一次性清空；当前运行流程只负责本机独立化和通知产品全部重新配对。
- 2026-08-19：ADR-023 取代旧资料自动恢复方案。`0.20.0` 之前的资料在解锁后由 Engine 一次性清除旧成员关系和旧恢复状态，保留本地内容与解锁材料后建立单设备新空间，并保存需要重新配对标记；完成后以带 `all_devices` 范围的事件通知产品立即展示重新配对引导，保存标记继续作为漏事件后的恢复依据，产品不参与清理或旧关系恢复。
- 2026-08-18：旧空间升级的等待状态改为双方完成恢复后才结束。发起方保留等待记录并在重启后主动请求旧设备补齐成员资料；旧设备完成资料恢复后回传确认，发起方收到确认才结束等待。保护关系已建立不再单独视为恢复完成，避免中途退出后留下无法继续配对的半完成状态。
- 2026-08-18：修复启动时成员范围暂不可读会拖延已接受设备自动恢复的问题。活动 Space 的连接运行期现在
  先立即检查当前范围；资料可用的统一会话事件会直接唤醒连接恢复，真实网络失败仍按原有退避处理。成员
  资格继续只由已接受历史决定，设备页面的手动刷新保留为补充动作而非恢复条件。
- 2026-08-18：修复规格 025 的旧加入远端清理闭环。新的明确加入立即发送自己的请求，历史清理由后台使用
  各自原邀请方地址独立处理；邀请方保存取消并返回可重复验证的拒绝结果，加入方验证后才清除待办。正式
  启动恢复改用真实配对发送和远端确认，删除永久延期占位；连续失败且邀请方不同的历史清理仍各自隔离，
  升级前已保存的加密加入记录继续可读。邀请方成功发出确认后收拢活动记录，后续重复清理仍从精简记录
  重放同一结果，避免连续失败长期堆积未完成记录。后台恢复可以被暂停或关闭立即中断，尚未确认的清理
  保留密文待办并在下次恢复继续，不能拖住锁定、切换或关闭流程。
- 2026-08-18：稳定活动剪贴板拉取的拒绝场景测试。对端可在连接建立后的任一通信阶段拒绝非成员或
  已失去准入资格的成员，测试统一接受这些等价的无内容结果；生产行为和架构不变。
- 2026-08-18：补强规格 025 的失败关闭条件。旧加入缺少有效首次请求，或 Candidate 阶段资料及身份绑定不完整时，
  不再允许进入取代事务；首次请求已确认送达但仍未进入 Prepared 时仍可取代，旧清理继续关联该请求。
  同时补齐全部禁止状态、三种空间切换边界、邀请消费不可改绑、旧清理确认隔离和 SQLite 主文件及其两个
  辅助文件的密文检查。旧终态压缩后收到 Candidate、Commit 或 Complete，正式入口仍能识别其为被取代记录
  并稳定停止。负责人、公开入口、持久化格式和三端结果不变。
- 2026-08-18：统一 UniFFI 公共契约测试的 Engine 关闭等待上限为 30 秒。连续运行完整契约测试时，15 秒
  仍会在不同用例中误报正常网络关闭失败；只调整测试期限，不改变生产关闭行为。
- 2026-08-18：修复宿主剪贴板变化正在后台发送时可能阻塞 Engine 关闭的问题；该后台动作现在与普通操作
  使用同一会话关闭信号，关闭后不再继续占用旧会话。仅修正运行期收尾，不改变剪贴板保存和发送规则。
- 2026-08-18：完成规格 025。每次用户明确 JoinSpace 都创建全新加入；旧本机加入仅在 Prepared 前由
  `WorkspaceConvergence` 原子保存为 SupersededByNewJoin 并建立新尝试，Prepared 及以后返回稳定的
  1295 冲突并继续恢复旧加入。正式消息入口先把迟到 Candidate、Commit 和 Complete 记录到旧尝试，再停止
  推进；旧清理、并发调用和重启恢复也按加入尝试隔离，三端绑定保持同一公开结果。相关筛选测试与完整
  工作区测试均通过；Android 双端实体设备验收因当前仅连接一台设备而跳过，
  iOS、HarmonyOS 实体设备及额外网络矩阵也标记为跳过。
- 2026-08-18：新增规格 025，将 ADR-022 拆成六个可验证阶段：先固定用户新动作与自动恢复的边界，
  再跑通 Initiated 原子取代，补齐 Candidate/Prepared、迟到消息与恢复、三端错误契约，最后完成故障矩阵和
  Android 双端实体设备验收。当前只完成实施设计，生产行为和实体设备结果仍待后续实现与验证。
- 2026-08-18：采纳 ADR-022。每次用户明确 JoinSpace 创建全新加入；断线、重启和后台恢复继续原加入。
  旧本机加入只在 Prepared 尚未持久保存时可由负责人原子保存为 SupersededByNewJoin 并创建新尝试；
  Prepared 及以后返回专用不可取代冲突并继续向前恢复。该决定保留邀请一次性消费、当前 Space 和全部
  本机资料，调用方不编排取消或重置。当前仅完成决策和权威文档修订，生产实现与实体设备复验待后续完成。
- 2026-08-18：完成规格 024 的成员收敛内部职责整理。`WorkspaceConvergence` 继续是唯一完整负责人，主文件只保留负责人定义、公开类型、共享状态保存和必要协调；准入、成员关系、状态投影和连接维护分别归入对应领域目录，旧平铺路径和单一大测试文件已删除。测试随完整工作归位，仅无业务含义的公共夹具留在受限测试支持位置；公开行为、持久化、网络格式和运行顺序不变。
- 2026-08-17：采纳 ADR-021，明确成员收敛内部按准入与完成恢复、成员历史核对、移除与用户决定、当前成员范围与状态查询、旧数据兼容与启动恢复划分职责。`WorkspaceConvergence` 继续是唯一完整负责人；主文件只保留稳定入口和必要协调，调用方不获得逐步流程。该决定指导后续整理，不改变当前运行行为。
- 2026-08-17：新增并修订规格 024，将 ADR-021 细化为可逐项执行的成员收敛内部整理顺序、领域子模块目录、并发与重启边界及验证要求。加入、成员关系、状态投影和连接维护分别收口，避免拆成平铺同级文件；该规格不改变公开动作、成员规则或持久化格式，仅定义后续实现如何保持唯一负责人并删除旧位置。
- 2026-08-17：将规格 023 接入最新主线时，保留旧升级后已移除设备不得重新获得普通资格的修正，并删除一项仍依赖已退役成员历史入口的重复测试；主线宿主验收同步核对新建空间后的本机信任记录，以及移除本机与不存在设备的稳定失败分类。现行历史范围和真实加密重启验收继续覆盖同一规则，无架构变化。
- 2026-08-16：修复旧升级标记残留导致已移除设备重新出现在普通列表，并同步修订 `CONTEXT.md` 的旧空间安全提升定义。只要已应用成员历史存在，普通范围立即且永久以该历史为准，旧迁移标记在启动时自动清理；未完成的旧空间安全提升只处理持久记录中明确等待重新接纳的设备，不读取成员资料表扩大候选。历史资料继续保留，公开操作不变。验收覆盖真实加密旧状态的首次启动、自动修正和再次启动，并确认旧成员资料仍在但不再恢复普通资格。
- 2026-08-17：同步规格 023 完成后的领域词表。成员候选、历史验证凭据和成员准入激活不再标记为待实施；补充空间加入的三类稳定结果、提交前取消、可恢复空间切换、普通重置门禁和彻底重置的本机销毁边界。仅修正文档与当前行为的一致性。
- 2026-08-17：校准 HarmonyOS 绑定的依赖边界检查，把设备信任结果转换已经使用的 JSON 库纳入明确允许清单；内部模块仍禁止被绑定直接依赖，不改变运行行为。
- 2026-08-17：修正移动绑定生命周期验收。后台销毁期限从启动完成后单独计时，避免把初始化耗时误判为关闭泄漏；彻底重置后旧运行实例保持失效，宿主重建实例后再确认空间和设备资料为空。仅校准既有生命周期与彻底重置约定，不改变运行行为。
- 2026-08-17：规格 023 的状态更新为仓库内实现完成。正式流程、自动化和仓库规则检查已通过；缺少官方旧密文样本、问题基线旧程序及实体移动设备的项目继续保留为待补验收，因此不宣称全部外部兼容与设备验收完成。
- 2026-08-17：完成规格 023 的仓库内故障边界验收。正式提交中的完成接力路由最多保存 256 条，本机生成和网络接收均拒绝超限资料；成员历史的激活回执按同一 256 条边界分页，并在验证后继事件前先应用其依赖回执。乱序到达的正式提交、应用回执和完成消息不会修改已保存阶段，双方在每个持久阶段重启后均从同一磁盘记录继续。官方历史密文样本、问题基线旧程序和实体移动设备因本机缺少验收条件保持跳过，不计为通过。
- 2026-08-17：完成规格 023 的第三成员准入完成接力。原接纳设备在新设备已保存正式提交和应用回执后离线时，另一台合格当前成员可通过独立受限通道接手；正式提交只携带接力所需的成员标识、设备标识和网络路由资料，不重复携带设备名称等资料。三方身份、双方网络身份、单调挑战和完整加入材料均受签名保护。帮助设备必须证明加入前后资格、保存同一事件和回执并实际应用自己的安全更新，结果完全一致后才返回完成；重启复用同一加密记录，新设备在此之前保持等待。原接纳设备在正式提交前离线仍不能由第三方猜测结果。
- 2026-08-17：补齐规格 023 的协议和单调计数边界。配对与成员历史单帧在分配内存前执行 4 MiB 限制；未知事件、决定、回执、签名算法和准入仓储元数据版本统一要求升级或失败关闭；设备信任版本在同一 profile 内跨 Space 递增，过期写入、计数溢出和不可能的序号组合不会留下半份状态。历史分页和第三成员完成接力仍由后续阶段实现。

- 2026-08-17：删除成员历史 `/1` 的 Hello、分页、决定和确认成功路径，正式运行只接受 `/2` 完整版本化历史。同一 Space 重新加入时，邀请方历史可以比加入方更新，但必须完整包含加入方已确认的连续前缀；缺失、倒退或分叉仍拒绝。决定按作者合并，发送方只能新增自己签署的决定，接收方已有的其他决定不会丢失。五设备连续删除和重新加入场景已验证完整历史、安全状态、内容密钥和最终关闭。

- 2026-08-17：完成规格 023 的生产加入闭环。创建 Space 只有在单设备保护状态和可信成员起点都保存后才报告成功，因此可立即邀请第一台新设备。新设备先验证并保存完整成员历史，再通过 Candidate、Prepared、Commit、Applied、Complete 完成双方持久保存；加入方激活后先归档可查询结果，再发送可重建 CompleteAck，跨空间切换和发送失败均由 Engine 在启动时继续。同一 Space 的邀请方历史必须完整包含加入方已确认的连续历史；跨 Space 时来源历史只负责迁移与清理，目标完整历史独立验证并暂存。旧 Confirm、Ready、AdmissionSaved 和独立 SwitchSpace 产品路线已删除。跨空间来源历史在联网前检查：未确认的不可读密文返回稳定冲突，确认后原密文被隔离保留并计数。普通 ResetSpace、FactoryResetSpace、取消和待激活移除继续由同一 profile 负责人裁决。

- 2026-08-16：规格 023 的首条加入请求携带并校验同一份已保存成员实例、历史验证凭据、续接公钥和加入前签名事实；加入方从 J0 私有状态生成签名，不提前启用目标安全组。发起方必须先由 `WorkspaceConvergence` 验证请求结构和成员签名，之后才允许取走或远端核销邀请；无效请求不会消耗邀请。该日只交付请求边界，完整生产闭环见 2026-08-17 记录。
- 2026-08-16：规格 023 的本机加入准备接入生产握手。加入方在首次请求发送前必须由 `WorkspaceConvergence` 原子保存或恢复同一尝试编号、公开加入编号、请求编号、成员实例、KeyPackage、私有加入状态和续接密钥；配对层不再自行生成编号或直接准备第二套成员材料。设备显示名和当前传输地址不参与稳定尝试绑定，因此变化后重试仍续接原尝试。该日只交付本机准备边界，完整生产闭环见 2026-08-17 记录。

- 2026-08-16：接入规格 023 的 profile 常驻产品边界和可恢复彻底重置。JoinSpace、CancelJoinSpace 与设备信任快照使用统一 Active、Pending、Rejected 投影；普通 ResetSpace 在任何未收束加入工作存在时保持零副作用，静止时只隐藏公开结果并清设置和未消费邀请。FactoryResetSpace 停止旧运行后先删并确认密钥，再清数据库、配置导入暂存、空间世代和受管文件；启动可续完中断清理，旧 Engine 永久失效，宿主重新创建后进入全新世代。该日只交付产品与重置边界，完整生产闭环见 2026-08-17 记录。
- 2026-08-16：完成规格 023 的统一当前成员激活门禁。所有普通流程继续只读取一个完整成员范围；V2 创世和迁移基线直接生效，普通新增必须具有历史内永久激活回执，加入方还必须完成本机激活。跨空间最终切换开始前继续使用来源空间；提交后被移除的加入保留目标历史事实并恢复来源空间；无关、锁定或损坏的 V2 历史关闭普通流程，不回退旧成员表。旧空间仅在明确不存在 V2 历史时保留原兼容路线。

- 2026-08-16：完成规格 023 的本机 J3 世代落地。加入方持久状态明确区分 Fresh、Same-Space 和 Cross-Space；三种路径都验证并安装完整成员历史、安全状态、内容钥匙目录和成员关系，Fresh 创建首个活动世代，Same-Space 保留原 MasterKey 与内容密文，Cross-Space 以所有业务表提交共同推进的数据库修订固定最终来源快照并重封装内容。修订机制排除 Diesel 迁移元数据，并在降级时对称移除当前迁移点的全部修订触发器。目标数据库、受管文件目录和内存会话只随加密活动清单提升，设置查询在清单存在后只投影该清单；重启从同一持久阶段继续。现有配对 wire、profile 常驻产品入口和 Active/Pending/Rejected 公开结果仍待后续阶段接入，不得把本记录描述为新设备加入已正式上线。
- 2026-08-16：规格 023 的目标空间本地访问材料改为先准备、后启用。准备阶段只生成并返回由准入记录加密保存的不透明材料，不写 keyslot、安全存储或内存会话；启用阶段重新核对目标 Space、profile、KEK、MasterKey 和 keyslot 的完整绑定，保存原状态后才替换目标钥匙并切换会话，失败恢复原空间。该内部启用能力尚未接入生产跨 Space 数据世代和活动清单，生产路径继续失败关闭。
- 2026-08-16：规格 023 的旧空间切换恢复开始收口到 profile 级 `WorkspaceConvergence`。旧状态恢复不再由静默解锁和手动解锁分别触发；基础设施使用私有旧格式读取器核对孤立备份、Prepared、HandshakeDone、Swapped 和损坏或矛盾状态。证据不足时保留状态文件、备份和临时密钥并进入恢复要求；证据完整时才向前重封装、核对主记录、切换 setup 并清理旧工件。公开旧迁移阶段、进度查询和新建旧式迁移仍待新版跨 Space 数据世代接替后删除，因此本记录不代表旧路径已经完全退出生产。
- 2026-08-16：开始把规格 023 的跨 Space 切换并入同一次准入恢复。加入方在历史和目标安全结果核对后保存与本次加入绑定的目标暂存记录；收到最终消息时先保存事实，再逐阶段保存切换进度，只有目标恢复和清理完成后才保存成功。启动扫描可从同一记录继续，长期结果保留迁移数量和确认重建材料；保存层拒绝错绑、跳阶段和终态替换。当前生产装配在完整目标密钥、完整业务数据世代和活动清单能力接入前明确拒绝该路径，旧 Space 切换仍未删除，因此本记录不代表跨 Space 加入已经可用。
- 2026-08-16：统一真实 Engine 宿主契约测试的关闭期限，使局域网能力拒绝测试允许底层网络完成已有的有界连接排空；测试断言和生产关闭行为不变，无架构变化。
- 2026-08-16：继续实施规格 023 的应用准入事务。候选时固定并加密保存基础成员历史，正式提交以当前历史未变化为原子条件；即使历史恰在保存瞬间变化，也永久保存明确拒绝且不覆盖新历史。待激活设备移除由同一负责人裁决：提交前无加入事件，提交后永久保留加入再移除，完成后转交普通成员移除；并发测试确认不会留下半状态。拒绝原因在双方永久保存；邀请回收、既有成员安全更新和历史/回执批次分别等待各自的精确持久确认，重启恢复会实际处理待发送消息和终态清理。终态记录只能追加接收确认并把原待确认消息单向标记为已确认，不能重新激活、替换或删除既有投递事实。应用层新增当前 Space 唯一入站候选投影，并以只向前阶段编排彻底重置，可从删密钥或清状态中断点继续且不依赖活动 AppFacade。数据库故障验证尝试与成员历史共同回滚。真实准入网络、profile 常驻入口、彻底重置的生产清理能力和产品公开结果仍待后续接入，不得视为新设备加入已经可用。
- 2026-08-16：完成规格 023 应用准入事务的首条持久闭环。生产装配使用 profile 级加密准入仓储；应用负责人覆盖本机开始加入、发起方候选、加入方历史核对后暂存、正式提交、双方应用、完成确认、提交前取消、提交后取消、重复消息重建、启动恢复扫描和终态压缩。现有配对网络协议、真实 V2 历史与安全适配器调用、公开结果和无活动 Space 常驻入口仍待后续阶段接入，不得把本记录作为产品加入完成证据。
- 2026-08-16：修正配对与在线探测共存测试误查剪贴板协议的问题；测试现在核对实际安装的在线探测协议，生产运行和架构边界不变，无架构变化。
- 2026-08-16：完成规格 023 的加密保存基础。新增 profile 级准入元数据、三类尝试状态、唯一准入槽、原子待发送消息、邀请与加入编号防重放、重启扫描和终态压缩；每次尝试使用独立临时密钥，最终结果先改由长期 profile 密钥保护再移除临时资料。安全状态的准备、暂存、重开核对、启用和放弃统一由一个适配器负责；工作空间 V2 到 V3 的旁路写入增加启动清理；彻底重置的三阶段生命周期标记可跨重启继续。完整准入事务、跨 Space 切换、统一权限门禁、网络协议和公开结果仍待后续阶段接入。
- 2026-08-16：完成规格 023 的核心历史基线。新增版本化事件与决定、永久成员凭据、精确父历史授权、永久激活回执、V1 原证据保留、规范迁移检查点和独立成员证明、FullyVerified/LegacyAccepted 激活基线及公共安全承诺；核心入口不依赖网络、持久化或当前安全成员树。生产持久化、准入事务、恢复、统一门禁和三端公开结果仍待后续阶段接入。
- 2026-08-16：新增规格 023，定义成员历史永久验证凭据、按精确父历史授权、先准备后正式提交的准入激活、历史内永久激活回执、只向前恢复和 V1 诚实迁移；正式状态扩展现有设备信任快照并复用现有成员移除入口，不把诊断收敛接口提升为产品契约；补齐无活动 Space 时的 profile 级常驻负责人、长期准入加密根、入站/出站互斥、终态重封装、提交前取消边界、多消息待确认集合、V3 槽与启用指针、普通重置和彻底重置的唯一边界；彻底重置纳入搜索与受管文件本体清理；同步清理规格 017 中已失效的 OpenRaft 多数方案，并纠正 ADR-017“职责已完全收口”的过度表述。当前仅完成设计文档，运行实现、自动化和设备验收均待完成。
- 2026-08-15：清理发送门禁测试构造入口、加入握手中未参与流程的重复数据和引擎装配中未读取的重复依赖；现有发送判断、加入安装、安全状态和成员历史协议不变，无架构变化。
- 2026-08-15：同步 `CONTEXT.md`、规格 022 和 ADR-020 的旧空间分批安全提升规则：当前历史起点建立后普通范围不回退旧成员资料，但持久升级记录中的保留成员仍可跨重启完成同一次共同保护和历史接纳；两者都覆盖全部保留成员后才结束提升。本次只消除文档与已实施行为的矛盾，不改变运行架构。
- 2026-08-14：文件发送成功日志只保留条目标识、大小、是否复用和耗时，不再记录来源文件名或路径；文件发布职责和对外行为不变，无架构变化。
- 2026-08-14：修复三设备分批升级时加入端可能创建竞争历史起点。加入端完成共同保护关系后只保存本机当前身份，并从已认证接纳端取得唯一成员历史；即使加入端设备编号最小，也不得在拉取前自行建立起点。成员历史继续只有一个来源。
- 2026-08-14：补齐当前成员历史确认的双向保存。首次向未知成员发送完整签名历史后，只有对端明确回复历史一致或更新已应用，本机才把未知或待升级关系保存为一致；待决定、分叉和无效结果不会被成功确认覆盖。双方因此使用同一确认事实恢复普通同步。
- 2026-08-14：修复旧空间分批升级时过早结束迁移。稳定发起端建立只含本机的历史起点后，旧成员范围继续负责同一次安全提升；只有全部保留成员都提交签名身份并进入已应用历史后，双方才清除迁移标记并永久切换到当前成员历史。旧成员表不授予普通同步资格，也不形成第二份长期成员事实。
- 2026-08-14：修复 v0.19.1 设置中的 `auto_sync` 在升级后被默认值覆盖。当前设置模型读取旧字段名并映射到 `auto_sync_enabled`，`sync_enabled` 继续使用新版本默认值；保存时只写当前字段，不保留平行实现。无架构变化。
- 2026-08-14：修复旧安全升级首次启动中断后的恢复。共同保护状态已经写入但成员历史尚未建立时，持有未完成升级记录的创建方在重启后继续完成历史起点，即使它不是编号最小的设备；不增加产品端补偿。
- 2026-08-14：修复旧安全升级完成后的连接恢复。加入端安装共同保护状态并取得完整成员历史后，立即重新连接接纳端，清除升级期间留下的离线结果；暂时失败仍由既有后台恢复继续处理，产品端不增加延时或重试补丁。
- 2026-08-14：修正旧安全升级加入端的成员历史拉取方式。加入端安装共同保护状态后，使用已认证接纳端主动取得完整当前成员历史，再进入普通成员核对；避免空历史加入端只返回空响应后永久缺少成员资格。此修正落实既有交接规则，不改变架构边界。
- 2026-08-14：修正 v0.19.1 缺少收敛状态行时的迁移来源。Engine 读取档案已有的应用版本记录，只有确认从更早版本升级且状态行不存在时才建立持久旧升级标记；已有状态优先，全新安装、同版本启动、回退和无效版本字段均不开启旧成员范围，版本文件不可读或损坏时停止本次启动。
- 2026-08-14：补齐旧安全升级完成后的当前成员历史交接。接纳端先准备本机当前身份，加入端安装共同保护状态后立即向接纳端交换成员历史，避免双方因普通成员范围尚为空而互相等待；普通列表和内容资格仍只来自已应用历史。
- 2026-08-14：修正当前成员范围架构检查对格式化换行的误报；检查仍要求所有指定消费者读取完整快照，不改变运行架构。
- 2026-08-14：修复旧空间安全提升与当前成员历史建立的时序冲突。当前历史起点暂时只有本机时，已知旧成员可凭持久等待重新加入记录完成同一次保护状态接纳；该记录不授予普通成员资格，未知、已移除及没有等待记录的设备仍关闭式拒绝。
- 2026-08-14：修复三设备分批升级时成员历史先于共同保护关系到达的完成时序。只有当前签名历史和共同保护关系都覆盖保留成员后才结束旧版升级阶段；历史先到时继续等待安全接纳，避免成员显示有效但同步因安全关系不完整而暂停。
- 2026-08-14：修复 v0.19.x 远端成员表在旧空间安全提升已经开始后误判本机已移除。当前成员范围查询始终把本机加入空间保护状态核对；旧成员表只提供远端候选，本机资格由实际保护状态决定，不要求旧表保存本机副本。没有当前历史时，只有明确处于等待原成员重新加入的旧升级记录可以继续使用旧成员范围；普通就绪状态和恢复失败状态仍关闭式拒绝。
- 2026-08-14：修复分批升级时先完成安全提升的设备拒绝后升级成员。旧升级接纳统一以当前成员运行范围和本机有效成员资格授权；本机建立当前历史后仍可把范围内旧成员接入同一保护状态，但不回退旧成员表，已移除和未知设备仍被拒绝。
- 2026-08-14：修复 v0.19.x 旧成员表只保存其他设备时的升级判断。明确旧保护状态与已恢复空间会话证明本机资格，旧成员表只提供其他设备；不再因旧表缺少本机重复记录而把本机误判为已移除或创建空的保护组。当前签名成员历史的判断不变。旧版接纳失败额外记录固定阶段、可重试性和脱敏错误种类，不记录请求资料。
- 2026-08-14：修复旧工作空间升级后的设备信任读取。加密收敛记录增加明确格式标记，原地迁移移动端已使用的无标记成员历史布局和 ADR-020 前变化链布局；保留旧空间、设备和安全资料，不补造无法证明的签名历史，设备继续走既有共同升级流程。迁移后立即重写并跨重启使用当前格式；无法识别的数据使用独立稳定错误。新建空间尚无签名成员历史时，设备信任复用既有当前成员范围结论，不另行猜测成员关系。
- 2026-08-14：实施规格 022。普通名单、连接、升级、内容、活动状态和手动重发统一使用当前成员运行范围；旧设备隐藏入口已删除。成员事件后续处理和受限决定投递均保存加密进度并在启动后恢复，新增成员只在整批处理完成后进入普通范围，已接受移除立即退出。已完成自动化验证，真实双 profile 验收仍待执行。
- 2026-08-12：新增当前 SQLite 数据库关系图，依据完整迁移后的实际结构展示剪贴板、接收与文件传输、搜索、设备关系和空间安全状态，并单独标明只用于旧数据迁移的保留表；仅增加解释材料，不改变现有架构决策或运行行为。
- 2026-08-12：ADR-020 主动核对统一先交换轻量摘要，一致时不发送完整历史，存在差异时才按需补齐有界记录；同一对端的版本识别和当前核对串行执行。明确拒绝当前入口不会触发旧入口探测，拒绝移除只回传事件发起方并只隔离对应关系。版本探测改用 `0.19` 已存在的旧内容入口空连接；本次 `1.1` 不监听该入口，普通内容发送也不回退该入口。关闭流程可打断不可达设备拨号和未结束的成员公告后台任务，并在操作忽略取消时保持有界。当前核心、应用、加密落盘和多设备场景已有验证；历史提交可证明 `0.19` 入口，但当前仓库对象缺少旧清单引用的后台程序目录，发布页也无 `0.19` 可执行包，真实混合版本运行仍未完成，不视为 ADR-020 整体交付。
- 2026-08-12：新增 ADR-020 可视化讲解页，用决策图和测试场景图说明完整负责人、设备上线核对、移除决定、关系级分叉范围及多设备验收边界；仅增加解释材料，不改变现有架构决策或运行行为。
- 2026-08-12：补齐 ADR-020 已保存成员历史的决定边界。远端移除到达时只有新进入本机已应用分支的条目才能改变安全状态；待用户决定的移除及其后续条目只保存，不提前生效，用户接受后才按原顺序应用。对端已经分叉时拒绝其旧 Space 成员历史问候、请求和资料响应，但仍接受用于确认的决定。移除发起方按移除发生前的已确认成员关系和签名核验对端决定，并与本机决定分开保存，避免被移除设备因已从当前成员集合消失而无法回传接受或拒绝结果。
- 2026-08-12：完成 ADR-020 的混合版本内容暂停落地。低于 `1.1` 的已确认对端仍保留设备可见、Space 和成员资料，但统一暂停双方普通内容、当前剪贴板的上线补发和恢复补发；旧设备主动拉取当前内容时只收到不可用结果，不读取或发送内容。对端完成本次 `1.1` 确认后自动清除暂停并回到当前成员核对流程。未知、离线、网络和身份失败不写入升级提示。
- 2026-08-12：收窄 ADR-020 的版本边界：只区分低于 `1.1` 的设备和本次 `1.1` 设备。当前成员核对与内容同步流程只服务于 `1.1` 设备之间；低于 `1.1` 的对端保留可发现、Space 和成员资料，但双方普通内容同步暂停。负责人识别后加密保存可跨重启恢复的“需要升级”状态，产品只在 `1.1` 一侧提示；对端升级到 `1.1` 并重新确认后自动恢复同步。未知版本、网络和身份问题不显示为需要升级，也不存在旧版本继续同步内容的备用流程。该记录更新目标架构，运行实现和验收仍待完成。
- 2026-08-12：成员多设备验收的每个场景现在都在测试前用中文列出设备顺序、关键决定和最终要证明的结果，便于从场景本身理解覆盖边界；仅补充测试说明，不改变运行架构。
- 2026-08-12：发起方离线后的成员验收改用分阶段公开状态报告，区分首次恢复和双方重启后的连接恢复；不输出内容或私密资料。仅更新验收，不改变运行架构。
- 2026-08-12：四设备接力验收在 C 未观察到新加入设备或 A 恢复后未恢复所需双向可用联系时，报告两端公开成员状态和观测数量，区分成员事实未保存与连接刷新未完成；不输出内容或私密资料。仅更新验收，不改变运行架构。
- 2026-08-12：四设备接力加入验收改为在每次下一台设备离线前，确认下一位发起方和加入方都已保存完整成员记录；后续恢复只建立在已接收的状态上，不以发起端返回替代接收端事实。仅更新验收，不改变运行架构。
- 2026-08-12：修正 ADR-020 多设备验收仍沿用自动移除的前提：离线设备不再被用作加入发起方，收到远端移除的保留成员必须先明确作出接受或拒绝决定，才验证其所在分支的后续状态；仅更新验收，不改变运行架构。
- 2026-08-12：补充 ADR-020 长期离线验收在旧状态接入被拒绝后的分阶段状态诊断；验收以移除发起方 C、旧状态设备 A 和已确认移除的新分支设备 D 构成最小链路，只要求所需状态一致，不把无关设备或即时直连当作前提；同一 Space 内只有发起方成员历史更旧时才拒绝加入，避免旧状态覆盖已确认分支，同时保留被移除设备以新身份重新加入的路径；超时时仅报告公开收敛状态，不记录业务内容。

| 日期 | 修改范围 | 架构结论 |
| --- | --- | --- |
| 2026-08-21 | Engine v1.1.0-rc.5 发布准备 | 同步工作区与 HarmonyOS 宿主版本，发布历史投递范围、工作区状态恢复和设备关系重置修复；无新增架构变化。 |
| 2026-08-20 | Engine v1.1.0-rc.4 发布准备 | 同步工作区与 HarmonyOS 宿主版本，发布旧资料独立化、重新配对恢复和运行期修复；无新增架构变化。 |
| 2026-08-16 | 旧升级残留设备的当前范围修复 | 已应用成员历史成为普通成员资格的即时唯一来源；旧迁移标记只用于建立首条当前历史并在此后自动清理，未完成的安全提升由原有持久待重新接纳记录独立继续。 |
| 2026-08-16 | Engine v1.1.0-rc.3 发布准备 | 同步工作区与 HarmonyOS 宿主版本，发布旧版升级后残留已移除设备修复和无用接线清理；无新增架构变化。 |
| 2026-08-17 | ADR-023 成员历史分页恢复 | `WorkspaceConvergence` 负责按来源设备加密保存未完成页面、断点续传、重复和乱序处理，并只在全部页面完成整体验证后替换正式成员历史；每页同时受 256 条记录和 4 MiB 实际传输大小限制，单条记录无法放入时直接失败；网络层复用同一上限且只传输单个页面，不保留整份历史一次发送的第二条路径。 |
| 2026-08-15 | Engine v1.1.0-rc.2 发布准备 | 同步工作区与 HarmonyOS 宿主版本，发布 v0.19.1 原地升级恢复、分批成员安全提升、旧设置兼容和日志脱敏修复；无新增架构变化。 |
| 2026-08-14 | 旧版后台被拒绝后的空目录清理 | Engine 启动组装只在当前目录已经存在、且唯一旧版 profile 目录可确认为空时删除该空目录并继续；多个旧目录、任何非空目录或无法确认的目录仍停止启动，不覆盖或删除身份及缓存资料。仅修正升级兼容检查，无架构变化。 |
| 2026-08-14 | 旧安全升级加入端历史起点修复 | 加入端安装共同保护状态后只登记本机身份，并从已认证接纳端拉取唯一当前成员历史；设备编号顺序不得产生第二个起点。 |
| 2026-08-14 | 当前成员历史成功确认 | 未知成员首次接收完整签名历史后，以其明确成功回复保存本机一致关系；非成功回复不改变既有安全关系，双方确认事实不再单边缺失。 |
| 2026-08-14 | 旧空间分批升级完成边界 | 只含本机的历史起点不再提前结束迁移；旧成员范围仅维持同一次安全提升，共同保护状态和已应用历史都覆盖全部保留成员后由负责人一次性切换到唯一当前成员范围。 |
| 2026-08-14 | 旧安全升级加入端主动拉取历史 | 加入端通过已认证接纳端完成首次当前成员历史交接，再进入普通成员核对；仅修正实现与既有架构不一致的问题。 |
| 2026-08-14 | v0.19.1 缺少收敛状态行时的迁移来源 | 只在已有版本记录确认从更早版本升级且状态行不存在时建立持久迁移来源；已有状态优先，全新安装、同版本启动、回退和无效版本字段均不开启旧成员范围，版本文件不可读或损坏时停止本次启动。 |
| 2026-08-14 | 旧安全升级成员历史交接 | 已认证旧升级流程负责完成保护状态后的首次当前历史交换；普通成员范围不承担这次自举，也不提前接纳尚未应用的成员。 |
| 2026-08-14 | 当前成员范围架构检查 | 接受格式化后的跨行调用写法，仍检查每个指定消费者使用完整快照；无架构变化。 |
| 2026-08-14 | 旧成员等待重新加入接纳修复 | 持久等待重新加入记录只允许完成已经开始的同一次旧空间安全提升，不改变当前成员运行范围；普通成员资格仍只来自已接受成员历史。 |
| 2026-08-14 | v0.19.x 旧成员范围兼容修复 | 明确旧保护状态与已恢复空间会话证明本机资格，旧成员表只提供其他设备；当前成员历史建立后仍只使用签名历史，不保留平行判断。 |
| 2026-08-14 | 旧工作空间设备信任兼容修复 | 存储适配器只读取已经实际使用的旧加密布局，验证空间归属后原地重写为带格式标记的当前记录。ADR-020 前资料保留旧空间和有效设备事实，并以持久迁移标记继续既有共同升级；共同保护状态和已应用历史都覆盖全部保留成员后清除标记。新建空间尚无签名成员历史时复用已有当前成员范围结论。无法识别的数据以独立错误关闭式失败，不由产品端猜测或修复。 |
| 2026-08-13 | 配对接纳与在线事实一致性修复 | `WorkspaceConvergence` 在开始新准入前核对本机持久成员实例与当前安全身份，不一致时保存恢复状态并拒绝产生成功结果；P2P 在线连接收到远端当前空间明确接纳后才发布在线，被拒绝的连接只保留离线结果。成员、信任与空间保护检查均未放宽。 |
| 2026-08-13 | 设备信任正式入口收口 | 正式 Engine 与三个移动平台删除旧查询、旧决定和旧提醒，只保留完整设备信任查询、稳定决定和失效提醒；旧内部快照仅限诊断构建。完整查询在同一权威状态时点构建，三平台透传同一完整 JSON 结果，不重复计算关系。 |
| 2026-08-13 | 个人设备信任核对产品契约实施 | 规格 021 的非恢复范围已实现：`WorkspaceConvergence` 继续作为唯一完整负责人，统一返回来源、目标、选择影响、逐设备独立事实、直接同步结果和动作限制；稳定决定支持本机退出二次确认、重复与过期结果；Engine、iOS、Android、HarmonyOS 使用同一完整查询、决定和失效提醒。决定仍保存在原加密成员历史中，不新增队列。共同设备组恢复和 DTR-12 仍待产品确认。 |
| 2026-08-13 | Engine 上一版本互通消费者构建筛选 | 发布互通验证逐个采用上一公开版并以完整构建成功作为旧桌面版本的入选条件；失败候选继续向前查找，公开产物不覆盖，产品通知仍以双向内容传输成功为前提。仅修正交付流程，无架构变化。 |
| 2026-08-13 | Engine 发布旧消费者选择回退 | 新旧版本互通验证优先使用明确固定到上一公开 Engine 的桌面历史提交；若上一版本尚未被产品仓采用，则选择最近一个固定到该版本祖先、可由正式采用脚本升级到上一版本的桌面提交。不得因缺少产品采用记录跳过双向互通，也不得用当前桌面主线直接构建旧 Engine。仅修正发布验证，不改变运行架构。 |
| 2026-08-13 | Engine v1.1.0-rc.1 发布准备 | 同步工作区与 HarmonyOS 宿主版本，发布已实施的成员历史核对、本机移除决定、关系级分叉和低版本同步暂停；无新增架构变化。 |
| 2026-08-13 | ADR-020 领域词表同步 | `CONTEXT.md` 删除旧成员移除等待与恢复定义，改为记录当前成员历史核对、本机移除决定、关系级分叉和低于 `1.1` 的升级暂停规则；仅同步已实施架构的统一业务用语。 |
| 2026-08-13 | ADR-020 完整多设备验收与关闭生命周期 | 当前 `1.1` 串行矩阵覆盖三至五台设备的离线恢复、分支决定、两次移除与重新加入、分支内送达和旧状态拒绝，15 项实际运行全部通过；2 项依赖官方 `0.19` 程序和配套资料的本地场景明确跳过。引擎请求登记现在随调用方请求结束自动释放，调用方放弃等待不会再占满关闭期限。ADR-020 状态更新为已实施。 |
| 2026-08-13 | ADR-020 正式双机升级验收与旧空间提升边界 | 以正式 `v0.19.0` 双机资料完成 A 单独升级、重启、双向暂停和 B 原地升级后的双向恢复验收。`1.1` 不保留旧格式内容同步，但必须保留独立的旧空间安全提升入口；它只建立共同保护状态，不生成第二份成员事实。双方升级后由稳定发起端建立唯一历史起点，各设备提交自己签名的当前身份资料，之后只运行当前成员核对与内容流程。 |
| 2026-08-13 | `0.19` 网络身份原地延续 | Iroh 网络身份继续由独立的 `iroh-identity/` 目录持有；运行节点与配置导入读取同一份身份，主安全存储只负责 KEK 等其他安全资料。升级接管目录后必须复用原 32 字节身份，不能生成第二身份，否则旧设备虽能发现新地址也无法把它认作原关系中的设备。完整生产启动测试覆盖首次启动和重启。 |
| 2026-08-13 | `0.19` 带 profile 资料原地接管 | Engine 启动组装负责在打开网络身份和大文件缓存前，将 `0.19` 带 profile 后缀的目录一次性归一到当前目录；成功后不保留旧读取路径。新旧目录同时存在时停止启动，避免覆盖既有身份或缓存。新增单元验收覆盖接管、无旧目录和冲突拒绝；真实双机升级验收仍在进行。 |
| 2026-08-12 | 成员端口错误定义归位 | 成员端口只声明协作边界，所有成员错误类型统一由 `crates/uc-core/src/membership/error.rs` 定义并从成员模块对外提供；未改变运行行为或公开名称。 |
| 2026-08-12 | ADR-020 版本识别证据收紧 | 对端只按低于 `1.1` 与本次 `1.1` 区分，不按发布渠道或构建号分支。只有已认证的本次流程确认才能进入成员核对；只有已认证对端接受旧入口空连接才能显示“需要升级”。本次 `1.1` 不监听旧入口、不运行旧自动流程，空连接不发送旧请求。连接失败、超时、地址或身份问题始终保持未知，不能被误报为升级要求；运行实现和验收仍待完成。 |
| 2026-08-12 | ADR-020 待决定重启验收 | 三设备验收覆盖 C 在收到 A 对 B 的移除后、作出决定前重启；恢复后保留同一待决定项，C 与 B 各自拒绝同一移除后都只与 A 分叉，B 与 C 保持双向通信。 |
| 2026-08-12 | ADR-020 首次加入身份初始化与三设备验收 | 新建空间的发起设备在首次接纳前从当前本机签名身份取得稳定成员实例并持久化到成员历史，避免首次加入因缺少本机状态失败。三设备验收明确覆盖远端移除先等待本机决定，再由用户接受后恢复一致；C 与 B 各自拒绝 A 对 B 的移除后，A 与 C 互相停止内容发送，B 与 C 仍保持双向通信。 |
| 2026-08-12 | ADR-020 成员历史门禁修正与旧规则正文清理 | 未决定移除之后尚未采用的新增成员不会被误判为已移除；总览删除旧移除意图、交接、确认和等待流程正文，只保留当前成员历史核对与本机决定规则。 |
| 2026-08-12 | ADR-020 旧成员移除流程清理 | 成员历史成为唯一成员事实来源；删除旧的变更链、移除意图、确认、交接、等待和恢复通道。对外状态只表达成员历史、当前成员、待本机决定和对端分叉；设备上线只触发成员历史核对。 |
| 2026-08-12 | ADR-020 应用层旧成员事实兜底删除 | 邀请代数和加入测试不再读取旧成员变化数量、交接或旧移除记录；应用层当前成员事实完全从签名成员历史取得。核心保存结构仍待删除这些旧字段。 |
| 2026-08-12 | ADR-020 应用层旧确认和交接入口删除 | 删除应用负责人中旧成员变化接收、确认、交接批次和待确认查询入口，以及只验证这些入口的测试。应用层成员事实只经签名成员历史读写；旧核心保存结构将作为下一步整体收缩。 |
| 2026-08-12 | ADR-020 旧 Iroh 移除通道删除 | 删除旧成员移除交换、迟交和通知的 Iroh 适配器、路由安装和构造入口。成员历史使用独立的已认证有界通道，网络层不再用旧信封承载新历史，也不保留备用通信路径。 |
| 2026-08-12 | ADR-020 在线状态旧入口删除 | 删除 `WorkspaceConvergence` 中按成员或设备写入可达性、等待状态的应用入口；上线观察唯一可触发的流程是成员历史核对，离线观察不再进入工作空间状态。旧核心保存字段仍待在成员变更链整体删除时移除。 |
| 2026-08-12 | ADR-020 加入回执与在线状态收缩 | 加入回执改为“已保存成员历史”，只携带成员历史进度和发起方资料；加入方保存资料但不再写入旧确认状态。设备上线只触发有界成员历史核对，设备离线不再改变工作空间成员、等待或分叉事实。旧确认、交接和等待保存状态仍待整体删除。 |
| 2026-08-12 | ADR-020 加入提交改用成员历史 | 发起方保存加入时只追加签名成员历史并清除本次加入记录，不再写旧成员变更或旧交接记录。返回给加入方的进度依据已保存成员历史计算；成员历史是加入后的唯一成员事实来源。旧确认、等待和交接状态仍待随其对应运行流程整体删除。 |
| 2026-08-12 | ADR-020 负责人删除旧恢复依赖 | `WorkspaceConvergence` 不再接收旧成员移除恢复资料，也不从该资料推断本机身份；本机身份只从已加密保存的工作空间状态取得。加入前仍先执行成员历史核对，旧恢复、通知和交换测试替身一并删除。旧变更链和网络实现仍待在同一替换序列中整体删除，ADR-020 尚未完成。 |
| 2026-08-12 | ADR-020 邀请代数改用成员历史 | 邀请创建和使用现在依据已验证的成员历史进度判断是否过期；成员历史已经前进时，早于该进度的邀请不能继续建立旧权限。仍未写入成员历史的旧状态只保留原有变化数量作为临时进度，待旧链整体删除时一并移除。 |
| 2026-08-12 | ADR-020 旧成员移除入口启动检查 | Engine 宿主启动检查现在同时确认成员历史入口可用，并确认三个已取代的成员移除入口均未启动；这防止后续装配误把旧入口恢复为可访问状态。旧保存状态和旧内部代码仍待随加入链替换一并删除，ADR-020 尚未完成。 |
| 2026-08-12 | ADR-020 主动移除改为成员历史 | 本机主动移除现在只追加签名成员历史，不再创建或传播会让其他设备自动生效的旧移除记录。对端只能在历史核对后保存该事实并等待自己的接受或拒绝。旧协议定义和安全交接尚未全部删除，完整多设备验收尚未完成。 |
| 2026-08-12 | ADR-020 运行期停止旧自动移除推进 | `WorkspaceConvergence` 运行期只在已认证设备上线时启动有界成员历史核对；定时、恢复和通用唤醒不再触发旧的意图、通知或恢复资料传播。旧协议实现已失去生产入口，仍待从核心、网络和测试中删除，因此 ADR-020 尚未完成。 |
| 2026-08-12 | ADR-020 成员历史传输边界 | `WorkspaceConvergence`、Engine 组装和测试组装统一改为只依赖成员历史交换入口，调用方不能再把意图、通知或确认混入成员核对消息。Iroh 适配器暂时仍以旧信封承载该入口，待同一实施序列删除；这不是兼容承诺，ADR-020 尚未完成。 |
| 2026-08-12 | ADR-020 成员历史网络入口 | Iroh 节点现在只安装经身份核对且有界的成员历史入口；旧的意图交换、迟交和通知入口不再启动，也不接受外部消息。Engine 宿主检查改为确认新入口存在。旧适配器和保存状态尚待删除，ADR-020 尚未完成。 |
| 2026-08-12 | ADR-020 用户决定的稳定入口 | Engine、两个移动绑定和移动验收宿主现在只接收待决定编号及接受或拒绝，并复用同一完整工作空间结果。待决定编号是不可解释的稳定标识，不包含设备、成员、签名或内容；决定的验证、保存、签名和发送仍只由 `WorkspaceConvergence` 负责。旧自动移除链路尚未删除，完整多设备验收尚未完成。 |
| 2026-08-12 | ADR-020 上线成员历史核对与本机决定 | `WorkspaceConvergence` 现在在已认证设备上线后主动交换有界成员历史，并在保存验证后的远端移除后停在本机决定；拒绝只隔离对应设备，本机分支仍可继续。用户决定先持久化再发送给对端，接收方据此更新双方关系。旧移除意图、通知和迟交链路尚未删除，对外稳定动作和完整多设备验收也尚未完成，因此 ADR-020 仍不能宣称完成。 |
| 2026-08-12 | ADR-020 历史成员查询优先级 | 一旦成员历史已有实际生效的事件，工作空间当前成员集合优先从历史计算；成员实例到设备的对应关系也只从已生效历史恢复，未确认移除后的记录不能提前影响结果。旧链仅在尚未写入成员历史的过渡状态中继续存在，待真实加入和移除写入完成后删除。 |
| 2026-08-12 | ADR-020 成员历史进入工作空间保存状态 | 每个本机成员实例现在拥有一份随工作空间状态一同加密保存的成员历史和本机决定记录；首次本机加入或重新加入时建立新实例对应的空历史，旧实例事实不再混入。后续加入、移除和网络核对尚未写入该历史，因此旧成员链仍是当前运行来源。 |
| 2026-08-12 | ADR-020 成员加入历史的设备绑定 | 签名成员历史中的加入记录现在同时固定成员实例与设备标识，事件身份计算覆盖两者。后续从成员历史恢复有效成员和对端关系时不再依赖可变的旁路映射。当前运行时仍由旧成员链驱动，完整替换尚未完成。 |
| 2026-08-12 | ADR-020 待决定和分叉关系的统一查询 | `WorkspaceConvergence` 的核心快照、Engine 结果和两个移动绑定现在明确返回等待本机决定的设备与已分叉设备。两项关系仍独立于在线状态和当前成员资格，且不暴露成员历史、签名或安全资料。当前仅完成查询可见性；用户决定和设备间历史核对尚未接入运行流程，不能宣称 ADR-020 已完成。 |
| 2026-08-12 | ADR-020 分叉和待决定关系的保存与内容门禁 | `WorkspaceConvergence` 开始加密保存每个对端的成员历史关系，并让现有内容发送门拒绝 `PendingRemovalDecision`、`Diverged` 和 `Invalid` 对端。该关系不改变成员资格或在线状态，其他设备不受影响。当前新历史网络流程尚未写入这些关系，因此这只是后续完整替换的持久化和门禁基础，不能宣称 ADR-020 已完成。 |
| 2026-08-12 | ADR-020 核心成员历史与用户决定开始实施 | `uc-core` 新增签名成员事件、用户决定和本机双历史头的纯规则模型；收到的新增可前进，未由本机接受的移除停止在已应用位置，接受决定必须绑定收到移除的结果摘要，重复资料仍保持原待决定项。旧的通知、持久化、内容门禁和对外流程尚未替换，因此这一阶段不改变产品运行行为，也不能宣称 ADR-020 已完成。 |
| 2026-08-12 | ADR-020 最终改为上线核对与用户决定 | 设备上线后由 `WorkspaceConvergence` 交换双历史头和摘要，差异时按需补齐签名事件。新增可自动应用；本机未接受的移除必须等待用户。接受应用收到的同一事件并可恢复一致；拒绝只把相关设备关系标记为分叉，不自动移除发起者。分叉设备之间停止普通内容和成员历史传播，各自分支继续运行。可达性、成员资格和历史关系分别表达。该决定取代同日早先的“发现分支后停止整个旧 Space 并创建新 Space”方案。 |
| 2026-08-12 | ADR-020 改为用户解决成员分支 | 取代 8 月 11 日的 OpenRaft 多数确认方案。任一当前分支成员可在其他设备离线时加入或移除设备；签名版本绑定唯一父历史，不可比较后继保持为冲突。发现冲突的设备先保存事实，再停止旧 Space 的普通共享和成员变化；未发现冲突的设备仍可能继续自己的分支。系统明确接受旧分支在得知移除前继续邀请设备并披露其已掌握内容的风险，不自动合并或选择赢家。恢复只能创建使用全新沿革、MasterKey 和成员实例的新 Space。`WorkspaceConvergence` 继续负责完整流程；OpenRaft、投票、多数、learner、联合配置和唯一正式后继不再进入实现。 |
| 2026-08-11 | `to-spec` 技能文件标准开头标记 | 仅补充技能的名称和用途标记，未改变 Engine 的架构、职责边界或运行行为。 |
| 2026-08-11 | ADR-020 成员变化多数提交与同空间只进不退 | 采纳新的正式成员变化边界：离线移除意图仍可立即建立本机收发限制；加入、正式移除和重新加入由实施时锁定的 OpenRaft 0.9.25 建立唯一顺序，并提交同时满足旧、新配置各自多数的联合配置和最终配置。待加入设备不参与旧配置授权，且本机未提交并应用最终配置前不能激活。较旧同空间状态立即拒绝，同序号不同摘要进入恢复。没有 OpenRaft 创世记录的旧 Space 不原地转换，必须新建沿革；准入节点按 learner、联合配置和正式成员分段清理。成员变化与全空间完成使用两条公开进度。新增四设备端到端红测准确证明当前 Engine 仍会接受旧 A 并返回加入成功；本次仅新增红测和文档，正式修复未实施。ADR-015 至 ADR-017、ADR-019、四份规格和当前架构正文已同步；历史记录保留当时事实。 |
| 2026-08-11 | 同步设置与主线更新整合 | 删除已经由文件发送列表承担的重复检查，保留手动重发的既有规则；无架构变化。 |
| 2026-08-11 | 应用层未使用代码清理 | 删除重复导出、未接线的旧成员名单操作、无读取的文件计划字段、未使用错误分支，以及已被收敛运行期替代的旧入口；保留现有对外入口和实际工作空间收敛流程。无架构变化。 |
| 2026-08-11 | 同步设置领域词表 | 补充同步总开关、自动同步和离线投递恢复的稳定用户规则；与既有同步流程一致，无新增架构变化。 |
| 2026-08-11 | Engine v1.0.0-rc.8 发布准备 | 发布已验证的同步总开关与自动同步拆分；无新增架构变化。 |
| 2026-08-11 | 同步总开关与自动同步选择拆分 | 设置中的 `sync_enabled` 是所有同步方向、文件和恢复的总许可；`auto_sync_enabled` 只控制本机自动投递与离线自动补送。手动重发和历史恢复仍是用户动作：前者只受总开关和文件开关限制，后者受总开关与自身选择限制。入站运行期在解密前检查总开关，出站计划和运行期共同保证总开关不能被明确动作绕过；对外设置和手动重发结果由 Engine 统一表达。 |
| 2026-08-11 | Engine 发布互通验证消费者选择 | 互通验证中的上一版 Engine 使用桌面端主分支历史中最后一个明确固定到该提交的消费者；新版继续使用当前桌面端。两套消费者在隔离目录中构建，再做双向配对和内容传输；避免当前桌面端调用新版能力时把“旧版无法编译”误判为版本互通失败。仅修正交付检查，无架构变化。 |
| 2026-08-11 | Engine v1.0.0-rc.7 发布准备 | 同步工作区与 HarmonyOS 宿主版本，纳入当前主分支已合并的空间收敛和中继节点修正；无架构变化。 |
| 2026-08-11 | ADR-019 与规格 019 按设备公开收敛等待状态 | `WorkspaceConvergence` 已从当前有效成员、确认、待交接和不可达事实派生当前阻塞设备名单：仅等待阶段公开，排除本机、已移除和历史实例，稳定排序并使数量等于名单长度。缺失实例到设备标识映射时不发布部分名单，而是转入 `RecoveryRequired`。查询、移除结果和变化事件共用 Engine 摘要，UniFFI、HarmonyOS 与移动测试宿主完整透传；未新增持久化字段或迁移。产品仓投影和界面验收不在本仓执行。 |
| 2026-08-11 | 历史移除记录误标新加入设备修复 | 修复两处成员身份语义缺陷（正文 ADR-016 段已同步）：（1）移除目标推导改为"当前有效成员 ∩ 已验证意图目标集"，不再用"当前成员 − 意图视图认可集合"——历史意图的因果视图不含意图创建之后加入的成员，旧推导会把新加入成员误算进移除目标并生成移除其的变化，导致新设备刚加入即显示"此设备已被移除"且持续拒绝同步；修复后历史记录只移除其精确目标，全新加入与同一设备重新加入均不受影响。（2）成员身份改为"设备标识 + 本次加入生成的成员凭据"唯一确定：被移除设备本地安全视图残留旧实例（被移除设备不再接收组更新），本机身份解析一律优先本次准入凭据推导的实例（`PreparedGroupJoin` 携带，`local_admission_facts` 接受覆盖），移除发起与通知签发优先已记录的当前实例；`own_instance` 视图解析补充实例摘要日志。新增 3 个核心单元用例（历史意图不误伤新成员、仍精确移除原目标、旧意图不命中重新加入的新实例）与 2 个进程隔离端到端用例（全新设备加入含历史移除记录的空间、同一设备移除后重新加入）全部通过。 |
| 2026-08-11 | 可观测性与测试日志基础设施 | 修复 e2e 测试日志长期不可用的问题：`try_init()` 在多测试并行时只有第一个成功且静默失败，部分测试（如 W01）甚至未初始化 subscriber，导致调试只能靠临时输出。新增 `uc_engine::init_test_tracing()`（dev-tools 下公开，`Once` 幂等、按 `RUST_LOG` 过滤、失败时提示），space 成员 e2e 全部测试统一调用；slice/mdns/host 契约测试保留各自本地初始化。生产路径补充结构化 tracing：恢复交接的推送计划（目标、范围、批次、has_more）、请求/交接/确认收到与拒绝原因（空间不符、连续性缺失、试算失败、重放编号、身份不符）、Ack 推进、加入前置同步的候选与每设备结果与超时、本机确认记录（摘要截断 8 字节）、组更新跳过与乱序告警、运行期关闭等待超时。日志只含设备标识、代次、批次计数与截断摘要，不含成员实例全文、资料、密钥或内容。 |
| 2026-08-11 | ADR-016 加入前置同步与组更新幂等 | 发起加入的 sponsor 在保存准入变化前先向已知成员拉齐本地链头（`synchronize_chain`，尽力而为、20 秒有界），避免在旧链头追加形成分叉；失败或超时只记录并继续加入，接收方仍以摘要校验拒绝分叉变化。组更新应用放宽为"本地组代次大于等于更新代次即跳过"，后加入者经 welcome 已持有最新组状态，应用历史变化链的旧组更新不再乱序失败。W01、W02、连续移除等 8 个端到端场景通过；五设备连续重加入（W03）的传播在多设备旧 Offer 拒绝循环下慢于 60 秒测试窗口（传播最终可完成），暂记跳过。 |
| 2026-08-11 | ADR-016 恢复交接全链路接通 | 恢复请求驱动接通：已跟上成员对未确认成员主动交接，落后成员按轮转向已知成员请求；确认由本机签名产生并经普通成员通道广播，交接方以 Ack 把接收方计入完成。受限恢复通道的收发两侧接入应用层 AEAD 密封：信封明文头携带绑定重建所需的版本、空间指纹、历史密钥编号、请求/回复编号、变化范围、目标摘要与双成员实例（连接层已认证加密，实例为 32 字节随机标识，不泄露资料、地址、签名、密钥或内容；修订原"外层不携带成员实例"的字面约束）。入站恢复连接不再要求来源已准入或已知：密封、空间指纹与变化链验证决定来源能取得什么，未知来源只能得到稳定拒绝。接收方应用交接变化时在同一提交点保存成员资料、可信关系与地址。加入产生的 OpenMLS 组更新经变化链携带，恢复接收方随链应用，落后成员的安全世代与链一同恢复。sponsor 重启后从已保存准入记录重新等待同一加入方就绪；收敛运行期关闭不再被网络交换阻塞；被移除设备以新实例重新加入时丢弃旧实例的本地链与移除事实。W01、W02、连续移除等 8 个端到端场景通过；并发重加入在不同保留 sponsor 上分叉变化链（W03）仍未解决，不报告全局收敛完成。 |
| 2026-08-11 | 成员发现遗留资料准备流程清理 | 删除没有生产调用入口的旧资料准备、分批、统计及其仅覆盖该流程的测试；当前成员发现与工作空间收敛的责任边界不变。 |
| 2026-08-11 | 工作空间收敛对外结果收口 | `uc-engine` 的查询、移除结果和变化事件统一使用同一份 Engine 自有完整结果；删除对 `uc-core` 工作空间摘要、阶段、失败类别和快照的公开再导出。绑定与产品端只依赖稳定入口，不再感知内部成员收敛表示。 |
| 2026-08-10 | 会话恢复后在线状态刷新改为后台 | 会话恢复不再阻塞在逐端拨号上，presence 刷新移入后台任务，状态经既有 presence 事件到达；仅调整时序，无架构变化。 |
| 2026-08-10 | ADR-018 传输收口回归修复 | 阶段 2 将接收就绪门（`ReceiveReadinessCoordinator`）从 Engine 共享单例拆成多个独立实例，导致剪贴板入站 apply 路径等待一个从未被打开的 gate 而挂起（lan-compat 下 `host_adapter_contract` 全挂）。修复：`FileTransferLifecycleDeps` 接收共享 `receive_readiness` 实例，`SharedRuntimeDeps` 重新携带单例，剪贴板入站与文件传输生命周期共用同一 gate；同时删除迁移后成为死代码的 `uc-engine/src/subsystems/inbound_staging.rs`。lan-compat 全量 112 项测试恢复通过。 |
| 2026-08-10 | ADR-018 阶段 5 实施：最终收紧 | `lib.rs` 收敛为只公开 `facade` 与 `deps`；删除 `trusted_peer/` 与 `file_sync/` 应用模块及残留 `usecases/` 目录；`facade/blob_transfer` 与 `facade/file_transfer` 的会话与编排实现移入 `transfer/{blob,file}`，门面只保留薄再导出。依赖防火墙新增四项长期约束：Engine 与 `uc-mobile-lan` 只允许导入 `uc_application::facade` / `::deps`，应用层 crate 根只公开 `facade` 与 `deps`，集中或嵌套 `usecases/`、根 `runtime/`、`membership/` 目录禁止存在。对外操作、结果、错误和事件语义不变。 |
| 2026-08-10 | ADR-018 阶段 4 实施：设置领域、共享能力与 LAN 兼容线 | `facade/{settings,config_migration,storage,upgrade,diagnostics}` 与 `usecases/upgrade/` 收进 `settings/{settings,config_migration,storage,upgrade,diagnostics}`；门面只保留对外再导出。`facade/host_event/` 的事件总线、出站缓存和发布器移入 `support/`，稳定事件类型继续经门面与 `uc-core` 公开。移动端同步工作流（`facade/mobile_sync` 与 `usecases/mobile_sync`）移入独立 crate `compatibility/uc-mobile-lan`：`uc-application` 不再承载 LAN 端口与依赖，`MobileDevicePorts`/`MobileSyncPorts` 由该 crate 定义并经 wiring 传递；`uc-engine/lan-compat` 显式依赖 `uc-mobile-lan`，默认构建依赖闭包不含 LAN 能力。对外操作、结果、错误和事件语义不变。 |
| 2026-08-10 | ADR-018 阶段 3 实施：剪贴板与搜索领域收口 | 根 `clipboard_capture/`、`clipboard_write/`、`sync_planner/`、`entry_identity.rs`、`file_set_query.rs` 与 `usecases/{clipboard_history,clipboard_restore,clipboard_sync}/` 全部收进 `clipboard/{capture,write,history,restore,sync}`；出站计划拍平为 `clipboard/sync/outbound_plan.rs`。活动剪贴板、入站运行期、出站调度、历史维护运行期和资源读取分别归入 `clipboard/{active,inbound,outbound,history,resource}`；`content_tags.rs` 归入 `search/tagging.rs`，实时索引、查询、投影构建、协调器和运行期收进 `search/`。历史视图类型移入 `clipboard/history/views.rs`，门面只做再导出。Engine 引用经门面过渡再导出（阶段 5 收敛白名单）。对外操作、结果、错误和事件语义不变。 |
| 2026-08-10 | ADR-018 阶段 2 实施：传输领域收口 | `usecases/blob_transfer/`、根 `file_transfer/` 与 `receive_reconciliation.rs` 分别收进 `transfer/{blob,file,receive}`；接收就绪协调器、接收尝试核对、启动恢复、超时清理和隐私维护移入 `transfer/file/lifecycle.rs`，由 `FileTransferFacade` 持有并只暴露门面级生命周期动作（`ensure_receive_ready`、`close_receive_gate`、`spawn_timeout_sweep`、`reconcile_on_startup`、`receive_readiness_status`）。Engine 不再构造或持有 `FileTransferLifecycle`、`ReceiveReadinessCoordinator` 或接收就绪对象，`AppFacade` 新增 `file_transfer` 领域门面，`QueryReceiveReadiness` 经该门面完成。对外操作、结果、错误和事件语义不变。 |
| 2026-08-10 | ADR-018 阶段 1 实施：空间领域与密码适配收口 | `HmacProofAdapter` 从 `uc-application::proof` 移入 `uc-infra::security::admission_proof`，Engine 从 `uc-infra` 注入 `ProofPort`。空间生命周期、准入（含配对邀请签发/兑换）、成员名单（嵌套 `usecases/` 拍平）、成员连通、网络恢复、旧成员升级和群组更新投递全部收进 `space/{lifecycle,admission,roster,convergence}`；`trusted_peer/`、`usecases/{pairing,presence,setup}`、根 `group_update_delivery.rs` 和 `facade/` 中对应运行期/协调器/会话文件删除。新增 `SpaceConvergenceAssembly` 作为收敛对象唯一构造点，`SpaceApplicationRuntime::start` 统一启动收敛、gossip、连通与升级运行期；Engine 不再直接构造 `WorkspaceConvergence`、`GroupUpdateDelivery`、`MembershipConvergence` 或 `AutomaticLegacyUpgrade`，只经 `SpaceFacade` 与门面级生命周期动作进入空间领域。对外操作、结果、错误和事件语义不变。 |
| 2026-08-10 | ADR-018 门面目录收口 | `facade/` 只保留 `AppFacade`、领域入口和外部需要理解的输入输出；运行期、协调器、会话、内部适配、事件总线、缓存和投影构建分别移入所属领域或 `support/`。当前仅更新架构决策和文档，无运行行为变化。 |
| 2026-08-10 | ADR-018 用例目录统一 | 应用层目录按业务领域而非动作类别划分：删除集中 `usecases/` 和领域内同名嵌套目录的目标已写入 ADR。短动作直接归入所属领域或业务子域，持续流程与测试随领域收口；`facade/` 继续是唯一对外入口。本次仅更新架构决策和文档，无运行行为变化。 |
| 2026-08-10 | ADR-018 空间成员目录收口 | `space` 现在包含 `roster` 与 `convergence`：前者管理当前空间的成员名单和同步偏好，后者统一负责加入、移除、重新加入、资料补齐、确认和重启后的继续；设备发现成为 `convergence` 的内部部分，加入通信成为其私有 `admission`。原顶层 `membership` 与 `workspace_convergence` 目录及其调用路径已删除，不保留兼容入口。业务行为不变。 |
| 2026-08-10 | ADR-018 应用层按业务领域收口 | 采纳按业务领域组织应用层：短动作、持续流程和测试随领域一起收口，门面是唯一对外业务入口，`AppFacade` 只聚合少量领域入口。迁移时同步改外部调用并删除旧目录、再导出和转发层，不保留兼容路径；本 ADR 不改变当前业务行为。 |
| 2026-08-10 | `uc-application` 目录与对外入口治理 | 明确应用层按业务领域收口：门面是外部唯一业务入口，短动作由领域内用例完成，持续流程由同一领域的协调器或运行期负责；`AppFacade` 只聚合少量领域入口，不再作为平铺动作清单。组装数据是唯一可公开的非业务例外，实际组装仍由 Engine 负责。迁移必须删除旧目录路径、再导出和直接调用，不保留兼容入口。仅更新维护规则，无运行行为变化。 |
| 2026-08-10 | ADR-017 实施落地 | 配对三模块（入站/出站/邀请持有人）内聚为 `WorkspaceConvergence::admission` 内部通信实现，新增适配位置 `WorkspaceAdmissionOwnerPort`；负责人接管五步加入编排（`begin_admission` 保存发起方准入记录、`commit_joiner_admission` 同一提交点保存成员变化与待交接事实、`record_admission_committed` 记录准入确认），新增 `AdmissionCommitted` 会话消息承载"准入变化已保存"确认；删除配对侧成员/信任/地址保存、`subscribe_pairing_completion`、`PairingCompleted`、配对成功分析事件与 Dev 结果流；空间切换与重新加入共用同一准入门。 |
| 2026-08-10 | ADR-017 配对纳入工作空间准入 | 成员加入由 `WorkspaceConvergence` 完整负责；配对退为其内部通信通道。外部只创建邀请或加入工作空间，加入成功必须已有已保存成员变化；未保存会话重启后不产生成员资格，已保存变化由工作空间继续。仅完成文档决策，尚未实施。 |
| 2026-08-10 | ADR-017 技术说明 | 新增 `docs/specs/017-pairing-as-workspace-admission.md`：配对是工作空间准入内部私有通信通道，经适配位置验证加入者、发送工作空间决定；加入成功分层表达（`JoinSpace` 同步返回本机已准备，成员变化已保存由工作空间状态表达），实施时删除 `subscribe_pairing_completion`、`PairingCompleted` 与分析事件；会话与邀请均非持久事实，恢复只依据已保存成员变化；固定五步加入顺序与删除检查（配对侧成员保存、旧收敛补送、可选负责人、平行握手编排）。仅文档，无运行架构变化。 |
| 2026-08-10 | ADR-017 技术说明补强 | 重写为与规格 016 同深度的完整规格：补充保存边界与三类重启结果（本机准备事实、发起方准入记录加密落库）、准入代数与邀请失效规则、就绪确认与准入变化确认的时序、加入后与既有成员的交接衔接、适配位置两个独立测试面、稳定失败类别表、持久化与恢复边界、六阶段实施顺序、验收总则与 P01-P17 场景矩阵、当前证据边界（七项现状差距清单）。仅文档，无运行架构变化。 |
| 2026-08-10 | ADR-016 配对完成的准入资料保存 | 加入方只在本机安全会话可用后生成并签名准入资料，随就绪确认交给发起方；`WorkspaceConvergence` 在一次保存中补齐发起方和新成员的连续加入变化。实际三设备验证已证明发起方可保存三名成员并形成首次移除变化；新成员接收保存后变化的交接仍未接通，因此不能报告全局收敛完成。 |
| 2026-08-10 | ADR-016 验收场景实跑 | 已实际执行三个独立进程的移除场景，均在首次提交移除时被拒绝；原因是配对完成尚未将成员变化写入 `WorkspaceConvergence`。W12 删除检查通过，其余规格验收尚未执行，不能报告整体完成。 |
| 2026-08-10 | ADR-016 旧流程清除与移除传播 | `WorkspaceConvergence` 现为唯一流程负责人：普通成员通道只传播已验证移除意图及确认，被移除实例只可迟交历史意图或接收定向通知；通知按意图标识幂等保存。本次删除独立成员移除、共享设备刷新及其持久化/恢复路径，不保留兼容层。完整多设备验收仍未执行，真实配对准入、确认和恢复驱动尚未接通，不能报告工作空间收敛完成。 |
| 2026-08-10 | ADR-016 工作空间全局收敛实施（阶段 1-5） | 实施已落地：OpenMLS 验证转交、断档与分叉拒绝；`uc-core` 新增工作空间变化/摘要/确认/阶段状态机与加密持久化仓库（含迁移）；`uc-application` 新增 `WorkspaceConvergence` 统一负责人，接管移除提交、加入变化记录、交接进度、确认与快照，并实现移除目标门与准入门；`uc-infra` 新增 `workspace-recovery/1` 受限恢复通道与应用层 AEAD 密封（历史 Transport 密钥 + HKDF 域派生 + 强绑定）；`uc-engine` 与三端绑定删除 `RefreshSharedDevices`、`QuerySharedDeviceRefresh`、`QueryMembershipConvergence`、`QueryMemberRemoval` 及对应事件，公开 `QueryWorkspaceConvergence`、`WorkspaceConvergenceChanged` 与统一快照。完整多设备离线/重启验收与旧分散队列删除尚待阶段 6。 |
| 2026-08-10 | ADR-016 收敛规格校正 | 明确这是待实施目标：`WorkspaceConvergence` 将取代单独成员移除流程的完整负责人和公开状态边界；加入成功只表示本机准备，恢复回复须先授权当前有效成员实例，并以共同历史资料上的应用层密封交接有界连续变化。大于单批上限时持续交接，只有最终摘要确认才算完成。当前局部实现不因此被表述为全局收敛已交付。 |
| 2026-08-10 | ADR-016 技术说明 | 新增工作空间全局收敛的实施约束：应用层唯一负责人统一处理加入、移除、重新加入、连续变化交接、确认与重启恢复；外部只保留成员操作、完整状态查询和变化订阅。状态以所有当前有效设备保存相同摘要且关系可用为完成条件，旧的独立刷新、成员移除和收敛状态将在实施时删除，不保留兼容路径。 |
| 2026-08-10 | ADR-016 工作空间全局收敛 | 采纳单一工作空间收敛负责人：加入、移除和重新加入只产生成员变化，由负责人持久保存连续变更、允许任一已跟上设备转交、以接收方验证后建立关系，并以所有当前有效设备确认且关系可用作为唯一完成条件。旧的分散资料投递和“先认识发送者才接收”路径将在实施完成后删除。 |
| 2026-08-10 | 离线旧成员经新成员转交连续安全变化的可行性验证 | 新增 OpenMLS 验证：已有成员离线期间由另一已有成员准入新成员，发起准入的成员随后离线；新成员仅转交原发起者签发的完整变更，离线旧成员能独立验证、按序应用并与新成员恢复相同安全状态。证明底层能力可支撑工作空间统一收敛；产品级统一负责人和持久化转交流程尚未实现。 |
| 2026-08-10 | 五设备移除后重新加入验收红测 | 新增用户路径验收：用户只依据发起设备显示的“移除完成”继续移除另一设备，随后两台设备分别通过不同保留设备重新加入。通过条件是五台设备最终彼此可见并完成双向内容同步；当前该测试失败，说明现有实现尚不能将局部完成兑现为整个工作空间可用。本次不改变运行行为。 |
| 2026-08-10 | 配对完成与首次成员关系请求扣合 | 新协议要求发起方在最终确认后等待加入方的“本地关系已保存且可接收”确认，才创建并启动已有成员资料交接；首次加入和兑换邀请在发送该确认前恢复本机会话。已到达的成员关系请求若接收方尚未可读写，会在同一连接等待会话就绪后继续，不再先拒绝再依赖定时重试。目标设备离线而未送达的必要更新由成员关系运行期在观察到设备重新可达时立即补送，之后才继续确认成员关系。延时重试仅保留给离线、连接中断和超时等异常。 |
| 2026-08-10 | 清理废弃设备表 | 删除从未被代码使用的首张设备表 `t_device`（2026-01-04 创建后即被成员模型取代）：新增迁移 drop 该表并从 diesel schema 移除定义；`t_device` 的 down 迁移仅重建空壳以保持可逆。本次无其他废弃表：已逐表核对，其余 27 张表均有代码引用。 |
| 2026-08-10 | 缺失安全资料主动拉取与已应用资料转发 | 成员收敛补齐“资料到达”闭环：本机成功应用的安全升级资料（世代推进）经新的加密 `membership_applied_security_update` 记录持久化，成为 `events_for_request`、共享设备页面种子与摘要头部资料的来源，即使本机从未作为赞助种子候选也能向落后设备转发；存在 `WaitingForUpdate` 候选时，收敛循环主动向已连接成员发送缺失资料请求并立即应用，不再只依赖对方按自己的节奏推送。拒绝路径补记录原因（空间不匹配、请求方非成员、世代不匹配的双方世代）。新增数据库迁移仅扩展关系表 kind 约束，密文载荷不迁移。 |
| 2026-08-10 | uc-core 状态机建模规范 | `crates/uc-core/AGENTS.md` 新增强制规范：带生命周期的领域实体必须使用 `apply(event) -> (outcome, effect)` 单一转换入口，事件是状态变化的唯一途径；效果（持久化、唤醒）由调用方执行；构造与转换分离；终态不可回退；禁止 typestate 拆分持久化实体与第三方状态机库；转换矩阵与不变量测试要求。仅文档约定，无运行架构变化。 |
| 2026-08-10 | 候选状态机事件化 | `SpaceMembershipCandidate` 的状态转换收敛为 `apply(CandidateEvent)` 单一入口，事件（Seed/VerifiedAnnouncement/VerifiedPeer/Confirming/AttestationFailed/SecurityMaterialApplied/Admitted）是状态变化的唯一途径；转换返回 `CandidateEffect`（持久化、唤醒运行期）由调用方执行。`merge_*`/`mark_*` 改为私有，`should_persist_merge` 移入核心，终态（Blocked/Rejected）不可回退；序列化格式、协议消息与对外行为不变，全 workspace 2590 测试通过。 |
| 2026-08-10 | 等待资料更新候选的重试闭环 | 候选进入 `WaitingForUpdate` 后带与 `WaitingForPeer` 一致的指数退避继续重试，不再永久停住；收到并应用缺失安全更新（世代推进）时立即把本空间所有 `WaitingForUpdate` 候选唤醒为可重试。'会继续尝试'从此有真实的推进路径。 |
| 2026-08-09 | 解锁后自动查找已共享设备 | 成员关系收敛运行期在会话解锁恢复成功后自动调度一轮已有设备查找，复用手动刷新同一查询、确认与状态通知流程；按 Space 合并，锁定时不开始，一次恢复只启动一轮，重复解锁、就绪或在线事件不产生重复查找，来源离线时等待其重新上线后继续且不无限重试，锁定重置自动调度。产品端不保存查找队列，无新增可见状态。 |
| 2026-08-09 | 移除通知已实施 | 已验收移除意图的当前成员经独立受限通道 `removal-notice/1` 向被移除设备定向投递仅含空间沿革指纹、意图标识、目标成员/设备、签发者与签名的 `RemovedNotice`；接收方按本机保存的视图公开签名资料核对签发者并验签后写入独立的 `self_removed` 标记，`QueryMemberRemoval`/`MemberRemovalChanged` 携带 `removed`。通知不扩展现有 `RemovalExchangeMessage`(普通通道只接受当前成员)，非完成条件、失败不阻塞收敛；接收方拒绝类别与迟交入口一致(1 KiB/并发 4/每身份 10 秒一次)。重新准入产生新实例后旧标记惰性清除，重放旧通知不能再次锁定新实例；在线验收意图与收到通知写同一标记，L07 语义不变。spec 015、接口说明与逻辑通道表已同步，草案已合并删除。 |
| 2026-08-08 | 本地产物准备锁定依赖 | 本地准备脚本固定以 `--locked` 构建三个平台产物，与发布构建和交付检查使用同一 Cargo.lock；仅修正交付流程，无架构变化。 |
| 2026-08-08 | 移动端日志文件层与连接中继日志 | 移动绑定在系统日志之外叠加按天滚动文件层，目录与级别由宿主能力决定，失败降级不影响启动；连接刷新成功后核心只记录在线且经中继对端的中继地址，快照失败只记脱敏类别。 |
| 2026-08-08 | 本地产物准备流程 | iOS 构建脚本支持按设备或模拟器切片产出，新增本地准备脚本归集 `.artifacts/local/{ios,ios-sim,android}` 并生成带 sha256 的就绪清单；发布构建默认行为不变。 |
| 2026-08-08 | iOS 与 Android 绑定翻译层重构 | 绑定内部以 `map_enum!` 与 `unpack_operation!` 宏生成一一对应枚举映射并消除 OperationResult 提取样板；统一翻译函数命名，无运行行为与 FFI 契约变化。 |
| 2026-08-08 | iOS 与 Android 自定义中继绑定入口 | 绑定只将一个用户动作转换为 Engine 既有的中继设置事务；地址结果可返回给宿主，访问令牌仍只由安全存储处理，不改变核心设置或网络职责。 |
| 2026-08-10 | iOS 与 Android 多中继节点保存 | 绑定在单个节点新增、修改或删除前读取当前中继列表，并将合并后的列表与该节点凭证作为同一设置事务提交；其他节点不会被覆盖，访问令牌仍不返回给产品界面。 |
| 2026-08-08 | 成员移除恢复审查修正 | 恢复只处理当前空间的未完成记录；暂时无法读取持久化资料时保留可重试结果，只有资料缺失、解密失败或完整性失败才进入需要安全恢复的终态。 |
| 2026-08-08 | 移动验收宿主共享设备刷新结果 | 验收宿主只输出共享设备刷新的阶段和汇总数量，不保留设备标识或名称；仅补齐既有公开结果与通知的接收，无运行架构变化。 |
| 2026-08-07 | 成员移除准备记录恢复 | 应用层以同一个成员移除恢复入口处理启动、状态查询和重复移除；基础设施只核对安全组并原子保存结果；三端绑定只展示统一的恢复中状态，不自行重试或推断。 |
| 2026-08-08 | ADR-015 三设备收敛完成确认修复 | 后台推进改为“锁内决策与持久化、锁外网络交换、锁内应用响应”：入站消息与后台推进对持久化状态的修改串行化，消除旧快照覆盖新写入的竞态；本机是否仍为有效成员、是否本轮执行者一律按设备判断，恢复资料应用后成员实例变化不影响判断；完成确认投递记录使用发送计划中的成员实例键，恢复后不重新解析；完成判定只在恢复摘要与当前意图摘要一致时成立，旧轮恢复资料不会误结束新轮。三设备单移除与连续移除验收均稳定通过，连续移除的第二轮视图只含恢复后的当前成员。 |
| 2026-08-09 | 成员移除后的有效设备名单 | 应用层成员移除负责人同时约束对外设备名单、连接刷新和后台保持连接的目标集合；已移除旧实例不会因原始成员或地址记录再次出现或被拨号，重新准入的同一设备以新实例重新成为有效设备。产品端继续只消费核心返回的完整名单，不维护过滤或重试状态。 |
| 2026-08-08 | ADR-015 完成后的下一轮成员移除 | `Complete` 只结束当前意图集合；后续移除仍由应用层同一成员移除模块从已保存的当前安全状态发起。恢复后被替换的旧成员实例只用于历史验证、去重和迟到意图核验，不参与下一轮有效成员、资料分发或完成确认；该替换关系和其他成员移除事实一同加密保存并跨重启保留。 |
| 2026-08-08 | ADR-015 连续成员移除 | 一轮恢复完成后，被重新生成成员实例替换的旧实例只保留作历史验证，不再计入下一轮有效成员集合；这一事实和意图、确认一同加密保存。当前轮仍以恢复前的有效成员集合收齐确认，避免提前淘汰影响完成判定。 |
| 2026-08-08 | ADR-015 三设备验收日志与恢复资料 | 三设备独立目录验收在发生失败时保留受控的流程阶段和失败类别日志，便于确认成员移除停在资料交换、应用还是确认阶段；恢复接收侧还区分暂存资料读取、资料验证、保存、安装和清理失败。不改变对外行为，也不记录设备资料、密钥或内容。每个保留成员在同一轮恢复中只生成并保存一份资料，重复网络请求复用该资料，避免覆盖后无法应用执行者已持有的请求结果。普通准入与成员移除恢复的资料封装分别由明确入口处理，不凭资料内容猜测路径。 |
| 2026-08-08 | ADR-015 空间切换与保存不变条件 | 成员移除状态和恢复暂存资料都只接受当前空间的写入；空间切换后残留的旧后台任务不能把旧空间的意图、收敛状态或恢复资料写回磁盘，更不能影响新空间。任何已保存意图也必须始终和本机限制一起保存，后续写入不能重新信任其目标。 |
| 2026-08-08 | ADR-015 成员移除日志边界 | 成员移除相关日志只记录固定失败类别、计数和阶段，不输出底层错误正文，避免数据库、网络或设备资料经日志泄露；自动日志捕获覆盖了持久化失败原文。 |
| 2026-08-08 | ADR-015 公开因果证明 | 设备间只交换成员视图、公开签名资料和签名，不再传递本机完整安全状态。接收方用已保存的历史检查点判断是否具备基准历史；私有状态始终只在本机加密保存。 |
| 2026-08-08 | ADR-015 因果历史不足处理 | 将已锚定的因果视图检查点和成员移除状态一同加密保存。签名正确但本机缺少同一基准历史时进入需要人工处理；损坏或伪造的证明只被拒绝，不影响当前空间。 |
| 2026-08-08 | ADR-015 准入边界 | 将邀请创建和使用统一接入成员移除协调模块；未收敛、需要人工处理或状态不可读时拒绝，新增移除事实使先前邀请失效。判断只暴露允许或拒绝，不把成员、摘要或安全状态泄露给请求方。 |
| 2026-08-08 | ADR-015 实施状态与失败结果 | 将总览从“已实施”更正为“正在实施”，避免把局部验证写成整体交付；成员移除对外失败结果区分错误目标、找不到目标和当前状态不允许，调用方仍只使用一次提交入口。 |
| 2026-08-08 | ADR-015 生产实现的恢复保存顺序与通信职责 | 应用层在执行者本机安装前先持久化恢复资料和私有检查点，重启复用同一资料而不生成竞争状态；基础设施将普通交换与受限迟交通道分开持有依赖，Engine 只负责一次装配。旧单项成员移除、永久丢失继续入口和相关文档描述已删除。 |
| 2026-08-08 | ADR-015 技术说明的运行时详细闭环 | 补充从本机保存、普通交换、向前安全状态、迟到意图、完成状态重开到重启恢复的明确先后关系；产品端继续只负责提交、查询和展示，应用层统一负责完整流程。仅澄清既有决策，无架构决策变化。 |
| 2026-08-08 | ADR-015 技术说明的对外状态和推进规则 | 补充唯一对外入口、完整状态字段、状态转换、受限迟交入口的返回边界与固定上限；旧成员移除入口和状态转换将在 ADR-015 实施阶段删除。仅补充既有决策的稳定约束，无架构决策变化。 |
| 2026-08-08 | ADR-015 技术说明的后续实施流程 | 补充从前置验证、本机保存、生产接线、设备交换、后台推进到删除旧流程和验收的强制顺序及每阶段完成条件；明确重新加入、旧邀请和空间切换必须由成员移除模块在准入前统一判定，且旧空间事实不得进入新空间。仅澄清既有 ADR-015 的交付路径，无架构决策变化。 |
| 2026-08-07 | ADR-015 与离线优先成员移除技术说明 | 已决定由应用层统一管理可离线创建、分布式交换的签名移除意图；合法并发意图按移除优先合并，再向前形成统一安全状态。技术说明补充已移除发起者迟交、完成状态重开、安全模型边界和完整验收矩阵。无固定协调设备、中心排序或产品端队列；实现前保留当前单项拒绝行为。 |
| 2026-08-07 | 架构文档归档 | 需求、技术说明和重大决策分别归入 `docs/prd/`、`docs/specs/` 和 `docs/adr/`；本文继续是当前架构的唯一总入口，过时的架构教程不再保留。 |
| 2026-08-06 | 配对确认成员资料迁移 | 配对最终确认只承载准入所需的有界数据；已有成员资料在确认前分批持久化、确认成功后交接，并以确认和重试收敛。成员交接日志只保留脱敏统计和失败类别。 |
| 2026-08-06 | 剪贴板同步详细延迟日志 | 发送与接收阶段统一由 `uc-observability-contract` 输出结构化本地日志；每个阶段名称与实际测量范围一致，发送完成以接收端实际保存或重复确认回执为准，直连或中继取本次传输的实际路径。Engine 不接入外部发送器，宿主负责日志保留或采集；产品分析不接收逐阶段数据。 |
| 2026-08-06 | Engine 发布通知凭据 | GitHub App 的客户端编号和私钥均从发布流程可读取的受保护配置取得，并使用当前字段名；发布后的双向互通确认仍是通知下游的前置条件。仅修正交付流程，无架构变化。 |
| 2026-08-06 | Engine 移动端发布构建提速 | iOS 与 Android 发布构建独立并行，分别复用受版本约束的可重建编译缓存；汇总、产物校验和双向互通验收不变。仅优化交付耗时，无架构变化。 |
| 2026-08-06 | Engine v1.0.0-rc.6 发布准备 | 同步工作区与 HarmonyOS 宿主版本，纳入成员移除恢复与发布互通验证修正；无架构变化。 |
| 2026-08-06 | Engine 发布互通验证消费者选择 | 旧版与新版 Engine 在同一份当前桌面端源码上分别构建并进行双向互通验收；不得根据桌面端清单中较早的 Engine 提交推断其可处理后续公开事件。仅修正交付流程，无架构变化。 |
| 2026-08-05 | 成员移除旧等待记录核对与恢复 | 成员移除负责人在确认永久丢失前以当前安全组核对每台等待设备；组外设备在同一次持久化更新中结束旧等待，组内设备继续向前移除并通知保留设备，无法核对时进入安全组重建恢复，永久丢失设备不会被重新加入。 |
| 2026-08-05 | Engine v1.0.0-rc.5 发布准备 | 同步工作区与 HarmonyOS 宿主版本，纳入 `rc.4` 发布后互通工作流修正；无架构变化 |
| 2026-08-05 | Engine 发布互通消费者选择、通知凭据与工作流供应链校正 | 互通验证分别构建当前与上一版可兼容的桌面消费者，GitHub App 客户端编号与私钥使用发布流程可读取的受保护配置；发布工作流按作业收敛权限、固定 Action 提交并不在标签发布中共享 Rust 缓存；仅修正交付流程，无架构变化 |
| 2026-08-05 | Engine v1.0.0-rc.4 发布准备与 HarmonyOS 清单兼容 | 同步工作区与 HarmonyOS 宿主版本，并使发布版本校验兼容 JSON 与 JSON5 清单写法；无架构变化 |
| 2026-08-05 | Issue #1516 网络恢复审查修正 | 恢复负责人对每次公开状态转换都发出事件并携带等待重试的剩余时间；会话重建沿用同一代计数，Iroh 观察在无订阅者时保留本机恢复信号；传输进度使用独立新版本通道表达连接恢复中断。 |
| 2026-08-05 | Issue #1516 网络恢复关闭、过期失败与词表修正 | 关闭先取消恢复流程；自动恢复在尚未开始下一次重建前收到新鲜拨号成功时停止，手动恢复不受影响；词表同步稳定恢复状态与恢复中断传输规则。 |
| 2026-08-05 | Issue #1516 发送文件恢复收尾与移动端状态通知 | 会话替换前由发送进度运行期统一给所有未结束发送写入一次连接恢复中断结果，再停止旧任务；三端绑定接收同一稳定恢复状态，不自行判断或重试。 |
| 2026-08-05 | Issue #1516 网络自动恢复计划校核 | 仅校核并统一方案文字，当前运行架构和行为未变化；10 秒为严格触发路径的 Windows 双机验收目标，实际关闭与创建耗时必须以实测记录证明。 |
| 2026-08-05 | Issue #1516 应用层网络会话恢复负责人 | 新增应用层单一恢复负责人，收口严格自动触发窗口、同轮合并、失败状态、固定有界重试和关闭取消；已接入 Engine 会话监督者和完整会话替换，基础设施观察及产品接口仍在实施。 |
| 2026-08-05 | Issue #1516 自动网络恢复观察 | Iroh 中转恢复、此前在线设备的受限确认失败及新鲜拨号成功通过脱敏内部观察进入应用层恢复负责人；严格窗口使用两轮各两秒确认，低成本修复共享且有界，普通离线和 LAN 兼容线不触发完整重建。 |
| 2026-08-05 | Issue #1516 网络自动恢复方案规划 | 仅新增执行计划，当前运行架构和行为未变化；后续实现拟由应用层统一负责触发、单次执行和退避，Engine 运行期统一负责安全重建完整运行会话。对本机刚换网且此前在线设备持续失败的严格路径，计划采用两次各最多 2 秒的快速确认，以系统网络稳定后 10 秒内重新在线为 Windows 双机验收目标，不改变普通连接预算。 |
| 2026-08-05 | 跨平台产品分析平台字段 | 公共观测约定统一定义平台字段；移动端在启用产品分析时一次提供，发送层自动加入所有事件，桌面端复用同一约定，业务事件不再各自处理平台信息。 |
| 2026-08-05 | 文件接收取消并发收尾 | 同一接收尝试取得取消权后，下载线程与用户取消入口可先后完成同一无产物的取消收尾；持久层只把同尝试的已取消重复收尾视为成功，保持旧尝试和其他终态的冲突保护。 |
| 2026-08-05 | 离线投递最新项覆盖 | `ClipboardSyncRuntime` 对每台既有设备只保留最新本机内容的自动补送资格；新内容将同设备旧的暂时不可达记录置为 `Superseded`，设备上线时最多补送一条，避免历史内容倒灌。 |
| 2026-08-05 | Engine v1.0.0-rc.3 发布准备 | 仅同步工作区与 HarmonyOS 宿主版本，无架构变化 |
| 2026-08-05 | 离线投递恢复 | `ClipboardSyncRuntime` 统一本机自动发送、设备上线恢复和手动重发；手动重发与自动恢复只负责各自的目标选择，共用内部已有内容发送能力；仅依据既有条目、设备关系和投递记录恢复已存在设备的暂时离线内容，不建立待发送列表，也不向新设备补发历史。 |
| 2026-08-05 | 文件接收取消收尾 | 应用层的接收取消用例在取得取消权后统一负责下载停止、临时目录清理、终态持久化和终态事件；普通文件与目录共用同一终态规则，不依赖后续回调完成取消。 |
| 2026-08-05 | Engine v1.0.0-rc.2 发布准备 | 仅同步工作区与 HarmonyOS 宿主版本，无架构变化 |
| 2026-08-05 | `CONTEXT.md` 补充近期领域词汇 | 同步成员候选、成员移除、两种接收方式、不可读历史保留、空间切换和移动端产品分析的既有语义，无新增架构变化 |
| 2026-08-05 | 空间切换安全状态清理 | 切换到新空间且握手成功后，应用层统一清除目标空间之外的成员移除、密钥状态和旧安全流程记录，同时保留刚建立的目标空间安全状态 |
| 2026-08-05 | iOS 和 Android 空间状态诊断日志 | Engine 查询结果经 UniFFI 绑定写入移动系统日志；仅记录脱敏状态、数量和稳定错误字段，不改变运行架构 |
| 2026-08-04 | Engine 发布通知凭据读取 | 发布通知统一从组织密钥读取 GitHub App 身份和私钥；仅修正交付配置，运行架构无变化 |
| 2026-08-04 | 已发布 Engine 版本复核入口 | 已发布版本只复用其不可变提交完成构建和产物复核，不重新创建或覆盖发布。 |
| 2026-08-13 | 发布流程移除跨版本互通步骤 | 公开产物确认后直接通知产品仓库；跨版本互通不再属于发布流程，也不再阻塞发布结果。仅调整交付流程，无运行架构变化。 |
| 2026-08-04 | Engine 发布互通验证构建 | 明确发布验证同时构建命令行和后台程序；仅修正交付检查，运行架构无变化 |
| 2026-08-04 | Engine v1.0.0-rc.1 发布准备 | 仅同步工作区与 HarmonyOS 宿主版本，无架构变化 |
| 2026-08-04 | 成员移除审查修复 | 应用层继续以单一流程处理已完成和仍等待设备的混合永久丢失选择；核心先完整验证再改变恢复状态，基础设施在持久化前验证恢复材料，宿主只展示和查询，不自动推进永久丢失恢复 |
| 2026-08-04 | 空间范围内保护状态读取 | 旧升级记录由存储层在打开前按当前空间筛选，并排除已完成记录；保护状态与恢复只读取仍影响当前空间的记录，运行架构无变化 |
| 2026-08-04 | 成员移除等待、通知与永久丢失恢复 | 应用层单一流程完整负责冲突、幂等、等待、重试、跨重启恢复和多轮向前换钥；Engine 与三套移动绑定只提供统一状态、入口、错误和通知 |
| 2026-08-04 | 应用层复杂度收口 Phase 9 删除与整体验收 | 删除无调用者的通用生命周期空壳、只读搜索半模式、同步重建旁路及旧测试；新增架构检查，防止剪贴板入站、文件传输、移动上传、搜索和成员活动复杂度回流引擎 |
| 2026-08-04 | 应用层复杂度收口 Phase 8 应用总入口 | 生产对象改为一次完整构造，内部对象全部私有；引擎只调用顶层用户动作，删除默认补空、运行中后装、无用生命周期占位和未装入的移动同步槽位 |
| 2026-08-04 | 应用层复杂度收口 Phase 7 历史维护运行期 | 应用层单一运行期负责首次与定时维护、固定顺序、失败分流、后续重试、汇总日志和关闭等待；引擎只启动和关闭运行期，删除原维护模块与逐步入口 |
| 2026-08-04 | 应用层复杂度收口 Phase 6 移动文件上传 | 移动同步应用入口内部的单一负责人完整管理上传句柄、临时写入、进度、文件传输会话、失败清理和关闭等待；引擎只转换四个稳定动作，删除活动上传表和重复清理分支 |
| 2026-08-03 | 应用层复杂度收口 Phase 5 文件传输会话 | 应用层单一会话负责接收登记、开始、进度、唯一终态和关闭；Blob 拉取与移动流式上传复用会话，删除五个逐步状态入口、重复进度通知和 `AppFacade` 绕过路径 |
| 2026-08-03 | 应用层复杂度收口 Phase 4 入站运行期 | 应用层单一运行期完整负责远端接收、策略、解密、应用、宿主通知、发送方确认和关闭；删除中间广播、逐订阅者桥接、引擎层结果分支及旧测试路径 |
| 2026-08-03 | 应用层复杂度收口 Phase 3 入站模式 | 应用层提供普通接收和只保存拉取两个完整模式；引擎层不再逐项拼装入站能力，也不再用假写入器表达只保存语义 |
| 2026-08-03 | 应用层复杂度收口 Phase 2 搜索运行期 | 应用层完整拥有生产搜索的构造、后台工作、会话暂停恢复和关闭；引擎只持有一个完整运行对象，只读场景使用独立模式，删除运行中补装和重复补装终止进程的路径 |
| 2026-08-03 | 应用层复杂度收口 Phase 1 行为基线 | 仅补充稳定 Engine 入站、移动上传清理与失败、文件传输终态测试和覆盖矩阵，当前运行架构无变化；未能从最终负责人验证的场景保持未通过并绑定后续阶段退出条件 |
| 2026-08-03 | 成员移除等待与永久离线恢复方案规划 | 仅新增实施方案，当前运行架构无变化；后续由应用层统一负责等待、重试、状态查询和用户授权的永久离线恢复 |
| 2026-08-03 | Engine v0.20.0-rc.20 发布准备 | 仅同步工作区与 HarmonyOS 宿主版本，无架构变化 |
| 2026-08-03 | 空间切换异常历史恢复 | `SwitchSpaceUseCase` 继续单独负责预检、确认策略、密文保留、阶段持久化和恢复；平台调用方只提供明确选择并接收稳定结果 |
| 2026-08-03 | Engine v0.20.0-rc.19 发布准备 | 仅同步工作区与 HarmonyOS 宿主版本，无架构变化 |
| 2026-08-03 | iOS 和 Android 产品分析宿主契约 | 移动绑定新增显式启用的产品分析启动方式；核心拥有事件定义和身份切换顺序，宿主拥有用户许可、发送和身份持久化，原启动方式保持关闭状态 |
| 2026-08-03 | Engine 发布后自动通知产品仓库 | 新增上一版与新版的双向联通门槛，并由同一个 GitHub App 完成通知、版本分支推送和拉取请求维护；不改变 Engine 运行时架构 |
| 2026-08-03 | Engine v0.20.0-rc.18 发布准备 | 仅同步工作区与 HarmonyOS 宿主版本，无架构变化 |
| 2026-08-03 | 成员移除的跨重启恢复 | 明确仅替换落后于当前空间世代且尚未执行的准备记录，后续状态继续由原恢复流程推进 |
| 2026-08-03 | 首次建立架构总览，并固化 Agent 同步维护要求 | 建立当前架构基线 |
| 2026-08-19 | 旧资料独立化评审修复 | 独立化流程在清理前保存唯一目标并在中断后复用；会话恢复与手动解锁共用同一后处理，产品可通过事件或状态查询恢复重新配对提示 |
| 2026-08-19 | 旧资料升级识别收口 | Engine 启动与产品查询共用应用层升级判断；没有版本游标但已经完成初始化的旧资料会进入独立化，全新安装不会误触发。独立化是否需要的版本规则只保留在升级状态中。 |
| 2026-08-21 | 当前成员范围失败分类 | 失败日志只记录旧成员记录读取、空间保护关系读取或当前历史缺失等固定类别，以及必要的模式、布尔值和数量；不改变成员判断。 |
| 2026-08-21 | HarmonyOS 成员同步偏好绑定 | HarmonyOS N-API 直接暴露 Engine 已有的成员同步偏好查询与局部更新操作；产品宿主不再启动或调用平行的旧空间节点。仅补齐跨平台绑定，无核心架构变化。 |
| 2026-08-21 | 缺失空间收敛记录的旧资料恢复 | 只有确认的首次安装按新资料处理；既有安装缺少收敛记录时建立并加密保存旧资料迁移来源，已有记录优先，不补造签名成员历史。 |
| 2026-08-21 | rc.3 空间升级来源恢复补全 | 对已被早期修复转换的状态，不再要求空间处于完成阶段；本机身份与既有设备关系存在、成员历史缺失，并且空间未移除、未进入人工恢复且没有保存失败时恢复旧资料迁移来源。正常应用和收敛阶段都可继续既有受控迁移，证据不足或失败状态仍保持拒绝。 |
| 2026-08-20 | rc.3 空间升级来源恢复 | 读取 rc.3 加密空间状态时，缺少已生效成员历史即恢复为旧资料迁移来源；对已被早期修复转换的状态，仅在空间已完成、本机身份与既有设备关系均存在且成员历史缺失时恢复同一来源。既有受控迁移流程随后补齐成员基线，不把设备记录直接提升为长期成员事实。 |

## 相关文档

- `AGENTS.md`：仓库不可破坏规则和交付要求。
- `CONTEXT.md`：统一领域词表。
- `VISION.md`：长期目标和边界。
- `docs/README.md`：需求、技术说明和决策记录索引。
- `docs/specs/uc-engine-interface.md`：稳定操作、结果、事件和宿主能力。
- `docs/specs/ports.md`：内部能力接口和边界。
- `docs/security/encrypted-persistence.md`：密文持久化规则。
- `docs/security/release-integrity.md`：发布来源和校验规则。
- `docs/adr/011-reliable-member-revocation.md`：可靠成员移除和密钥世代。
- `docs/adr/015-offline-first-member-removal.md`：已由 ADR-020 取代的成员移除决策记录。
- `docs/adr/016-workspace-wide-convergence.md`：已由 ADR-020 取代的工作空间收敛决策记录。
- `docs/adr/017-pairing-as-workspace-admission.md`：配对作为工作空间内部准入通道的责任边界。
- `docs/adr/018-domain-oriented-application-layout.md`：应用层按业务领域收口的所有权和目录边界。
- `docs/adr/019-device-specific-convergence-waiting-status.md`：已由 ADR-020 取代的等待设备状态记录。
- `docs/adr/020-membership-reconciliation-and-user-decisions.md`：设备上线成员核对、未确认移除决定和分叉关系隔离规则。
- `docs/adr/021-workspace-convergence-internal-boundaries.md`：成员收敛保持唯一负责人时的内部职责边界和整理规则。
- `docs/adr/022-user-initiated-join-supersession.md`：用户明确加入、后台恢复和旧加入安全取代规则。
- `docs/adr/023-legacy-profile-isolation-and-re-pairing.md`：旧资料升级后本机独立化和全部重新配对规则。
- `docs/adr/024-reset-space-as-device-management-reset.md`：用户明确重置全部设备关系并建立单设备空间的决策。
- `docs/specs/015-offline-first-member-removal.md`：已由 ADR-020 取代的成员移除说明记录。
- `docs/specs/016-workspace-wide-convergence.md`：已由 ADR-020 取代的工作空间收敛说明记录。
- `docs/specs/021-device-trust-reconciliation-product-contract.md`：设备信任完整查询、决定和产品动作边界。
- `docs/specs/022-current-member-runtime-scope.md`：当前成员运行范围、历史身份与普通授权的一致性规则。
- `docs/specs/023-durable-membership-proof-and-admission-activation.md`：历史验证材料、准入正式提交、激活门禁、恢复和旧数据迁移规则。
- `docs/specs/024-workspace-convergence-internal-boundaries.md`：成员收敛内部职责边界的实施顺序、验证与回归要求。
- `docs/specs/025-user-initiated-join-supersession.md`：用户再次明确加入时安全取代旧本机加入的分阶段实施规格。
- `docs/specs/026-legacy-profile-isolation-and-re-pairing.md`：旧资料独立化、关系清理和产品提醒的实施规格。
