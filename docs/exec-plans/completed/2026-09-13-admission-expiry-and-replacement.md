# 1. Overview

状态：已完成。六项产品决策与 S0–S8 均已实施；本文保留实施过程与验收证据，当前稳定规则以 ADR-026、Space Application 设计和前端接入边界为准。

研究基线：2026-09-13，`b8b96105` 加当时工作区。决策回写时仓库已整理至 `255e1a68`；第 4 节保留研究时的源码结论，不代表本次重新审计了实现。实施前须核对准入恢复摘要、成员更新投递和性能路径的最新结构。

本次解决旧配对因网络中断长期占用加入入口的问题。用户确认的规则是：正式决定前可以直接替换；正式决定后无论能否恢复成功，都允许本机终止，并撤销该次配对造成的成员资格。加入方和邀请方分别保存到期时间，各自处理到期，不要求对方在线或通知。

整个尝试从用户发起时起共用 5 分钟，连接、准备、正式应用和等待确认不各自重新计时。邀请方已经完成本机正式应用时，期限内显示“已完成配对，等待对方确认”；到期未确认则显示“已完成配对，但未被确认”，保留关系供用户自行移除，迟到的有效确认仍可转为“已完成配对”。已移除或收到合法放弃的关系不能复活。

这里的“到期自动撤销”针对加入方尚未完成或邀请方尚未完成本机正式应用的尝试，不包含邀请方仅缺加入方确认的保留关系，也不是成功成员的永久租约；“清理”包含删除临时材料和撤销已提交成员两种动作。已发布的成员历史不能当作没有发生过，必须以新的撤销事实结束。旧方案中“决定后必须先恢复成功”“到期不能撤销”的约束被本规格取代。

研究发现三个不能隐藏的边界：

- 协议决定持久化、正式数据应用、双方最终确认不是同一时刻。将临时数据放到文件不能使两台设备的最后写入成为一个事务。
- 本机未收到提交消息，不代表邀请方没有提交。不能按本机阶段直接删除全部证据。
- 一端保存成功、最后回复丢失、另一端到期，仍可能发生。两端尚未完成时可以各自到期；已经成功的一端不再运行本次到期任务，之后需要收到可验证的放弃/撤销事实才能知道另一端放弃。不能承诺它在永远收不到任何消息时也自动知道。本次已确认用邀请方“等待对方确认／未被确认”显式呈现这类不确定性，不能把“未被确认”当作自动撤销。

# 2. Goals

- 新的有效加入意图持久化后，旧尝试不得因对方离线而占用当前加入槽位；旧后台任务不能激活旧空间覆盖新意图。
- 从发起尝试起总计 300 秒；双方在正式决定前认证并保存原截止时间，不从认证、接收消息或重新启动时重新给 300 秒。双方各自到期执行角色对应动作，不依赖取消消息。
- 邀请方持久化并向宿主提供“等待对方确认／未被确认／已完成配对”状态；未确认到期仅变更显示，合法迟到确认可补齐，手动移除后不可恢复。
- 需要自动终止的尝试到期，或用户显式取消后，立即停止旧尝试的本机业务使用；已提交的成员通过精确指向该次加入的撤销处理，不能误删后来重新加入的同一设备。
- 每次中断后均能从密文持久记录确定继续、终止或补齐清理；不得先恢复配对成功才允许放弃。
- 临时协调记录与正式成员状态隔离；正式生效后保留正确的成员历史、安全材料和恢复证据。
- 保留用户剪贴板历史和资料解密能力；不以重置整个资料目录代替配对清理。
- 网络失败不阻止本机放弃；磁盘写失败、密钥不可用或状态损坏必须报告真实失败，不假报已经完成。

# 3. Non-Goals

- 不实现跨设备同步原子提交，不保证两端同时显示成功或取消。
- 不保证离线设备立刻收到撤销，不保证删除其已经获得的历史内容或旧密钥。
- 不把所有正常成员改成必须续期的短期成员，不新增中心仲裁服务。
- 不重写成员历史、空间代际切换或传输系统；不自动回退 LAN。
- 本仓实现不修改产品仓、发布版本或移动界面。后续 Engine 实现必须提供可恢复的用户状态；产品端显示这两个新状态属于明确的后续接入任务，不能以 Engine 测试通过宣称前台已完成。
- 不为了配对清理删除整个 userdata、MasterKey、历史密钥或普通资料库。

# 4. Current Architecture Context

以下路径相对仓库根目录。结论以对应源码为依据，不以旧时序图的阶段名代替证据。

Component: 加入与取消入口

Path: `crates/uc-application/src/space/admission/protocol/joiner/start_join/execute.rs`、`joiner/cancel_join/execute.rs`（后者同属 `protocol/`）。

Responsibility: 串行读取当前加入、验证新邀请、保存新意图；取消转换并持久化。

Relationship: 当前先调用旧状态的 `supersede`，再验证新邀请。Core 拒绝替换 Prepared；已提交取消返回 `TooLateCommitted`，Application 返回仍待处理。不是用户要求的本机终止。

Component: 准入状态规则

Path: `crates/uc-core/src/membership/space_admission/state/transition/`。

Responsibility: Joiner、Sponsor、CompletionHelper 的状态转换、消息次序及重放证据。

Relationship: Joiner 的 Active/PendingSettlement 不是双方结算完成；Sponsor 收到 CompleteAck 并保存 Settled 回复后才进入 Completed。Joiner 保存收到的 Settled 后才结束结算。

Component: 正式决定与正式应用

Path: `crates/uc-application/src/space/admission/protocol/sponsor/handle_prepared/execute.rs`、`crates/uc-infra/src/space/admission/sponsor/state.rs`、`sponsor/complete.rs`（后者同属 `space/admission/`）。

Responsibility: 先生成并保存已提交历史、安全材料与准确回复，再应用安全材料、成员投影和账本。

Relationship: `commit_prepared` 保存的是准入仓储内的决定材料，不等于已经写入活动 `membership_ledger_state`。随后 Sponsor activation 才应用正式数据。两者间必须有可恢复的补偿责任。

Component: 密文准入仓储

Path: `crates/uc-infra/src/space/admission/repository/{persisted.rs,codec.rs,recovery_index.rs}`、`joiner/activation_state.rs`（后者同属 `space/admission/`）。

Responsibility: 保存 profile 级协调状态、每次尝试的密文记录、邀请占用关系及恢复摘要。

Relationship: 当前 V3 已拆分小元数据与逐条记录；记录表是 `admission_repository_record`。现场恢复摘要仍在修改中。当前格式没有共同的尝试到期约定；ActivePendingSettlement 可清除 current 指针，因此只扫描 current 会漏掉未完成清理。

Component: 正式空间应用与切换

Path: `crates/uc-infra/src/security/v3_admission_space_transition.rs`、`space_control_generation/mod.rs`、`space_transition_activation/mod.rs`（后两者同属 `security/`）。

Responsibility: 构建目标空间控制数据，验证后提升活动清单，再切换连接池、会话和密钥绑定。

Relationship: 活动清单提升是本机切换边界；提升后不能直接把旧目录重新当作活动空间。源控制数据可能已被回收，普通资料代际仍保留。

Component: 成员撤销

Path: `crates/uc-application/src/space/membership/remove_space_member/use_case.rs`、`crates/uc-infra/src/db/repositories/space_security_store/`。

Responsibility: 校验当前历史和权限，提交 RemoveDevice、待执行安全效果与投递责任，再恢复效果。

Relationship: 现有公开入口按 device_id 查当前有效成员，且拒绝移除自己。不能直接用它完成旧尝试清理；必须复用完整撤销负责人，新增内部精确目标及授权契约，不能只删除关系表。

参考已有模式：SQLite 明确说明 WAL 下多个数据库不是一个整体原子事务；本设计因此使用持久意图、幂等应用和补偿，而不是把多处写入称为“最后一次原子写入”。见 [SQLite WAL](https://www.sqlite.org/wal.html)。MLS 将移除成员与更新后续组密钥结合，并把谁有权移除留给应用策略；本设计沿用这种职责分离，不以引用标准代替本仓的准入授权设计，也不替换现有安全协议。见 [RFC 9420，第 3 节](https://www.rfc-editor.org/rfc/rfc9420.html#section-3)。

# 5. Proposed Design

## Components

完整负责人仍为 `SpaceAdmissionProtocol`；Joiner、Sponsor、Recovery 是其私有分工，不新增一个平行的总调度器。

| 所有者 | 输入 | 输出及责任 |
| --- | --- | --- |
| Core 准入规则 | 当前记录、已验证消息、时间判断、终止原因 | 下一状态及必要效果；禁止终止记录重新激活 |
| Application 准入协议 | 新加入、取消、入站消息、恢复触发 | 完整本机结果；统一创建终止证据和清理责任 |
| Application 成员撤销负责人 | 精确成员实例、合法终止证据、已验证空间历史 | 撤销事实、本机安全应用结果和待投递责任 |
| Infra 准入仓储 | 条件更新、加密记录、到期索引 | 原子更新意图与旧记录；重启后返回待处理项 |
| Infra 空间切换负责人 | 意图代次、终止围栏、目标控制资料 | 激活或停止旧空间，保证旧任务不能覆盖新意图 |
| Engine | 既有完整能力与唤醒信号 | 装配和调度；不根据内部阶段编排清理 |

到期和撤销授权属于业务规则；原子写、密钥封装、计时器属于基础设施。重试退避只影响执行频率，不影响是否到期、是否撤销和是否允许替换；不得用“最近没有流量”等经验条件判断失败。

## Data Model

下列名称是拟议内部模型，不是现有 API。

| 数据 | 必需字段 | 生命周期及约束 |
| --- | --- | --- |
| AttemptDeadline | 协议版本、attempt_id、双方身份绑定、space_id、started_at、expires_at = started_at + 300 秒、期限策略版本、认证证明 | 发起时先持久化，认证后双方绑定原截止点；连接、解析、重试、重启、Helper 接手均不续期 |
| PairingConfirmation | 等待确认、超时未确认、已确认；正式应用凭据、确认消息证据、状态版本 | Sponsor/Helper 正式应用后保存；前两种不是终止状态，保留迟到确认的验证材料；不占新配对入口 |
| LocalJoinIntent | profile_generation、递增 intent_generation、当前 attempt_id | profile 级；每次有效新意图原子替换 |
| AttemptRecord | 角色、记录版本、协议进度、终止结果、决定证据、成员实例、到期约定、清理责任 | 业务负载密文；区分“尚未知是否远端提交”和“可证未提交” |
| AttemptTermination | attempt_id、space_id、加入身份、成员实例或提交绑定、原因、终止者、签名证据 | 在释放旧材料前保存；先不知道成员实例时仍可按尝试绑定后续 Add |
| CleanupObligation | 操作标识、精确目标、决定/应用进度、必要封装材料、待投递证明 | 不占当前加入槽位；完成本机处理前不能按原配对到期时间删除 |
| ExpirySummary | 可恢复的下一唤醒摘要及记录定位标记 | 非事实来源；业务时间与关联信息仍加密，索引损坏可由记录重建 |

成员实例必须绑定 `space_id + admission_id + Add 事件身份`（采用现有成员模型的等价稳定标识）。device_id 只作校验，不是删除目标。旧空间的清理必须使用旧空间作用域和授权，不能借用新空间的签名身份。

状态用三个正交维度表达，避免把“用户已经结束”和“后台全部清理”混为一谈：

- 协议进度：准备中、正式决定已保存、正式效果应用中、等待结算、已结算。
- 本机结果：进行中、已完成待确认、已完成未确认、已完成已确认、已取消、已到期终止、已被新意图替换、已被合法撤销。前两种确认状态在邀请方使用，不能与已到期终止共用同一枚举值。
- 清理责任：无、确认远端决定、提交撤销、应用本机效果、投递待补齐、已完成。

终止原因首次落盘后不可被迟到 Complete/Settled/CompleteAck 改回成功。“已完成未确认”没有终止围栏，允许有效迟到 CompleteAck 转为已确认；用户移除或合法放弃持久化后则禁止恢复。已确认记录停止自身尝试到期，但所有保留关系仍接受针对该次成员的合法放弃/撤销事实。重复消息返回已保存结果，不能延长期限。

| 角色及阶段 | 前台状态 | 到期动作 | 迟到消息 |
| --- | --- | --- | --- |
| 邀请方正式应用未完成 | 配对中 | 终止；已存在决定则精确撤销 | 只能补齐清理，不恢复配对 |
| 邀请方正式应用完成，尚未收到加入方确认 | 已完成配对，等待对方确认 | 保存“已完成配对，但未被确认”，保留关系与最小确认验证材料 | 有效确认变为“已完成配对” |
| 邀请方超时仍未确认 | 已完成配对，但未被确认 | 不重复撤销或重开期限，用户可自行移除 | 有效确认仍可完成；合法放弃则撤销 |
| 邀请方已收到确认并保存 | 已完成配对 | 不因本次期限撤销 | 重复确认幂等；合法放弃仍撤销 |
| 加入方尚未完成本机结算 | 配对中 | 自行终止，撤销本机已写关系，保存放弃责任 | 不因迟到成功回复复活 |
| 任一方已经移除或终止 | 已结束 | 只补齐清理 | 旧确认永远不能重建关系 |

这两个新增状态只描述配对确认程度，不代替现有安全资格检查，也不额外授予数据访问权限。

### 存储选择与最终持久化清单

已确认复用 userdata 内现有独立密文准入仓储，按记录保存每一步，不为每一步单独创建文件。它满足“中间状态不污染正式成员表”的目的，也允许新意图和旧终止记录在同一事务保存。不新建第二套阶段文件事实来源。

| 存储内容 | 准备阶段 | 正式应用阶段 | 成功后或终止后 |
| --- | --- | --- | --- |
| 准入元数据、`admission_repository_record`、恢复摘要 | 密文记录每次转换、准确回复、到期约定 | 保存决定、应用凭据、结算进度 | 已确认压缩；未确认保留成员绑定和迟到确认验证材料；终止保留最小防重放证据和未完清理 |
| `membership_ledger_state` | 不发布候选成员 | 保存有效 Add 历史及相关待执行效果 | 撤销追加 Remove，不删除原 Add 冒充从未发生 |
| `encrypted_relationship` | 不发布候选成员、信任及地址 | 从已验证材料应用对应投影 | 撤销精确成员及关联使用权限，不影响新实例 |
| `space_key_epoch_state` 与相关安全投递记录 | 临时材料只在隔离记录中 | 应用目标空间密钥状态及必要更新责任 | 撤销应用后续安全状态，投递可延迟 |
| `space_admission_credentials`、密钥封装/凭据材料 | 为本次准备且密文保存 | 安装正式空间凭据与所需密钥 | 仅清理失效尝试专有材料，保留历史读取所需材料 |
| 活动清单及空间控制代际 | 保持旧活动资料；可准备隔离目标 | 验证目标后提升清单并重新绑定 | 提升前丢弃未使用目标；提升后执行本机脱离，不能盲目回滚 |
| 普通剪贴板资料和历史文件 | 不迁移、不删除 | 保持原资料代际 | 取消和到期不得清空 |

“最后一步写表”具体定义为：双方分别在正式应用阶段写各自正式状态，而非等待一个可被双方同时知道的最终网络消息。双方最后回复用于结算；不宣称跨库、跨清单或跨设备原子。准备目录可包含尚未活动的目标数据库，但不得对外发布其候选成员。

## API / Interface

沿用公开加入/取消入口；在 Application 内部定义完整能力，不向 Engine 暴露阶段查询。

| 拟议能力 | 参数 | 结果及错误 |
| --- | --- | --- |
| replace_join_intent | 新邀请、预期意图代次 | 新意图已保存，或输入/存储/并发错误；不等待旧设备 |
| terminate_attempt | 精确尝试、取消/到期/替换原因、预期版本 | 本机已终止及剩余责任，或真实持久化错误；不返回“必须先配对成功” |
| recover_due_attempts | 有界批次、可信的本机时间观察 | 到期处理与下一次唤醒；包括所有角色和等待结算记录 |
| revoke_admission_member | 精确成员实例、终止证明、空间作用域 | 已撤销、已不存在、本机效果待补齐或保留 source 的失败 |
| detach_abandoned_activation | 意图代次、精确控制代际、终止凭据 | 未提升目标已清除，或提升后已停止旧空间使用；不清空普通资料 |

上述能力由现有完整负责人实现，不要求每项都成为新 trait。配对查询和订阅的既有稳定结果必须增加可区分的 `AwaitingPeerConfirmation`、`Unconfirmed`、`Confirmed` 用户状态（名称拟议），重启后从持久记录重建；宿主不自行按断线时长推断。状态变化需沿用既有通知路径，Engine 不为此查询内部步骤。公开取消结果需要能表达“本机已结束，远端尚待补齐”；若现有稳定类型无法表达，先形成兼容性变更清单，不把 Pending 当作成功。

## Workflow

### A. 新加入与期限建立

1. 验证新输入的语法和本机必要条件；明显无效输入不得取消旧尝试。短码沿用 Ready→Started 持久化后只查询一次的防重放规则，不能为重试重新兑换同一码。
2. 用户发起本次有效加入意图时生成独立 attempt_id，立即保存 started_at 和 expires_at = started_at + 300 秒；邀请码解析、连接和认证均消耗同一预算。短码尚未解析/身份未认证时虽没有双方认证约定，也不得另给准备阶段 5 分钟，更不得据此撤销他人成员。创建尚未被兑换的邀请不是这一次 Joiner 尝试的起点，邀请本身的有效期仍属原有规则。
3. 在本机短临界区内条件写入新意图、旧尝试终止围栏及其清理责任，再释放锁。若必须先签署旧放弃证明，则签署失败不能宣称旧清理已经保存；可重试，不发起网络等待。
4. 旧任务每次持久化和活动清单提升前核对意图代次；旧回包只用于补齐旧清理，不可推进新加入。
5. 新双方认证时校验并保存来自本次发起的 DeadlineContract；邀请方继承原截止点，只获得剩余时间，不从收到请求或认证完成重新计 5 分钟。已过期则拒绝开始，异常未来时间不能获批延期。邀请兑换有效期与尝试期限是不同字段，不混用。
6. 每次外发确认都在对应转换落盘后；接收重复消息只重放已经保存的答复。

### B. 正式决定与完成

1. 邀请方决定前检查期限、记录版本和终止围栏，保存绑定该尝试的 Add 决定及准确回复。此后旧 Add 不能凭删文件回滚，但仍可通过 Remove 撤销。
2. 正式应用由完整负责人驱动，每个跨存储效果有稳定操作标识与完成凭据。重启后先检查终止/到期，再决定补齐成功路径还是终止路径。
3. 加入方提升活动清单前最后检查意图与期限，检查和提升必须受同一串行化边界约束，不能只在网络调用前检查一次。
4. Joiner 的成功终态仍为正式应用完成且 Settled 已验证并保存。Sponsor/Helper 正式应用完成后持久化“已完成配对，等待对方确认”；CompleteAck 经验证、Settled 回复保存后转为“已完成配对”。未在原截止点前收到确认只转为“已完成配对，但未被确认”，不自动撤销；之后收到合法 CompleteAck 仍可保存 Settled 并转为已确认。确认处理不得被通用“已过期消息全部拒绝”挡住，终止/移除围栏仍优先。
5. 本机活动但未结算仍纳入到期扫描；按角色与正式应用凭据选择终止或仅标记未确认。不能根据 current 指针为空或界面已显示设备便回收证据。标记未确认后停止此记录到期唤醒，不占当前加入槽位，也不自动续期；保留最低限度的确认与放弃处理能力。

### C. 到期、取消或替换

1. 本机在启动、恢复前台、接收旧消息、接受新意图以及到期调度时检查。运行时安排最近截止时间；暂停或进程关闭期间不声称定时器运行，恢复后的首次业务动作先处理已到期记录。
2. `now >= expires_at` 时先按状态分流：Sponsor/Helper 已完成正式应用但缺确认，只原子保存“已完成配对，但未被确认”，不创建终止围栏、不执行下面的自动撤销；已确认则无到期动作。Joiner 尚未结算或 Sponsor/Helper 正式应用未完成，才原子保存终止原因、围栏及清理责任。显式取消/移除和有效新意图替换不必等到期，本机结果不依赖远端回复；其他未确认关系不会因开始另一条邀请而自动被移除。
3. 可证明未形成正式决定：清理临时业务材料与占用。保留邀请已消费/拒绝重放证据，不能把一次性邀请变回可重用。
4. 正式决定已知存在：按该 Add 创建撤销；必要时只补齐构造和应用撤销所需的内部材料，不发布成功、不恢复常规同步，也不要求旧设备在线。
5. 远端决定未知：保存已认证放弃证明和尝试围栏，本机仍结束。之后发现匹配 Add 时转成精确撤销；不得因本机停在 Prepared 就把所有证明删掉。
6. 加入方已经提升清单：先隔离该空间的业务使用，再由空间切换负责人完成本机脱离，保留普通资料。源控制代际已被删除时不能假造恢复旧空间成功。新意图可持久化；新空间正式激活须等待本机切换一致性恢复，不等待网络。
7. 旧清理作为独立持久责任重试；完成本机安全效果后不以远端未确认阻止新加入。密钥不可用等真实本机故障须单独报告，不能以无限“配对中”掩盖。

同空间重试与跨空间切换必须分开处理：同空间只停止本次新增成员实例的使用，保留不属于该次尝试的有效身份和关系；跨空间只撤销目标尝试的身份，不能顺便移除来源空间的其他设备。无法验证数据归属时报告状态不一致，不扩大删除范围。

### D. 撤销授权与消息接收

- 加入方只能签署放弃自身这一次尝试的证明，不能凭邀请或 device_id 任意撤销别人。
- Sponsor/Helper 创建正式 Remove 时仍须满足已有成员历史与签名权限。Helper 不能仅凭自报角色获得撤销权限；没有权限时保存并转交合法证明，不伪造账本写入。
- 新证明绑定协议版本、空间、双方认证身份、attempt_id 和对应 Add；授权与签名字节的完整规范须在 S0 固化，并加入篡改/跨尝试重放反例。
- 对未知 Add 可先保存拒绝继续的尝试记录，不能构造针对“该设备最新成员”的 Remove。匹配 Add 到达时，再由合法负责人在当前已验证历史上追加撤销。
- 本机成员身份已经停止后，普通成员消息通道可能不再放行。终止证明需走受限、独立认证的准入控制消息路径，只能提交该尝试的证明，不能借此获得数据访问。成功记录也必须接受合法证明。
- 并发撤销沿用成员历史冲突处理，不用最后写入覆盖历史。移除事实与本机密钥/权限效果分别持久化；安全效果未完成时禁止继续按旧成员发送业务内容。

### E. 时间、保留与兼容

期限是认证的绝对时间约定，但两端时钟不保证完全一致。进程内用单调时间限制剩余时长，持久化已观察的时间下界，重启不能因简单回拨重新获得完整期限；大幅前跳允许安全地提前结束，不能靠不断延期掩盖。跨关机且系统时钟任意回拨时，没有可信时间源就不能保证真实世界精确截止，必须公开该限制。总时长已确认为 300 秒；时钟偏差的安全检测阈值属于 S0 的工程验证项，不是额外宽限时间，也不允许给认证前后各 300 秒。

已确认：大准备材料在完成清理后回收；最小终止证据、邀请消费证据和撤销历史不能跟着配对期限一起删除。未建立可验证的重放拒绝下界前，不按任意天数删除最小证据；容量耗尽应在接纳新尝试前明确失败，不能静默丢弃安全证据。

旧记录无共同到期约定时标记 LegacyNoDeadline，不伪造对方已同意的期限。可以实现本机显式放弃，但不能宣称旧设备会自行到期。新版本能力在占用邀请前协商，不支持则结束本次配对并明确提示对方升级；不提供旧流程降级选择，也不得悄悄回到旧的长期阻塞流程。迁移只向前，保留旧邀请消费记录；迁移编号实施时重新分配，不能覆盖现场已有的 000002/000003。

# 6. Implementation Plan

## 6.1 实现方式与完成标准

从上到下指每个切片先确定调用方能得到的完整结果，再下沉到 Application 编排、Core 规则、Infra 保存/网络，最后回到 Engine 完整入口验收；不是先铺完所有接口、再铺所有存储。禁止只返回假状态的空实现。

实施按 S0 → S8 顺序完成，每片都在前一片验证通过后增加行为并形成独立提交。S1 是第一个最小端到端切片；S0 只完成契约与测试准备，没有冒充功能交付。整个实施没有增加永久产品配置层或第二套准入仓储。

每片完成必须同时具备：唯一完整负责人、既有公开调用路径、明确成功/失败结果、持久恢复责任、非零目标测试和实际输出证据。仅有类型能编译不算完成，无法安全独立开放的切片不得单独发布。

## 6.2 当前接线与具体修改契约

以下是在决策回写后重新读取的接线点，路径相对仓库根目录。新名称均为拟议名称，既有接口改变时同步迁移全部内部调用方，不保留无意义转调兼容层。

| 上层入口/结果 | 当前具体位置 | 修改方式及边界 |
| --- | --- | --- |
| 发起与取消加入 | `crates/uc-engine/src/operations/space/{join_space.rs,cancel_join_space.rs}`；Application `space/admission/model.rs` | 继续一个 Join/Cancel 动作；增加已本机终止的稳定结果，取消成功不能继续返回 cancel_requested=false 的 Pending |
| 邀请方设备行 | `crates/uc-application/src/space/membership/query_device_trust/{model.rs,use_case.rs,ports.rs}` | `DeviceTrustDevice` 增加可选配对确认摘要；按当前有效 MemberInstanceId 与尝试绑定匹配，不能按 device_id 单独关联 |
| Engine 对外结果 | `crates/uc-engine/src/contract/result.rs`、`operations/device/member.rs`；`bindings/{uc-engine-uniffi,uc-ohos-napi}` | `DeviceTrustRelationshipSummary` 增加 `pairing_confirmation`；三种值 AwaitingPeerConfirmation / Unconfirmed / Confirmed。加入终止原因完整映射到两个移动绑定。无可验证记录用 None，不把旧成员猜为未确认 |
| 配对展示读取 | Application `LoadCurrentJoinStatusPort`；Infra `space/admission/joiner/cancellation.rs` | 把已有读取能力改为一次加载所需加入及精确成员的配对摘要，调用方不逐设备查仓库。新的读取模型来自准入根出口，不穿透子模块 |
| 用户状态刷新 | `crates/uc-engine/src/contract/event.rs`、现有准入状态刷新路径 | 状态持久化后触发既有 RefreshRequired 路径，前台重新查询。不要为纯准入展示变化伪造 DeviceTrustChanged 的成员账本 revision，也不让相同账本 revision 挡住刷新 |
| 发起原子保存 | Application `protocol/joiner/start_join/{execute.rs,model.rs,ports.rs}`；Infra `space/admission/joiner/start_state.rs` | 扩展 JoinerStartMutation，创建记录、旧终止围栏、旧清理责任和 current/latest 指针同一事务保存；延续不透明 commit token |
| 最终确认 | Application `protocol/sponsor/handle_complete_ack/execute.rs`；Core `state/transition/sponsor.rs` | 沿用 settle_complete_ack 的 peer、前驱和准确回复校验；允许 Applied+Unconfirmed 进入同一合法结算，不新增宽松 ACK 旁路 |
| 到期恢复 | Application `protocol/recovery/recover_pending/{execute.rs,model.rs,ports.rs}`；Infra `space/admission/repository/recovery_index.rs` | 当前读取只返回 Joiner；新增按角色区分的窄到期任务视图，覆盖 Sponsor/Helper。Application 不因此获得完整 SpaceAdmissionAggregate |

现有 `DeviceTrustRelationship::ConfirmationPending` 源于成员历史核对，承担安全范围判断，不是本次配对确认摘要。两者独立；新增摘要只能描述状态，不能让 scope 放行。

查询读取的 ledger 快照与准入摘要不在同一事务：先取得已验证 ledger 中的精确成员实例集合，再按集合读取摘要；返回前若相关版本变化则整体有界重读。匹配不上的旧尝试不显示在新成员上，查询不写数据、不触发网络、不偷偷执行到期。到期由唯一 Recovery 负责人先行处理，状态变化再通知查询。

## 6.3 S0：固定公开结果与协议测试向量

状态：已实施。S0 建立稳定类型与验证字节，尚未把 V2 契约接入真实网络或准入仓储。

- **完整结果**：实现者拿到确定的输入、状态与错误映射，不再自行解释“完成”或“到期”。
- **文件**：6.2 所列结果模型的邻近测试；Core `space_admission/tests/{message.rs,state/settlement.rs,state/cancellation.rs}`；稳定准入设计及 ADR。
- **接口契约**：Join 输入不增加由宿主指定的超时；Engine 接收有效操作时采样起点，Application 保存同一起点。复用现有 `ClockPort::now_ms()` 与 tokio 定时设施，不引入新计时依赖。排队、邀请解析、认证均计入 300 秒；每次有意发起的全新尝试才产生新 id 与新预算。
- **消息契约**：首次认证时尚不知道正式 Space 与 Add，因此 V2 分两层绑定。`AdmissionAttemptContractV2` 覆盖协议用途、attempt_id、invitation_id、started_at/expires_at 及双方通道身份；`AdmissionMemberBindingV2` 再用前者摘要绑定 Space、member_instance 与 Add event。未知提交的放弃证明先绑定尝试，发现 Add 后只能补齐匹配的成员绑定，不能另选成员。签名重用现有身份/成员签名能力，按用途作域隔离。
- **兼容契约**：网络格式版本与磁盘记录版本分别管理；不得直接给旧 postcard 结构加字段后声称旧字节仍可读。选定可区分的新 wire 格式，旧记录经版本化 decoder 向前迁移；旧端在消费邀请和提交成员前明确拒绝。
- **验证**：Core 6 项定向测试通过，固定摘要为 `77c8c49a972eab5eef57fbfb84f1679527ef8a6caeddcf122d37af47f4cba3e6`；覆盖 300 秒、缩短/续期、负时间、溢出、相同 peer、编码往返/尾随字节、每个首次绑定字段以及 Space/member/Add/跨尝试变化。Engine 8 项设备结果转换测试与 48 项公开序列化测试通过；两个移动绑定均有终止状态映射测试。旧 V1 准入测试随 Core 全部 88 项通过。发布摘要不含凭据或内部阶段。
- **最终检查**：workspace 全目标检查、格式检查、Rust 规范检查、Engine 仓储边界检查和 diff 检查均通过；仅保留仓库原有的 HarmonyOS 测试未使用导入警告。真实网络、重启恢复和设备矩阵属于后续切片，本片记为跳过。
- **风险/停点**：若现有凭据无法证明加入方“放弃自身这次尝试”，必须先补全认证契约，不能靠 device_id 或 transport 身份跳过。

## 6.4 S1：发起后到期、本机结束、可以重新发起

状态：已实施。依赖 S0。这是第一个贯通公开入口、真实密文保存和重启恢复的切片，限制为尚未向对方发出可能导致正式决定的消息。

- **调用与结果**：Join → Pending；从发起起第 300 秒 → 本机已到期；随后 Join B → B Pending，A 不占槽位。显式 Cancel A 无须网络。
- **文件**：`protocol/joiner/start_join/`、`resolve_invitation/`、`cancel_join/`；Core `state/{joiner.rs,terminal.rs,transition/joiner.rs,persistence/}`；Infra `repository/{persisted.rs,codec.rs,token.rs}`、`joiner/{start_state.rs,cancellation.rs}`。
- **字段/转换**：记录保存 started_at_ms、expires_at_ms、intent_generation、terminal_reason。Core 以注入 now 和持久记录输出完整转换，不直接读系统时钟；checked_add 计算 300000 毫秒，溢出拒绝。
- **事务**：新意图与旧结束记录共同提交；存储错误零成功回复。短码 Ready→Started 后只解析一次，解析时间消耗原预算，重启 Started 不重复兑换。
- **恢复**：保存最小终止证据后才删除旧私密准备材料；启动、恢复前台和新 Join 前处理本机到期。对需要正式撤销的旧状态仍显式报告尚未实现，不假报此片已经支持。
- **验证**：临时真实资料目录中发起 A，网络不可用，推进 299999/300000 毫秒并重开仓储；检查 A 结束、B 可发起、普通数据及邀请防重放证据保留；注入新意图事务失败，确认 A/B 不发生半替换。
- **风险/发布**：Prepared 不能在此片当成确定未提交；此片只提供隔离端到端证据，不开放半套新协议。
- **实施结果**：Engine 在接受 Join 时记录唯一五分钟期限；Application 在发起、恢复和短码解析返回后先判断到期；Core 只允许解析邀请、等待认证和 Candidate 阶段本机结束。到期、取消和新意图替换均先保存结果，旧 current 指针随后释放。Prepared 及以后仍沿用旧安全限制。
- **保存与调度**：尝试时间线和终止原因进入新的 Core 记录版本，正文继续由现有准入仓储整体加密；旧记录仍可读且没有虚构期限。唯一成员维护运行期持有最近一次准确截止唤醒，暂停和恢复不重置期限。
- **验收证据**：Core 282 项测试、Application 全部 807 项测试和准入 50 项测试、Infra 真实 SQLite 准入 26 项测试及准确截止唤醒测试通过；Application 另有 1 项既有测试按原设置忽略。真实 SQLite 覆盖 299999/300000 毫秒边界、关闭后重开、A 到期后 B 使用不同身份发起，以及注入事务失败时 A/B 不发生半替换。真实网络、实体设备和产品前台验收仍记为跳过。

## 6.5 S2：双端统一期限与邀请方确认状态

依赖 S1；贯通一条正常双端配对与 CompleteAck 丢失场景。

状态：已完成。双端统一期限、邀请方三种确认状态、原期限到期、迟到合法确认、精确成员关联、重启恢复和新版网络拒绝旧端均已接通并验证。

- **调用与结果**：同一个 Join 完成正常握手；邀请方正式应用完成即能查询 AwaitingPeerConfirmation；原截止点到达改 Unconfirmed；合法迟到 CompleteAck 改 Confirmed。
- **文件**：Core `message.rs`、`state/{sponsor.rs,helper.rs,transition/sponsor.rs,persistence/sponsor.rs}`；Application `sponsor/{handle_join_request,handle_applied,handle_complete_ack}/`；Infra 对应 Sponsor adapter；6.2 的查询和 Engine 映射。
- **字段/保存**：确认摘要和 activation_receipt 在同一次准入转换内保存；Applied 已包含正式应用凭据，新增确认字段必须与该凭据匹配。Unconfirmed 保留准确回复、前驱与成员绑定；状态不写进 membership ledger。
- **期限传递**：Sponsor 验证继承 Joiner 发起的原截止点；剩 60 秒就是 60 秒。新协议尚未认证的请求不能靠自报未来时间延长预算。拒绝错误期限时不消费邀请、不发布成员。
- **ACK 判定顺序**：认证与精确绑定 → 已移除/终止围栏 → 重放校验 → 根据消息种类判断期限 → 合法 CompleteAck 结算。Unconfirmed 接受迟到确认是白名单分支，不允许迟到 Prepare/Commit 重新开始。
- **查询与刷新**：状态保存成功后发刷新；模拟通知丢失，重新查询/重启仍正确。成员历史 revision 不因文案变化而递增。
- **验证**：双端共享起点、单独时钟；丢 CompleteAck 后到期重启邀请方保留成员，迟到 ACK 成功，错误 peer/前驱拒绝；同时保留现有 sync_state，不把 Unconfirmed 变成放行条件。
- **风险/发布**：此片还不能覆盖 Joiner 已决定后的到期撤销，必须继续 S3–S6 后才开放。
- **当前实现**：新版首次认证把加入方保存的原始起止时间、邀请和双方连接身份共同认证；整条消息链保持新版标记，不允许新认证混用旧消息。邀请方正式应用时在同一条准入记录中保存精确成员与加入事件绑定，原期限到达后只把等待确认改为未确认，合法迟到确认沿用原有精确前驱和重放校验完成结算。设备查询只把摘要关联到当前成员历史中的同一成员实例和同一 Add，不改变原同步状态；状态变化沿用现有准入刷新通知。旧记录继续可读，但没有期限的旧记录不会被虚构成新版尝试。
- **验证结果**：Core 相关 95 项、Application 准入 52 项、设备查询 8 项、Infra 准入存储 26 项、真实本机网络交互 6 项全部通过；全仓编译、格式、Rust 规则、架构与隐私门禁、差异检查通过。现有 HarmonyOS 测试仍有与本片无关的未使用导入警告。实体设备和跨真实网络验证跳过，留到 S8。

## 6.6 S3：把已有移除能力收紧为精确成员撤销

依赖 S2；先把撤销作为完整业务能力验证，再接到自动到期。

状态：已完成。按设备移除在首次读取时固定精确成员，准入撤销同时绑定原空间、准入和 Add；并发重试与重复调用都不再重新解析到后来同设备的新成员。

- **负责人**：现有成员移除 case；不在 admission 复制 Remove 的签名、账本与安全效果流程。
- **文件**：Application `membership/remove_space_member/{use_case.rs,model.rs,revocation.rs,tests.rs}`、`membership/signing/`、`membership/ledger/`；Core 成员历史签名/目标规则；Infra 原安全效果 adapter。
- **输入**：内部 `AdmissionRevocationTarget { space_id, admission_id, member_instance_id, add_event_id }` 与已验证原因证明。公开按设备移除先在同一 verified snapshot 固定精确目标，再进入同一个完整执行函数；原 SelfTarget 拒绝保持不变。
- **输出**：Removed、AlreadyAbsent、LocalEffectsPending 或有来源的错误。无权签名不得返回 Removed；未知 Add 不得返回 AlreadyAbsent 来丢弃责任。
- **事务**：核对 current history digest + revision + 精确实例后保存 Remove 和 Prepared effect、受限投递责任；重复动作按稳定 operation_id 去重。不能先删关系再签历史。
- **重试**：CAS 冲突重读当前历史，但目标始终保持原实例，不能重新解析成该设备最新实例。本机效果失败继续保留责任；网络未确认不挡后续本机动作。
- **验证**：A 被移除后同设备加入 B，重放 A 撤销不伤 B；目标永久离线，本机 Remove/安全效果完成；无权、错空间、错 Add、并发 Remove 分别验证。
- **风险**：Joiner 自我放弃证明不是普通 Remove 签名权限；它由有权的 Sponsor/在册成员转换，Joiner 本机脱离由 S6 负责。
- **当前实现**：公开移除和准入撤销复用同一个完整成员移除流程。目标已移除时必须找到原 Remove 才返回 AlreadyAbsent；未知 Add、错误空间、错误成员或无有效本机签名都不会丢弃责任或返回成功。正式 Remove 与待执行安全效果同一次提交，网络未确认不阻塞本机结果。
- **验证结果**：六项成员移除定向测试通过，覆盖正常移除、冲突重试、旧实例移除后同设备重配、错误空间、错误 Add、无效本机凭据和本机效果待完成；全仓编译、格式、Rust 规则、架构与隐私门禁、差异检查通过。实体设备和跨真实网络验证跳过，留到 S8。

## 6.7 S4：已决定与远端决定未知时终止

依赖 S3，接通用户 Cancel、新 Join 替换和到期的同一终止规则。

状态：已完成本机终止与持久清理责任。Prepared 保存“远端是否提交未知”，收到 Commit 后的阶段保存精确成员目标；取消、到期和新意图都不再等待旧设备在线。放弃事实的可靠投递属于 S5，跨空间活动状态的最终隔离属于 S6。

- **文件**：Core `state/transition/{joiner.rs,sponsor.rs,terminal.rs}` 与角色 capability；Application `joiner/cancel_join/`、`joiner/start_join/`、`recovery/recover_pending/`；Infra 准入仓储与取消 adapter。
- **内部结果**：Core 产生 `KeepUnconfirmed | TerminateWithoutCommit | TerminateWithKnownCommit | TerminateWithUnknownCommit` 等价的角色变化，Application 只接收 Joiner/Sponsor 能力与完整变化，不自行拼 aggregate。
- **持久责任**：AttemptTermination 与 CleanupObligation 属于旧 attempt 的密文记录，profile current/latest 可以指向 B。旧 A 与 B 同库事务持久化；旧网络响应只能补齐 A 的清理。
- **执行顺序**：创建并持久化自身放弃证明/围栏 → 停止 A 的普通业务使用 → 已知 Add 调用 S3；未知 Add 保留待匹配责任 → 释放 A 对加入入口的占用。不得先删凭据导致无法证明放弃。
- **邀请方分支**：Applied 未确认到期只走 KeepUnconfirmed；Received 合法放弃或手动移除才走撤销。Sponsor 尚未完成正式应用则到期终止，即使决定已经持久化。
- **恢复**：所有恢复先读终止围栏；旧 Prepared 等不到提交回复也不阻止 B。存储/密钥失败返回具体本机错误，不无限 Pending。
- **验证**：Sponsor commit 已落盘但回复丢失；Joiner A Prepared→Cancel/Join B→晚到 A Commit；A 永不激活，B 不覆盖；已知/未知两条路径都留下完整可恢复责任。
- **风险**：本片本机清理对活动空间的最终隔离由 S6 完成；不得把“已保存终止意图”当作“所有本机安全效果已完成”。
- **当前实现**：新版加入在认证后保存尝试摘要。Prepared 本机终止时保留认证连接资料并标记远端提交未知；Committed、Applied、Activating 从准确 Commit 中保存原 Space、成员实例和 Add 绑定。取消、原期限到期和新 Join 替换共用这条转换，结束记录与 current 指针更新由原准入仓储提交；并发晚到 Commit 因版本变化不能覆盖终止结果。分支内各切片产生的过渡布局已在发布前统一收敛到 V2；主线原有 V1 仍可读取。旧格式的 Prepared、Committed、Applied、Activating 和 Cancelling 在用户取消或再次加入时保存为本机已结束并释放 current 指针，不补造共同期限、远端撤销或清理责任；迟到旧消息不能重新占用新尝试的入口。产品调用方只发起一次加入，不自行查询、取消和重试旧请求。
- **验证结果**：Core 准入 97 项、Application 准入 70 项、Infra 真实 SQLite 准入 26 项通过；覆盖 Prepared/Committed 取消、新 Join 替换、到期、重启编码、上一版记录兼容、晚到 Commit 竞态和旧记录继续认证。全仓编译、格式、Rust 规则、架构与隐私门禁、差异检查通过。放弃消息、跨空间退出、实体设备和跨真实网络验证跳过，分别留到 S5、S6、S8。

## 6.8 S5：放弃消息可靠投递与迟到消息隔离

依赖 S4，完成双方在失联后恢复通信时的处理。

状态：已完成 Sponsor 接收与 Joiner 可靠投递。终止记录固定同一条放弃消息和准确回复；对端保存围栏后，将原成员交给 S3 移除流程。Helper 接手、跨 Space 清理和有界全角色索引仍分别属于 S7、S6、S7。

- **文件**：Core 新版消息/重放规则；Application `sponsor/handle_authenticated_message/` 与 Recovery；Infra `network/iroh/space_admission/`、`space/admission/recovery/`。
- **输入/授权**：受限 AttemptTermination 只能声明自身绑定尝试已放弃；接收方认证消息、校验用途签名及尝试身份，不能以此读取全量成员历史或剪贴板。Helper 须验证自己的在册权限，不能只凭角色字段提交 Remove。
- **保存与回复**：认证后先保存终止证明、已知目标/待发现决定责任，再回复已接收；重复消息只重放结果。发送方收到可信接收回复后可结束该次投递，但本机安全效果责任与永久防重放证据分别判断，不能一起删除。
- **先后乱序**：termination 先于 Add → 保存尝试围栏；Add 后到 → 验证绑定后精确撤销。CompleteAck 先于 termination → 短暂已确认，再合法撤销；termination 先到 → ACK 不得复活。
- **通道**：复用准入初始/继续认证设施，加入成员身份已停止时仍能投递最低限度放弃证明；不依赖普通 member scope 放行。不得绕过成员认证去发送普通数据。
- **验证**：独立端点模拟回包丢失、重复、乱序、撤销端已退出普通 scope、错误签名及恶意重放；断网时没有假全局成功，恢复后只影响原实例。
- **风险**：迁移后凭据已丢失的旧记录不得伪造证明；保存可诊断失败，不能扩大权限。
- **当前实现**：新版 Joiner 在 Prepared、Committed、Applied 或 Activating 本机终止时，把绑定原尝试、原前序和可选精确成员的 Abandonment 与继续认证资料一同保存。Recovery 每次复用完全相同的消息；Sponsor 只接受新版协议、准确认证对端、尝试摘要和当前阶段前序，先保存 Abandoned 准确回复及清理责任。Committed 直接保留精确成员绑定；Applied 缺少 Space 绑定时保存成员实例与 Add，恢复时只在当前已验证历史中核实，不按设备重新选择。成员已不存在或本机已失去成员资格时结束本地责任，不要求旧设备上线，也不保持无限配对中。
- **隔离结果**：重复 Abandonment 只重放同一回复；旧阶段 Abandonment、错误前序、错误尝试或错误成员绑定均失败。Joiner 收到 Abandoned 后仅结束消息投递，保留终止围栏。上一版终止记录继续可读；没有保存控制路由的旧记录不伪造可投递消息。
- **验证结果**：Core 全部 287 项、Application 全部 821 项通过（另 1 项既有忽略）、Infra 真实 SQLite 准入 27 项及 Iroh 准入网络 17 项通过。覆盖回包丢失后的同消息重试、重复回复、旧阶段迟到、已知与待核实撤销目标、重启后继续撤销、原成员消失和同设备新实例隔离。全仓编译、格式、Rust 规则、架构与隐私门禁、差异检查通过；现有 HarmonyOS 测试仍有与本片无关的未使用导入警告。实体设备、跨真实外网、Helper 接手与跨 Space 已激活退出跳过，留到 S6–S8。

## 6.9 S6：跨空间激活期间取消，保证旧任务不覆盖新意图

依赖 S5；这是开放“任何阶段都可取消”前的最后安全核心切片。

状态：已完成。终止记录保留准确本机切换计划；最后启用前在同一切换关口重新核对当前准入意图。取消返回前，未提升目标被丢弃，已提升目标先保存停止使用事实，再断开控制库和安全会话。重启后的当前空间查询也会拒绝暴露该目标。

- **文件**：Infra `security/{v3_admission_space_transition.rs,space_transition_activation/mod.rs,space_control_generation/mod.rs}`；Application `protocol/joiner/activate_complete/` 的 model/ports；准入终止仓储。
- **输入/输出**：目标控制代际、intent_generation、已保存终止凭据 → 目标未提升已丢弃 / 已提升空间已停止使用 / 本机恢复待补齐。
- **串行化**：意图最终校验与 manifest 提升必须共享同一完整切换约束；不能检查完后释放锁，再让旧任务提升。网络等待不持有该锁，已开始本地提交不因取消 future 被半途打断。
- **提升前**：证明目标没有被活动 lease 使用后才清理，不改来源空间。提升后：先持久化停止使用意图并限制 scope，再由现有切换负责人完成脱离；不凭源目录存在与否决定回滚，不调用 ResetSpace。
- **跨库责任**：准入意图、控制数据库、清单、会话不是单事务；每个效果以 operation_id/代际匹配重开校验，完成凭据成功后才回收下一份旧材料。
- **验证**：对清单提升前后、数据库绑定前后、密钥绑定前后逐点崩溃；重启旧尝试不恢复业务，新意图不会被覆盖，普通历史和历史密钥可读。测同空间重试、跨空间加入、已有旧清理三类。
- **风险**：真实本机损坏仍可能阻止新空间正式激活；允许保存新意图并报告本机恢复，不承诺存储不可用时也能立即完成。
- **当前实现**：Joiner 的准确切换计划随终止责任写入新版密文准入记录，恢复入口先处理本机隔离再进行网络投递。每次正式提升或提升后恢复都携带原始计划摘要，在 manifest 切换锁内重新读取准入记录；记录已终止、已替换或计划不一致时不再提升。取消与到期先提交终止事实，再进入同一切换锁：来源仍活动时只删除未使用目标，目标已经活动时先写入加密停止标记，再切到空控制库并清除活动安全会话；另一个新目标已活动时旧清理不触碰它。
- **验证结果**：Core 准入 99 项、Application 准入 75 项通过；Infra 真实 SQLite 意图绑定 1 项和 V3 Fresh、SameSpace、CrossSpace 3 项通过。覆盖精确计划重启保留、错误或已结束意图拒绝、提升前删除、提升后停止使用、旧目标不影响后来活动目标，以及停止标记重启后不再暴露当前空间。全仓编译、格式、Rust 规则、架构与隐私门禁、差异检查通过；现有 HarmonyOS 测试仍有与本片无关的未使用导入警告。实体设备和跨真实网络验证跳过，留到 S8。

## 6.10 S7：双角色到期调度、状态保留与迁移收口

依赖 S6。此前切片的手动恢复测试升级为完整运行期自动触发。

- **文件**：Application `protocol/recovery/recover_pending/`、`membership/maintenance/{runtime.rs,use_case.rs,ports.rs}`；Infra `repository/{recovery_index.rs,codec.rs,persisted.rs}`；Engine 既有装配；新迁移。
- **摘要内容**：角色、attempt_id、expires_at、下一完整动作类别、正文 content_token 均经现有密文封装；摘要和正文条件提交。查询待办采用有界批次与游标，不能只扫描 current，也不能每 100ms 解密全部历史。
- **调度**：启动时分批读取小摘要建立最近截止时间，运行时状态变化更新内存调度；到点唤醒唯一 Recovery，不建第二个维护 runtime。S2 Unconfirmed 不再安排到期任务，但保留消息接收摘要。
- **公平性**：本机到期处理先于本轮网络交换；单个离线 peer 不占用整轮。暂停停止新网络工作，恢复前台先处理过期，时钟变化重新核对原截止点。
- **回收**：结束记录不占 active 配额；大准备材料与最小结束证据分别回收。未确认关系保留验证迟到确认必需材料；不能把普通结束 tombstone 套上去。容量不足拒绝新的接纳，不删除现有安全证据。
- **迁移**：旧记录无期限标 LegacyNoDeadline，不以更新时间补造 started_at；旧 record/summary 密文向前迁移，失败原资料可恢复，旧端提示升级。迁移编号按最新目录分配。
- **验证**：Sponsor-only、Helper-only、current=None 仍到期；大量无关记录、缺摘要重建、睡眠跨期、时钟前跳/回拨、进程重开；测实际读取规模与 idle 唤醒次数。

当前实现：恢复仓储用同一次加密摘要扫描统一返回 Joiner 待办、Sponsor 到期、Sponsor 撤销和最近期限；摘要绑定角色、准入标识、正文版本、期限类别和下一完整动作。扫描每批最多 64 条，以不透明游标继续；正文只在动作当前可执行、摘要缺失或旧摘要迁移时解密。旧摘要在同一数据库快照内从原正文重建，损坏或密钥不可用时保留原记录并失败关闭。没有双方共同认证期限的旧记录明确归为 LegacyNoDeadline，不用更新时间补造期限。历史 CompletionHelper 记录没有生产创建入口和共同期限，因此只按这一旧记录规则保留；当前可达协议的到期双方是 Joiner 与 Sponsor。

唯一成员维护 runtime 在启动、恢复和状态变化后读取最近期限；后来的期限不能推迟已经安排的更早唤醒。到点仍由同一 Recovery 先处理本机结束和撤销，再进入 Joiner 网络交换。Sponsor 在完成前到期会自行结束；尚未写入成员时直接收口，已写入成员时保存精确撤销依据并由下一维护轮完成撤销。Sponsor 已进入等待对方确认时只转为 Unconfirmed，保留关系和迟到确认材料。Joiner 本机空间隔离完成后删除大型切换计划，继续保留最小终止证据和未完成的放弃投递。

验证结果：Core 准入 101 项、Application 恢复 25 项、Infra 真实 SQLite 准入 28 项及仓储 12 项通过（另 2 项显式性能/现场探针忽略）。覆盖超过两个索引批次的 130 条无关记录、缺失与旧摘要重建、current 为空、重启恢复、邀请方未完成到期、等待确认到期、已写入成员的准确撤销、本机收口后材料回收，以及更晚期限不能推迟最近唤醒。全仓编译、格式、Rust 规则、架构与差异门禁通过；实体设备、跨真实网络和产品前台验收跳过，留到 S8 与产品仓接入。

## 6.11 S8：完整接线、公开兼容与交付

依赖 S7；只有这一片通过才能宣告完整支持新配对规则。

- **文件**：`uc-engine` 契约序列化与操作测试、`crates/uc-engine/tests/space_membership_auto_pairing_e2e.rs`；已有移动绑定契约测试；稳定设计/ADR、本文与架构圣经。
- **双端路径**：成功、5 分钟内无连接、准备后中断、已决定后取消、等待确认超时、迟到确认、未确认后手动移除、同设备新实例、旧端升级提示均从 Engine 完整操作触发。
- **宿主契约**：确认新增枚举/字段在各绑定序列化完整，不由绑定重写规则。前台接入验收清单包括两条准确文案、刷新不受旧 ledger revision 去重、重启重查、手动移除；产品仓接入另列交付，不在本仓夹带实现。
- **验证顺序**：先列出并确认目标测试非零，再执行 Core → Application → Infra → Engine 定向测试，最后全仓门禁。共享 target 串行 Cargo，不为切片另建 target；有其他构建占锁时等待，不清缓存或绕过锁。
- **可执行基线命令**：`cargo test -p uc-core --lib space_admission --locked -- --list`、`cargo test -p uc-application --lib space --locked -- --list`；确认覆盖后去掉 `--list` 并用 `--test-threads=1` 运行；Infra 新测试以确定名称先 list 再运行；Engine 用 `cargo test -p uc-engine --test space_membership_auto_pairing_e2e --locked`。每片新增测试名与实际结果填回该片，不写“测试通过”替代输出证据。
- **收口**：用新正文取代稳定设计中“Commit 后只能恢复成功/取消只在边界前生效”的旧规则；保留历史 ADR 并标记被取代，不把阶段切片作为第二套长期事实来源。
- **完成边界**：Engine 与两个移动出口完成接线和本机双端验收。真实设备、跨真实外网和产品前台仍记为跳过，不由模拟时钟和双端 host 代替。

当前实现：Engine 继续只公开完整加入、取消、成员查询和移除操作；三种配对确认状态由同一设备结果返回，两个移动出口直接序列化这份结果，不复制业务规则。新增双端验收从公开操作完成配对，确认邀请方查询为已确认，重启后仍保持；随后移除该成员并让同一设备以新成员实例重新加入，旧移除不影响新关系。五分钟到期、中断、迟到确认和撤销竞态继续由前述可控时钟及持久仓储测试证明，未向产品暴露调时入口。

验证结果：Engine 公开合同 1 项、UniFFI 合同 1 项、HarmonyOS 合同 1 项和新增真实双端流程 1 项通过；完整 Engine 双端套件、Core、Application、Infra 及全仓门禁结果见本节最终验收记录。实体设备、跨真实外网和产品前台验收跳过。

# 7. Edge Cases

| Scenario | Expected behavior | Implementation |
| --- | --- | --- |
| 空记录、无当前加入 | 无副作用；仍查其他角色待到期项 | 摘要索引不等同 current 指针 |
| 新邀请格式错误 | 不取消有效旧尝试 | 本机输入校验先于意图替换 |
| Prepared 回包丢失，Sponsor 已提交 | 本机终止但保留撤销责任 | UnknownCommit 与签名放弃证明，不猜测未提交 |
| Sponsor 已成功，Settled 丢失 | Joiner 到期退出；Sponsor 接收合法放弃后撤销 | 明确允许短暂分歧，不无限增加 ACK |
| 邀请方缺 CompleteAck，5 分钟已到 | 保留关系并显示未被确认；不占新入口 | 按角色分流，不创建终止围栏 |
| 未确认后收到迟到 CompleteAck | 验证后转为已完成配对 | 保留最小认证证据，不受通用过期拒绝拦截 |
| 用户移除与迟到确认并发 | 移除生效后不可恢复；确认先到也不免除随后移除 | 同一记录版本下串行判定，移除围栏最终优先 |
| Complete 与到期同时到达 | 一个条件更新胜出，终止后不能成功 | 记录版本、意图代次、期限在同一决策边界核对 |
| 取消与清单提升并发 | 不得终止后再激活旧空间 | 共享切换串行化边界，提升后走脱离 |
| 同一设备重新加入后收到旧撤销 | 新成员不受影响 | 按 Add 实例和空间匹配 |
| Helper、Sponsor 同时处理 | 不双重扩大撤销范围，不覆盖分叉 | 幂等操作标识与现有历史冲突规则 |
| 磁盘满、密钥锁定、密文损坏 | 报告本机未能安全完成，保留可恢复资料 | 保留 source；不删除原记录或返回假成功 |
| 邀请方切换到其他空间 | 旧清理不能借新空间身份执行 | 旧作用域封装责任；授权不可用明确待处理 |
| App 关闭/手机挂起直到过期 | 再运行时先处理过期，不先继续激活 | 启动和恢复前台检查 |
| 时钟回拨/前跳/极端期限 | 不续期，不溢出；不接受超过策略上限 | 单调剩余时间、持久时间下界、checked 时间计算 |
| 大量无效消息/重复终止 | 不产生无限记录或延长有效期 | 认证后持久化、幂等定位、有界接纳与明确容量错误 |
| 旧记录没有期限、旧端不支持 | 不编造双端保证，不静默降级 | 前向迁移与能力协商 |
| 清理中掉电、重复重启 | 不复活成员、不丢普通资料 | 每个副作用有凭据，删除最后执行 |

# 8. Testing Strategy

## Unit Test

每项按“输入 → 操作 → 预期结果”执行，时间与失败点可注入。

- 全角色每个协议进度、截止前/等于/之后 → 确认消息与到期分别先执行 → 未完成端终止不可逆；邀请方正式应用完成则转未确认且保留关系，迟到确认仍能完成。
- 邀请方未确认、随后用户移除 → 重放原 CompleteAck → 仍已移除，不恢复关系；在有效确认前后分别执行移除，最终都移除。
- 发起后解析耗时 120 秒、连接认证耗时 120 秒 → 双方继续并在第 300 秒检查 → 只剩 60 秒，不获得新的 300 秒；重启与重连同样不延长。
- A/B 不同 attempt_id、同 device_id、不同 Add → 旧终止或 Remove 重放 → B 不受影响。
- 已绑定期限 → 重连、重启、Helper 接手 → expires_at 不增加。
- 已成功 Sponsor 与 Joiner 签名放弃 → 验证并接收 → 只撤销绑定 Add；错身份、错空间、篡改期限全部拒绝。
- 无本机 Add 的 Prepared → 终止再收到匹配决定 → 从未知提交清理转入精确撤销，不转为成功。
- 旧格式、空仓储、超大时间、溢出代次 → 迁移或条件转换 → 明确结果，无 panic、无默许过期。

## Integration Test

- 两个独立资料目录和时钟 → 完整配对、关闭并重开 → 双方结算成功，正式表与密钥/清单一致，推进时间不自动撤销成功成员。
- 在 Prepare、Prepared、Commit、Complete、CompleteAck、Settled 各消息前后分别断网 → 从同一发起点推进 300 秒并重启 → Joiner 未完成则终止；Sponsor/Helper 正式应用未完成则终止并撤销已有决定，已完成但缺确认则保留关系并显示未确认。每个分支不等对方回复。
- Sponsor 正式应用完成、CompleteAck 丢失 → 原截止点后重启 → 查询与订阅显示“已完成配对，但未被确认”；迟到有效 CompleteAck 到达后显示“已完成配对”，再重启仍保持，普通新配对不被这条记录占用。
- 两个新增状态 → 产品宿主接入查询与订阅并实际运行前台 → 文案、到期变化、迟到确认与手动移除符合状态表；未执行产品接入时单列未完成，不能以 Engine 验证替代。
- Sponsor 保存决定后丢回复 → Joiner 新加入 B → A 的旧决定晚到 → B 不受覆盖，A 得到撤销，不显示 A 成功。
- Sponsor 保存成功后丢 Settled → Joiner 到期、断网 → Joiner 本机可新加入；恢复控制通道后 Sponsor 撤销 A。始终断网时不把 Sponsor 收敛标记为通过。
- 每个正式表写入、清单提升、会话重绑、终止落盘和材料删除处注入崩溃 → 重启 → 成功或终止方向一致，普通历史逐条可读。
- 邀请方离线/目标设备永久离线 → 另一本机有权限执行撤销 → 本机安全效果完成且新加入不等远端投递。
- 三端含 Helper、乱序 Add/Remove、历史分叉 → 合法清理与历史恢复 → 无越权、无错误目标、无盲目覆盖。
- 过期索引丢失或需要重建、大量无关大记录 → 启动恢复与空闲调度 → 恢复范围完整，正常轮询不反复解密全部记录。

## Regression Test

- 历史剪贴板、文件、多个空间历史密钥 → 取消跨空间加入并重启 → 原资料可读，无 FactoryReset 效果。
- 已消费的一次性邀请 → 清理旧尝试后再次兑换 → 仍被拒绝，不释放防重放证据。
- 原公开按设备移除与 SelfTarget 拒绝 → 原入口调用 → 既有用户行为保持；内部自我放弃不扩大公开权限。
- 当前空间仍有远端投递未确认 → 本机效果完成后发起邀请/加入 → 不被网络待确认长期占用，同时保留既有安全门禁。
- 新旧格式混合、升级中断 → 重新打开 → 向前恢复，无明文业务字段，无覆盖现场迁移。
- 最终执行仓库根 AGENTS 要求的 metadata、check、fmt、架构脚本及 diff 检查；实现阶段另跑相关 crate 测试及双端 host 测试。真实 macOS/iOS/Android 未执行项目明确标为跳过。

# 9. Acceptance Criteria

- [x] 第 11 节六项产品决策已由用户逐项确认并写回本文；不代表实现验收通过。
- [x] Prepared、已决定、应用中、等待结算均可本机结束；没有“先恢复成功才允许取消”的路径。
- [x] 从发起到确认总计 300 秒，无独立准备期限、认证后新期限或重试续期。
- [x] Joiner 未完成、Sponsor/Helper 正式应用未完成时各自到期终止，无需对方通知；邀请方仅缺确认则保留关系并显示未确认。
- [x] 两个新增确认状态持久化，前台可区分；未确认允许有效迟到确认，已终止/移除禁止复活。
- [x] 已终止尝试的迟到旧消息、旧后台任务不能激活旧尝试或影响新成员实例；未确认保留关系的合法迟到确认是明确允许的状态补齐。
- [x] 未知远端提交有持久终止证明，迟到 Add 不会遗留未处理的有效成员。
- [x] 已成功一端能够接收另一端合法放弃；永远断网时不承诺远端立即收敛。
- [x] 新意图保存不等待网络；本机存储/切换故障真实报告且可重试。
- [x] 成员撤销包含账本、安全效果与投递责任，不是只删关系表。
- [x] 用户资料、历史读取能力与防重放证据不被到期清理破坏。
- [x] 所有新增业务记录和元数据密文保存；没有日志或公开错误泄露身份、邀请、地址或密钥。
- [x] 测试覆盖第 8 节中断与恢复边界，真实设备验证与模拟结果分开记录。
- [x] 性能检查证明无新增全仓大记录高频扫描，后台工作和持久空间有明确预算。
- [x] 旧端不支持时提示升级并结束本次配对，不提供旧流程降级。

首版文档交付验证（2026-09-13；以下为首版记录，本次决策回写检查另记，不是以上实现验收）：

| 检查 | 结果 |
| --- | --- |
| `cargo metadata --locked --format-version 1` | 通过 |
| `cargo check --workspace --all-targets --locked` | 通过；现有 HarmonyOS 测试有 unused imports 警告，本次未修改 |
| `cargo fmt --all -- --check` | 通过 |
| `node scripts/architecture/check-rust-style.mjs` | 通过 |
| `node scripts/architecture/check-engine-repository.mjs` | 通过，含隐私检查、OpenMLS 校验及负面夹具 |
| 本文、active 索引、架构圣经的相对链接 | 32 个链接存在，本文 11 节齐全且有索引入口 |
| `git diff --check` | 通过 |
| 新方案双端及真实设备验证 | 跳过：尚未实现，不以现有仓库检查代替新行为验收 |

决策回写与切片细化时的验证：metadata、全 workspace/all-targets check、fmt、两项架构脚本和 diff 检查重新执行通过；check 仍有当时已有的 HarmonyOS 测试 unused imports 警告。此段仅保留开工前的历史检查；S0–S8 的实际实现证据见第 6 节及最终提交记录。

最终实现验收（2026-09-14）：Core 准入 101 项、Application Space 256 项通过（另 1 项显式忽略）、Infra 真实 SQLite 准入 28 项通过；Engine 公开合同 48 项、UniFFI 单元 24 项与公开合同 23 项、HarmonyOS 单元 15 项与公开合同 4 项全部通过。新增双端公开流程单独运行及完整套件中均通过。Engine 双端完整套件共 42 项，结果为 31 项通过、2 项失败、9 项显式忽略：其中“离线成员回来后继续通信”单独重跑通过；“暂停恢复后自动重连”单独重跑仍失败，并在 S7 之前的 `c24fac1a` 基线上得到同样失败，确认不是本计划引入。全 workspace 编译、格式、Rust 规则、架构、隐私与差异门禁通过；HarmonyOS 公开合同仍有既有未使用导入警告。实体设备、跨真实外网和产品前台验收跳过。

# 10. Risks and Trade-offs

- 本机优先终止换来暂时不一致；这是用户目标要求的取舍，不是“所有人已经同时撤销”。永久网络分区下不保证远端收敛。
- 正式决定后的撤销是新业务事实。可能需要补齐内部安全材料才能生成撤销，但不能恢复面向用户的成功状态或正常同步作为前提。
- 已选择复用密文仓储，避免阶段文件再引入一套原子替换与恢复逻辑；存储隔离仍不能消除最终确认丢失，必须保留确认状态与终止证据。
- 邀请方“已完成但未确认”可以长期存在，这是已确认的产品选择，不是到期清理遗漏。用户可移除，合法放弃可撤销，迟到确认可完成；不承诺完全无通信时双方看法一致，也不引入持续续期。
- 时间回拨没有纯本机软件的绝对真实时间保证；过早到期伤害可用性，过晚到期延长占用。策略必须显式，不隐藏在重试经验值里。
- 最小终止证据保留会增加空间；未经安全证明的过早回收更危险。通过受限接纳和压缩控制，后续建立重放拒绝下界后才能回收，不能承诺固定时间全删。
- 跨空间脱离与密钥保留是最大实现风险；必须复用完整切换负责人并做崩溃矩阵，不能使用重置资料接口省略。
- 研究基线后的性能修改会改变存储/投递入口，实施前对齐，防止把已完成拆分重新合并回大记录。

# 11. Open Questions

六项产品问题已由用户逐项讨论并确认，不再作为待选择项。以下是最终决定索引，行为正文以第 5 节为准：

| 原问题 | 已确认决定 |
| --- | --- |
| 1 成功与确认 | 邀请方正式应用完成后显示“已完成配对，等待对方确认”；到期改为“已完成配对，但未被确认”，保留供用户移除。有效迟到确认可变为正常完成；已经移除/终止则不可恢复。加入方尚未完成仍自行到期并撤销本机关系 |
| 2 存储载体 | 在 userdata 独立密文仓储逐条保存过程记录，不为每一步创建文件，与正式配对数据隔离 |
| 3 期限 | 从用户发起本次配对起一共 5 分钟；连接、准备、确认共用，重试、重连、重启均不重新计时 |
| 4 撤销权限 | 加入方只能放弃自身本次尝试；邀请方收到可信放弃后只移除对应关系，不能影响后来重新配对的实例 |
| 5 清理保留 | 清理不再需要的过程材料，保留少量结束记录防止迟到消息复活与邀请码重用；不占配对入口、不影响正常新配对 |
| 6 旧端 | 不支持新规则则提示对方升级并结束本次配对；不走旧流程、不静默降级 |

后续真实设备与产品前台验收不重新打开这些产品选择。异常时钟阈值、容量数值仍不得写成用户指定，也不得借这些参数延长 5 分钟、自动删除未确认关系或丢弃终止证据。若未来发现必须改变已确认行为的技术限制，需要另立决策，不能暗改。
