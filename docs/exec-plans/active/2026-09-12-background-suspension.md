# 后台暂停安全

状态：已修复已复现的租约与取消完成缺口，完整后台安全验收尚未完成。

后续实施以[统一暂停、恢复与中断恢复规格](2026-09-12-unified-runtime-lifecycle.md)为准；本文件保留已完成局部修复与验证证据。

## 完整责任

Engine 生命周期负责人统一拥有暂停、恢复和最终关闭。宿主只调用暂停或恢复，不维护内部资源清理列表。
暂停成功必须意味着停止接受工作、旧工作退出且交接所需文件锁已释放；恢复重新读取持久状态后才接受工作。
失败不能发布暂停成功；重试与恢复由同一生命周期负责人串行处理。最终关闭不可恢复，暂停必须可恢复。

## 最小切片与后续范围

1. 真实 Engine 创建 Space、保存内容、暂停后由独立文件描述符验证锁释放，恢复后验证内容读取与再次暂停。
2. 检查旧操作、后台自动重建、取消与停止超时，禁止暂停后重新占用资源。
3. 核对所有停止工作的共同预算和平台绑定等待语义，不能用停止等待代替完成收尾。
4. 执行相关回归、workspace 检查、格式及架构检查。

## 已有证据

- 交接程序在本机重复得到暂停后锁不可获取、最终关闭后可获取，退出码 101。
- 当前提交与交接源码检查提交一致；该独立程序链接的是已有产物，尚不能代替当前源码重新构建验证。
- 暂停仅停止 session，最终关闭另行永久关闭安全状态；操作等待两次超时后仍返回成功。
- 自动网络恢复有独立生命周期，需继续核查暂停期间的重建入口。

## 已实现

- SessionSupervisor 持有暂停边界，暂停后拒绝网络重建，停止 session 后交接安全资源。
- Clipboard 进程级 blob 物化与 spool 清理共用活动许可；暂停等待当前完整磁盘动作退出，阻止排队动作继续，恢复保留原队列。
- Vault 等待当前读写完成后撤销复用与缓存，暂停期间拒绝新读写；恢复先重新取得文件租约，失败继续保持暂停。
- 完全关闭仍不可恢复，暂停恢复不复用旧安全缓存。
- Engine 取消调用后保留真实在途登记，暂停等待其退出；操作门超时返回失败，之后可重试停止。
- TaskRegistry 和传输超时工作者取消后等待任务退出；资源未析构不能当作关闭完成。
- 取消传输失败后的重试仍执行取消，不因 session 已被取走而跳过。

## 验收状态

- 当前源码真实 Engine 回归：修改前失败（暂停后锁仍被占用），修改后通过。
- 真实 Engine 锁交接、占用时恢复失败、释放后恢复重试、原历史读取、新内容保存与再次暂停：通过。
- Vault 暂停期间另一合法使用者更新目录、恢复读取新内容、旧许可不撤销新运行期：通过。
- TaskRegistry 超时资源析构：新增断言修改前失败，修复后 3 项测试通过。
- Clipboard 后台暂停与真实 blob worker 排队处理：2 项测试通过。
- Engine 库测试：最终修改后重跑，162 通过、2 跳过；含真实暂停恢复与文件导出。host contract 3 项通过。
- Vault 全组：11 项通过。workspace 全目标检查、锁定依赖元数据、格式、Rust 规范、仓库架构和差异检查通过。
- iOS 目标构建及独立签名测试程序安装：通过。连接设备为 iPhone 16 Pro / iOS 27.0 / 24A5408d。
- iOS 真机解锁后，以独立测试程序完成两轮切换到系统设置再返回；自动暂停与恢复均成功，两次后台进程快照均为同一 PID，未重启。首次保存的记录恢复后仍可读取，新增记录后第二轮恢复查询得到 2 条；最终状态 Running、fatal_errors 为 0。第二轮后台至少等待 30 秒，但未取得系统实际挂起状态的直接证据。
- 真机证据保存在受管构建目录 `target/background-safety-ios/`：命令结果、两次后台进程快照和卸载结果。测试结束先执行 shutdown 成功，再卸载独立程序；控制台随后报告 signal 9 对应主动卸载，不是后台测试期间的退出。原有产品与测试程序未修改。
- iOS 真机锁屏、实际系统挂起及稳定系统设备：跳过，不能由上述前后台切换结果代替。
- 其他宿主与正式依赖版本基线：未执行。
- 发布与 Mobile 依赖更新：不在本轮授权范围。

## 尚未完成

- 子运行期停止尚未共享一个端到端预算；等待取消析构不能承诺强行中止任意同步宿主调用。
- UniFFI 10 秒等待上限与真实停止结果、Mobile 结束后台活动之间的完整契约尚未修改。
- 传输中退后台、启动中退后台、快速切换、全部持久资源清单与跨设备同步尚未取得完整矩阵证据。
- 本轮红绿测试基于交接源码提交；已核对正式依赖提交与交接源码提交只有发布脚本和维护记录差异，生产源码及锁定依赖一致，未另建正式提交 checkout 重复编译。

这些缺口不能用本机文件锁测试通过替代，也不能据此宣布 iOS 系统终止问题已完全解决。
